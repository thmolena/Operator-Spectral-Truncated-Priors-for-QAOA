% ============================================================
% Quantum Science and Technology (IOP Publishing) manuscript.
% Converted to standard article class for Overleaf compatibility.
% ============================================================
\documentclass[12pt,a4paper]{article}
\usepackage[margin=1in]{geometry}
\usepackage{authblk}

% --- Packages ---
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{url}
\makeatletter
\g@addto@macro\UrlBreaks{\UrlOrds}
\makeatother
\usepackage[colorlinks=false,bookmarks=true]{hyperref}
\hypersetup{hidelinks}
\usepackage{cleveref}
\usepackage{enumitem}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage[numbers,sort&compress]{natbib}
\providecommand{\Statex}{\item[]}

% Commands originally from IOP class; redefined for article class.
\newcommand{\QST}{Quantum Sci.\ Technol.}
\newcommand{\submitto}[1]{}
\newcommand{\ead}[1]{\par\noindent\texttt{#1}\par}
\newcommand{\ack}[1]{\section*{Acknowledgments}#1}
\newcommand{\orcidlink}[1]{\textsuperscript{\scriptsize\,\raisebox{-0.1ex}{ORCID:}~#1}}

\emergencystretch=1.5em

\newcommand{\ket}[1]{|#1\rangle}
\newcommand{\bra}[1]{\langle#1|}
\newcommand{\braket}[2]{\langle#1|#2\rangle}

\newcommand{\QAOA}{\textnormal{\textsc{QAOA}}}
\newcommand{\GNN}{\textnormal{\textsc{GNN}}}
\newcommand{\GIN}{\textnormal{\textsc{GIN}}}
\newcommand{\UQQAOA}{\textnormal{\textsc{UQ-QAOA}}}
\newcommand{\calG}{\mathcal{G}}
\newcommand{\calV}{\mathcal{V}}
\newcommand{\calE}{\mathcal{E}}
\newcommand{\calD}{\mathcal{D}}
\newcommand{\calN}{\mathcal{N}}
\newcommand{\calL}{\mathcal{L}}
\newcommand{\calF}{\mathcal{F}}
\newcommand{\calH}{\mathcal{H}}
\newcommand{\calP}{\mathcal{P}}
\newcommand{\calB}{\mathcal{B}}
\newcommand{\calT}{\mathcal{T}}
\newcommand{\calS}{\mathcal{S}}
\newcommand{\calR}{\mathcal{R}}
\newcommand{\bbR}{\mathbb{R}}
\newcommand{\bbE}{\mathbb{E}}
\newcommand{\bbP}{\mathbb{P}}
\newcommand{\bbN}{\mathbb{N}}
\newcommand{\mathbbm}[1]{\mathbb{#1}}

% Theorem environments.
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}
\newtheorem{definition}{Definition}

% ============================================================
\submitto{\QST}

\begin{document}

\title{Uncertainty-Calibrated Trust Regions for Query-Efficient QAOA Parameter Search}

\author{Molena Huynh\orcidlink{0000-0002-1234-5678}}
\affil{North Carolina State University, Raleigh, NC 27695, USA}
\ead{molena.huynh@jmp.com}

\maketitle

\begin{abstract}
For low-depth implementations of the Quantum Approximate Optimization
Algorithm (QAOA), the number of objective-function queries required to
identify useful variational parameters is a dominant practical cost.  We
present UQ-QAOA, a framework that converts graph-conditioned predictive
uncertainty into an operational trust-region geometry for query-efficient
parameter search.  A Graph Isomorphism Network predicts a diagonal Gaussian
over QAOA angles; four information sources---neural predictor, local
$k$-nearest-neighbour prior, global population prior, and a trotterized
quantum annealing schedule---are fused via precision-weighted combination to
obtain a posterior mean and anisotropic step sizes for sequential coordinate
refinement.  On an exact-statevector MaxCut benchmark (four graph families,
$n{=}14$, $p{=}3$, $Q{=}18$ matched queries), UQ-QAOA achieves a mean
approximation ratio of $0.865$ versus $0.853$ for the strongest single-source
baseline (paired advantage $+0.012$, bootstrap 95\,\% CI
$[+0.003,+0.024]$).  The method is best-or-tied-best at every intermediate
query budget.  All results are reproducible from deterministic scripts and a
public code artifact.
\end{abstract}

\vspace{2pc}
\noindent{\it Keywords\/}: Quantum Approximate Optimization Algorithm,
graph neural networks, uncertainty quantification, trust-region methods,
MaxCut, variational quantum algorithms

% ============================================================
\section{Introduction}
\label{sec:introduction}
% ============================================================

The Quantum Approximate Optimization Algorithm (QAOA) is a variational quantum
algorithm for combinatorial optimization in which a parameterized quantum
circuit is optimized by a classical outer
loop~\cite{farhi2014,preskill2018,crooks2018}.  For
near-term implementations, the dominant computational cost is
often the number of objective-function queries required to tune the variational
parameters~\cite{zhou2020,wurtz2021,cerezo2021,mcclean2018,arrasmith2021}.
Recent QST studies have treated QAOA performance evaluation, parameter
transfer, hardware execution, circuit cutting, and algorithm selection as
resource-accounting problems rather than only final-objective comparisons
\cite{larkin2022qst,streif2020,moussa2020qst,zhu2023qst,bechtold2023qst}.
Other accepted QST papers emphasize low-depth ansatz design, expectation-value
landscapes, parameter clustering, and sampling efficiency
\cite{vijendran2024qst,ozaeta2022qst,zhang2025qst,drapeau2026qst}.  In that
same spirit, this manuscript evaluates a bounded quantum-software question:
given a fixed number of objective queries, can graph-conditioned uncertainty
allocate those queries more effectively than single-source warm starts or
generic black-box optimizers?  Recent studies have demonstrated QAOA
performance gains through parameter strategies~\cite{shaydulin2024,bonet2023},
reinforcing the importance of query-efficient classical outer loops.  Reducing
the number of objective evaluations is therefore a central algorithmic
objective for low-depth QAOA, especially given the observation that barren
plateaus and noise-induced effects can render gradient-based strategies
inefficient~\cite{wang2021noise,bravyi2020}.

The problem addressed here is not only parameter prediction; it is
measurement and query allocation in variational quantum algorithms.  In
near-term settings, the query count---the number of distinct angle vectors
evaluated by the quantum processor or simulator---is a first-class resource
that constrains the practical utility of QAOA as quantum
software~\cite{cerezo2021,mcclean2018}.  A method that transforms learned
uncertainty into operational search geometry is therefore a quantum-software
contribution: it provides a principled mechanism for trading off exploration
width against a finite evaluation budget.

The key novelty is not ``a GNN predicts QAOA angles''---that capability
exists~\cite{jain2022,liang2024}---but rather that the \emph{graph-conditioned
predictive uncertainty} is repurposed as an \emph{operational trust-region
geometry} for query-efficient parameter search.  Concretely, the predicted
diagonal covariance determines (i)~the anisotropic step sizes of greedy
coordinate refinement, (ii)~the admissible search ellipsoid, and (iii)~
instance-adaptive budget allocation, thereby connecting learned structure to
query-policy decisions through an explicit geometric mechanism.

This work introduces UQ-QAOA, a graph-conditioned trust-region framework for
query-efficient QAOA.  Given a graph instance $G$, a graph neural network
predicts a Gaussian distribution
\begin{equation}
    q_{\phi}(\theta \mid G)
    =
    \mathcal{N}\!\left(\mu_{\phi}(G), \Sigma_{\phi}(G)\right)
\end{equation}
over the QAOA parameters $\theta \in \mathbb{R}^{2p}$.  The predicted mean
$\mu_{\phi}(G)$ initializes local optimization, while the covariance
$\Sigma_{\phi}(G)$ defines an ellipsoidal trust region that constrains the
subsequent search and helps allocate an instance-dependent evaluation budget.
The learned model thus defines a search policy rather than only a point estimate.

The approach is distinct from several related but different strategies:
\emph{parameter-transfer heuristics}~\cite{wurtz2021,shaydulin2023} predict
a single parameter vector without search geometry;
\emph{GNN warm-start methods}~\cite{jain2022,liang2024} use neural predictions
as initializations without covariance-guided refinement;
\emph{black-box optimizers}~\cite{lavrijsen2020,arrasmith2021} build
per-instance surrogates from scratch at each test instance without
cross-instance amortization;
\emph{classical trust-region methods}~\cite{conn2000,nocedal2006} derive
their region from local second-order information (Hessian or model gradient)
rather than from cross-instance learned structure;
and \emph{Bayesian optimization}~\cite{tibaldi2023,cheng2024} constructs a
per-instance GP surrogate from test-time evaluations,
whereas our method amortizes the trust region
across instances via a trained GNN and spends the query budget on direct
objective evaluation rather than surrogate refinement.

The motivation comes from the observed structure of QAOA parameter landscapes:
for fixed depth and bounded-degree graph families, QAOA parameters exhibit
concentration and transferability across related
instances~\cite{brandao2018,akshay2021,galda2021,streif2020,sack2021,lee2021}.  A learned trust-region
policy exploits this transferable structure while avoiding unnecessary
exploration.  The graph-conditioned component uses a Graph Isomorphism Network
(GIN)~\cite{xu2019,weisfeiler1968} augmented with spectral positional
information~\cite{dwivedi2023,lim2023,kreuzer2021}.

The contributions of this paper are as follows.
\begin{enumerate}
    \item \textbf{Uncertainty as search geometry.}
    We use graph-conditioned covariance not as a confidence score alone, but as
    the metric that defines anisotropic trust-region scales and the step sizes
    of coordinate refinement in QAOA angle space.  This converts a learned
    distributional object into three operational decisions: where to center,
    how wide to search per coordinate, and how to distribute a fixed query
    budget.

    \item \textbf{Posterior fusion of learned and physics-informed priors.}
    The search policy combines a GIN predictor, local $k$-NN information, a
    global population prior, and a TQA prior into a single posterior mean and
    diagonal covariance via precision-weighted (inverse-variance) combination.
    The fusion formula is explicit and non-iterative.

    \item \textbf{Controlled matched- and dimension-scaled-budget evidence.}
    The main benchmark uses depth $p{=}3$ with matched query curves, while
    $p{=}4$--$7$ experiments use dimension-scaled budgets $Q_p=5+4p$ and
    additional query-curve points for validation.  All evaluation is
    exact-statevector on unweighted MaxCut instances; claims are bounded to
    this CPU-only evidence.

    \item \textbf{Fair local-search baselines and ablations.}
    We include TQA with the identical greedy coordinate-refinement protocol,
    along with ablations that remove covariance, shuffle covariance, remove
    individual priors, or disable refinement.  We also include standard
    black-box optimizers (DE, CMA-ES, GP-EI, Nelder--Mead) under the same
    budget.

    \item \textbf{Calibration and finite-shot diagnostics.}
    Because uncertainty is the central object, we report trust-region coverage,
    calibration diagnostics, and finite-shot/noisy-objective sensitivity where
    available.

    \item \textbf{Auditable computational artifact.}
    The scientific results are generated by deterministic Python scripts
    (\texttt{code/generate\_all.py}), while
    a C++20 reference interface and benchmark record the evaluator contract,
    compiler settings, thread-level scaling behavior, and kernel structure
    required for future optimized threaded or accelerator-backed implementations.
    The code separates candidate generation from evaluator kernels to support
    future hardware or accelerated backends without changing the algorithmic
    comparison.
\end{enumerate}

\paragraph*{Manuscript organization.}
Section~\ref{sec:background} defines the MaxCut/QAOA objective and the
query-budget model.  Section~\ref{sec:related} places the work relative to
QAOA transfer, learning-assisted initialization, Bayesian optimization,
trust-region methods, uncertainty quantification, and accepted QST studies.
Sections~\ref{sec:method}--\ref{sec:theory} describe the posterior-fusion
policy and the limited guarantees used to interpret it.
Sections~\ref{sec:setup}--\ref{sec:results} give the research design and
bounded exact-statevector evidence.  Sections~\ref{sec:discussion} and
\ref{sec:conclusion} summarize limitations, reproducibility, and future work.

% ============================================================
\section{Problem Setting and Preliminaries}
\label{sec:background}
% ============================================================

\subsection{MaxCut and the QAOA objective}

Let $G=(V,E)$ be an undirected graph with $n=|V|$ vertices.  The MaxCut cost
Hamiltonian is
\begin{equation}
    H_C(G)
    =
    \sum_{(i,j)\in E}
    \frac{1 - Z_i Z_j}{2}.
\end{equation}
At QAOA depth $p$, the parameter vector is
$\theta = (\gamma_1,\ldots,\gamma_p,\beta_1,\ldots,\beta_p) \in \Theta_p$,
where the parameter domain is
\begin{equation}
    \Theta_p = [0, 2\pi)^p \times [0, \pi)^p \subset \mathbb{R}^{2p},
\end{equation}
corresponding to the $2\pi$-periodicity of the cost phase and $\pi$-periodicity
of the mixer rotation.  In the implementation, angles that leave these domains
are projected back by modular wrapping.
The QAOA state is
\begin{equation}
    \ket{\psi_G(\theta)}
    =
    \prod_{\ell=1}^{p}
    e^{-i\beta_{\ell} H_M}
    e^{-i\gamma_{\ell} H_C(G)}
    \ket{+}^{\otimes n},
\end{equation}
with mixer $H_M = \sum_{i=1}^{n} X_i$, and the exact QAOA objective is
\begin{equation}
    f_G(\theta)
    =
    \bra{\psi_G(\theta)} H_C(G) \ket{\psi_G(\theta)}.
\end{equation}
The approximation ratio is $r_G(\theta) = f_G(\theta)/C_G^\star$,
where $C_G^\star$ is the optimal cut value.

\subsection{Objective-query model}

We model QAOA optimization as an interaction between a classical optimizer
and an objective oracle.  One exact query returns $f_G(\theta)$; in the
finite-shot model, a noisy estimate is
\begin{equation}
    \widehat{f}_{G,S}(\theta)
    =
    \frac{1}{S}
    \sum_{a=1}^{S}
    C_G(z_a),
    \quad
    z_a \sim |\braket{z_a}{\psi_G(\theta)}|^2.
\end{equation}
The cost of an optimization run is measured by the number of objective
queries $Q = \#\{\theta \text{ evaluated}\}$ and, in the finite-shot
setting, by the total shot budget $B = \sum_{q=1}^{Q} S_q$.
Algorithms are evaluated through the query-performance curve
$Q \mapsto \max_{1\leq q\leq Q} r_G(\theta_q)$.

A method that achieves a slightly higher objective only after many more
queries may be less useful in a measurement-limited regime than one that
reaches comparable quality using substantially fewer queries.  We therefore
evaluate algorithms both by their best approximation ratio at a fixed budget
and by the query count required to reach a target ratio $\tau$:
\begin{equation}
    Q_G(\tau) = \min\{q : r_G^{\mathrm{best}}(q) \geq \tau\}.
\end{equation}

\subsection{Learning-assisted parameter selection}

A common strategy is to train a point predictor $h_\phi: G \mapsto \widehat{\theta}_G$.
Such a predictor may provide a useful warm start, but does not answer two
operational questions: how far from the prediction should the optimizer search,
and how many queries should be spent on that instance?

To address these, we consider probabilistic predictors
$h_{\phi}: G \mapsto (\mu_{\phi}(G), \Sigma_{\phi}(G))$,
where the induced distribution is
$\theta \mid G \sim \mathcal{N}(\mu_{\phi}(G), \Sigma_{\phi}(G))$.
The predicted mean is a warm start; the covariance constructs the local search region.

\subsection{Graph-conditioned trust regions}

A probabilistic predictor maps a graph to a Gaussian:
$h_{\phi}: G \mapsto (\mu_{\phi}(G), \Sigma_{\phi}(G))$.
The induced trust region is
\begin{equation}
    \mathcal{T}_{\phi,\rho}(G)
    =
    \left\{
    \theta\in\Theta_p:
    (\theta{-}\mu_{\phi}(G))^{\!\mathsf{T}}
    (\Sigma_{\phi}(G){+}\lambda I)^{-1}
    (\theta{-}\mu_{\phi}(G))
    \leq \rho^2
    \right\},
\end{equation}
where $\rho>0$ is a radius parameter and $\lambda>0$ ensures numerical
stability.  For diagonal covariance, each QAOA angle is searched in an
interval scaled by its predicted uncertainty.  The local search problem is
$\max_{\theta\in \mathcal{T}_{\phi,\rho}(G)} f_G(\theta)$.

% ============================================================
\section{Relation to Prior Work}
\label{sec:related}
% ============================================================

\paragraph{Parameter concentration and transfer.}
QAOA parameters exhibit concentration and transferability across related
instances for fixed depth and bounded-degree
families~\cite{farhi2014,zhou2020,akshay2021,brandao2018,shaydulin2023,streif2020,crooks2018,zhang2025qst}.
This has motivated transfer heuristics based on interpolation,
nearest-neighbor matching, and parameter
databases~\cite{wurtz2021,lotshaw2021,shaydulin2023,sack2021,moussa2022,wurtz2022}.
Warm-starting from classical solutions~\cite{egger2021} and building
spatial symmetries into the circuit~\cite{sauvage2024,skolik2023} are complementary
approaches for reducing search cost.  Our work builds on
this premise but addresses a different question: rather than producing a
single transferred parameter vector, we learn a graph-conditioned distribution
whose covariance determines the search geometry.  The posterior / prior
fusion explicitly incorporates transferability information (via the $k$-NN
and global priors) together with a physics-informed TQA schedule, but
converts these into a joint covariance that governs step sizes---not merely
a single point estimate.

\paragraph{Learning-based initialization.}
Neural networks and graph neural networks have been used to predict initial
QAOA angles from instance
data~\cite{amosy2022,jain2022,liang2024,khairy2020}.  These methods
typically evaluate success through point-prediction quality (e.g., cosine
similarity or mean-squared error between predicted and optimal parameters).
More broadly,
learning to optimize combinatorial problems with GNNs has received significant
attention~\cite{khalil2017,bengio2021,cappart2023,schuetz2022,kool2019},
and amortized optimization~\cite{amos2023,shu2022,chen2022} provides a
general framework for training models that map problem instances to solutions.
Meta-learning~\cite{finn2017} offers an alternative paradigm for rapid
adaptation.  Our method changes the role of the learned model: it returns
not only a warm-start point but an uncertainty estimate $\Sigma_{\phi}(G)$ that
constructs the local optimization domain and determines direction-dependent
search widths, connecting initialization to query allocation.

\paragraph{Bayesian and derivative-free optimization.}
Bayesian optimization, evolutionary methods, and derivative-free optimizers
have been studied as classical outer
loops~\cite{tibaldi2023,cheng2024,lavrijsen2020,arrasmith2021}.
The Nelder--Mead simplex method~\cite{nelder1965} remains a common
derivative-free choice despite its known limitations in moderate dimension.
Standard BO builds a per-instance GP surrogate from objective evaluations on the
current instance; each test-time query refines the surrogate before selecting
the next point.  Our method uses a graph-conditioned model trained offline
to propose a trust region \emph{before} any test-time queries, providing an
amortized cross-instance prior that requires no per-instance surrogate fitting.
The query budget is spent directly on objective evaluation rather than on
surrogate refinement.  This is complementary:
the learned trust region can serve as an informed initialization for subsequent
Bayesian optimization if additional budget becomes available.

\paragraph{Trust-region methods.}
Classical trust-region methods~\cite{conn2000,nocedal2006} restrict local
steps to regions where a model is reliable.  Our approach differs in two
ways: (i)~the region is predicted from the graph (not from local Taylor
approximation or a Hessian estimate computed during optimization), and
(ii)~the geometry is probabilistic (anisotropic covariance
determines direction-dependent search widths rather than a uniform radius).
Like classical trust-region contraction, our method halves step sizes upon
stagnation; unlike it, the initial geometry is learned cross-instance rather
than inferred from local curvature.

\paragraph{Uncertainty quantification in deep learning.}
Predictive uncertainty has been modeled through dropout~\cite{gal2016},
deep ensembles~\cite{lakshminarayanan2017}, heteroscedastic
regression~\cite{nix1994,kendall2017}, and calibrated
regression~\cite{kuleshov2018}.  Surveys by Abdar et
al.~\cite{abdar2021}, H\"ullermeier and Waegeman~\cite{hullermeier2021},
and Psaros et al.~\cite{psaros2023} provide comprehensive overviews.
Our work instantiates heteroscedastic regression for QAOA angles and
uses the predicted variance as operational search geometry.

\paragraph{Conformal prediction.}
Distribution-free coverage guarantees through conformal
prediction~\cite{vovk2005,angelopoulos2021,romano2019} have motivated
our conformal trust-region radius (Proposition~\ref{prop:conformal}).

\paragraph{Measurement cost and resource accounting.}
A growing body of work emphasizes that variational quantum algorithms should
be assessed by the quantum resources required to obtain a given
value~\cite{cerezo2021,arrasmith2021,sim2021,mcclean2018,bonet2023,larkin2022qst,bechtold2023qst}.  We adopt this
perspective: performance is reported as a function of objective-query count
under matched budgets.

\paragraph{Accepted QST studies most closely related to this manuscript.}
Several accepted QST papers provide the closest methodological context for the
present submission.  Larkin et al. evaluate QAOA for MaxCut using
approximation-ratio and sampling-based metrics, emphasizing that practical
performance depends on the cost of obtaining useful samples rather than on an
idealized final objective alone~\cite{larkin2022qst}.  Streif and Leib show
that QAOA parameters can be inferred classically and transferred, motivating
outer-loop reduction through problem-structure information~\cite{streif2020}.
Moussa, Calandra, and Dunjko frame near-term quantum optimization as an
algorithm-selection problem in which machine learning identifies instances
where QAOA is expected to be useful~\cite{moussa2020qst}.  Vijendran et al.
and Ozaeta et al. provide accepted QST examples of bounded low-depth QAOA
claims supported by explicit MaxCut/Ising benchmarks and landscape analysis
\cite{vijendran2024qst,ozaeta2022qst}.  Bechtold et al. offer a useful model
for QST-style research-question framing, resource accounting, limitations, and
data-availability reporting in QAOA experiments on NISQ devices
\cite{bechtold2023qst}.  Our manuscript follows the same conservative pattern:
state the resource question, control the benchmark, expose the data/code path,
and keep hardware and advantage claims outside the evidence.

\paragraph{GNN architectures for structured prediction.}
Message-passing neural networks~\cite{gilmer2017}, GraphSAGE~\cite{hamilton2017},
and structure-aware variants~\cite{dai2016,you2020} have established graph
neural networks as expressive function approximators over combinatorial
structures.  Our choice of GIN~\cite{xu2019} is motivated by its maximally
expressive injective aggregation among first-order message-passing schemes.

\paragraph{High-performance statevector simulation and artifact reproducibility.}
Large-scale statevector simulation is now standard infrastructure for
variational quantum algorithm
research~\cite{haner2017petabyte,jones2019quest,bayraktar2023cuquantum}.  At
the artifact level, reproducibility requires explicit numerical contracts:
deterministic candidate ordering, fixed floating-point reduction semantics, and
bitwise-reproducible evaluator output under identical seeds.  We use a small-$n$
reference evaluator ($n\le 18$) deliberate for algorithmic auditing; the kernel
boundary documented in \cref{sec:hpc_framework} enables future backends---CPU
threaded, GPU, or task-runtime---to replace the evaluator without changing the
scientific comparison.

\paragraph{Gap addressed by this work.}
Existing learning-assisted QAOA works predict parameters or provide warm starts
but do not use the predicted uncertainty as a test-time resource-allocation
object.  The central novelty of UQ-QAOA is that the learned covariance
constructs the local search geometry and the dimension-wise step sizes for
refinement, connecting initialization quality to query-budget distribution
through an explicit geometric mechanism.  Specifically:
(i)~the TQA schedule is used both as one of four prior sources \emph{and}
as an early-budget safety prefix, separating the contribution of posterior
fusion from the strongest physics-informed anchor;
(ii)~no full GP surrogate is maintained---the method is not Bayesian
optimization in disguise, but a learned-geometry local-search policy;
(iii)~the method does not require gradient evaluations or Hessian estimates
from the quantum backend.

% ============================================================
\section{Graph-Conditioned Gaussian Prediction and Trust-Region Search}
\label{sec:method}
% ============================================================

\subsection{Overview}

For each graph $G$, the algorithm constructs
$q_{\phi}(\theta \mid G) = \mathcal{N}(\mu_{\phi}(G), \Sigma_{\phi}(G))$
and defines a trust region $\mathcal{T}_{\phi,\rho}(G)$.
At test time, a small number of objective evaluations inside
$\mathcal{T}_{\phi,\rho}(G)$ are performed and the best parameter is returned.

\subsection{Graph representation}

We represent $G=(V,E)$ by its adjacency matrix and node features
(normalized degree, clustering statistics).  A Graph Isomorphism
Network (GIN)~\cite{xu2019,weisfeiler1968,gilmer2017} with learnable
$\varepsilon$ and two-layer MLPs with layer
normalization~\cite{ba2016} performs message passing:
\begin{equation}
    h_i^{(\ell+1)}
    =
    \mathrm{MLP}^{(\ell)}
    \!\left(
    (1+\varepsilon^{(\ell)})\, h_i^{(\ell)}
    +
    \sum_{j\in \mathcal{N}(i)}
    \tilde{A}_{ij}\,
    h_j^{(\ell)}
    \right),
\end{equation}
where $\tilde{A} = D^{-1}(A+I)$.  A graph-level embedding is obtained by
concatenating mean and max pooling of the final node embeddings:
\begin{equation}
    g_{\phi}(G) = \left[\tfrac{1}{n}\textstyle\sum_i h_i^{(L)}
    \;;\; \max_i h_i^{(L)}\right].
\end{equation}

\subsection{Gaussian prediction head}

The graph embedding is mapped to a mean $\mu_{\phi}(G) = W_{\mu}\,g_{\phi}(G) + b_{\mu}$
and diagonal covariance
$\Sigma_{\phi}(G) = \mathrm{diag}(\sigma_{\min}^2 + \mathrm{softplus}(a_{\phi,k}(G)))$.

\subsection{Training objective}

The primary training loss is the Gaussian negative log-likelihood:
\begin{equation}
    \mathcal{L}_{\mathrm{nll}}(\phi)
    =
    \frac{1}{2N}
    \sum_{i=1}^{N}
    \sum_{k=1}^{2p}
    \left[
    \frac{(\theta_{i,k}^{\mathrm{ref}} - \mu_{\phi,k}(G_i))^2}{\sigma_{\phi,k}^2(G_i)}
    + \log \sigma_{\phi,k}^2(G_i)
    \right],
\end{equation}
with weight-decay regularization:
$\mathcal{L}(\phi) = \mathcal{L}_{\mathrm{nll}}(\phi) + \eta_{\mathrm{wd}}\|\phi\|_2^2$.
Reference parameters $\theta_i^{\mathrm{ref}}$ are obtained by offline
high-budget optimization on training graphs.

The first term encourages accurate prediction; the second penalizes
unnecessary covariance inflation.  A model that underestimates uncertainty
incurs a large penalty when reference parameters fall outside the predicted
region; a model that inflates uncertainty everywhere produces trust regions
that are too large to be query-efficient.

\begin{remark}
In the submitted artifact, the GIN is trained with Gaussian NLL and
weight decay only.  At the tested scale ($240$ training graphs, $p{=}3$),
this produces sufficiently calibrated covariances for the four-source
posterior.  An explicit calibration regularizer is a principled extension
for settings where NLL alone underestimates uncertainty.  Graph contrastive
learning~\cite{you2020,khosla2020,chen2020simclr,oord2018,suresh2021}
provides a potential augmentation strategy for learning richer graph
embeddings in the data-limited regime.
\end{remark}

\subsection{Adaptive query budget}
\label{sec:adaptive}

The covariance determines how many queries are spent on each instance.
Define a scalar uncertainty score
$u_{\phi}(G) = \frac{1}{d}\mathrm{tr}\,\Sigma_{\phi}(G)$
and allocate:
\begin{equation}
    Q_G = Q_{\min} + \left\lceil (Q_{\max}-Q_{\min})
    \frac{u_{\phi}(G)-u_{\min}}{u_{\max}-u_{\min}+\varepsilon}\right\rceil.
\end{equation}
This allocates more evaluations to uncertain instances.  For fair comparison,
adaptive methods are evaluated under matched average budget:
$\sum_G Q_G = |\mathcal{D}_{\mathrm{test}}|\,Q_{\mathrm{avg}}$.

\subsection{Trust-region construction}
\label{sec:trust_region}

\begin{definition}[Trust region]
\label{def:trust_region}
$\mathcal{T}_{\phi,\rho}(G) =
\bigl\{\theta\in\Theta_p:
(\theta{-}\mu_{\phi}(G))^{\!\mathsf{T}}
(\Sigma_{\phi}(G){+}\lambda I)^{-1}
(\theta{-}\mu_{\phi}(G))
\leq \rho^2 \bigr\}$.
\end{definition}

The radius $\rho$ is selected on a validation set.  For diagonal covariance:
\begin{equation}
    \sum_{k=1}^{2p}
    \frac{(\theta_k - \mu_{\phi,k}(G))^2}{\sigma_{\phi,k}^2(G)+\lambda}
    \leq \rho^2.
\end{equation}

\subsection{Four-source posterior and algorithm}

The trust region fuses four independent sources via precision-weighted
combination.  The GIN predicts $(\mu_{\mathrm{GIN}}, \Sigma_{\mathrm{GIN}})$.
The local prior uses the median and variance of optimal angles from the
$k{=}5$ nearest training graphs.  The global prior uses training-library
statistics.  The TQA prior centers on the linear-ramp schedule.  The posterior is:
\begin{equation}
    \Sigma_{\mathrm{post}}^{-1} = {\Sigma_{\mathrm{GIN}}'}^{-1}
    + \Sigma_{\mathrm{loc}}^{-1}
    + \Sigma_{\mathrm{glob}}^{-1}
    + \Sigma_{\mathrm{TQA}}^{-1},
\end{equation}
where $\Sigma_{\mathrm{GIN}}'$ is calibrated by flooring at the local prior
variance.  Using TQA as a prior source is equitable: the TQA schedule is
computed from depth $p$ without consulting test-instance evaluations, so every
method has access to the same schedule information.  The budget is spent on
objective evaluations in Phase~2.

The algorithm proceeds in three budget-accounted stages.  Phase~0 evaluates a
short TQA safety prefix.  This makes the proposed policy dominance-preserving
with respect to the strongest physics-informed early-budget baseline: at each
early query count, UQ-QAOA has evaluated the same TQA candidate available to
the TQA curve before spending additional queries on learned geometry.  Phase~1
then evaluates nonduplicate deterministic posterior anchors (TQA, posterior
mean, GIN, local $k$-NN median, global median).  Phase~2 performs sequential
greedy coordinate refinement around the empirically best anchor: for each
dimension $j$, the algorithm probes
$\pm\delta_j$ from the current best, updating the incumbent on improvement.
After a full pass without improvement, step sizes are halved (trust-region
contraction).  Step sizes are $\delta_j = \mathrm{clip}(0.5\sqrt{[\Sigma_{\mathrm{post}}]_{jj}},\,0.05,\,0.30)$.

\begin{algorithm}[t]
\caption{UQ-QAOA: Phase 1 --- four-source posterior construction}
\label{alg:gctr}
\begin{algorithmic}[1]
\Require Trained predictor $h_{\phi}$; training library $\mathcal{L}$;
    graph $G$ with features $f(G)$; depth $p$; budget $Q_G$
\Ensure Posterior mean $\mu_{\mathrm{post}}$, step sizes $\delta_j$, anchors

\Statex \textbf{--- Source 1: GIN prediction ---}
\State $(\mu_{\mathrm{GIN}},\Sigma_{\mathrm{GIN}}) \leftarrow h_{\phi}(G)$

\Statex \textbf{--- Source 2: Local $k$-NN prior ---}
\State $\mathcal{N}_k \leftarrow k$-nearest neighbors of $f(G)$ in $\mathcal{L}$
\State $\mu_{\mathrm{loc}} \leftarrow \mathrm{median}\{\theta^*_i : i \in \mathcal{N}_k\}$;
    $\Sigma_{\mathrm{loc}} \leftarrow \mathrm{diag}(\mathrm{var}\{\theta^*_i : i \in \mathcal{N}_k\})$

\Statex \textbf{--- Source 3: Global population prior ---}
\State $\mu_{\mathrm{glob}} \leftarrow \mathrm{median}\{\theta^*_i : i \in \mathcal{L}\}$;
    $\Sigma_{\mathrm{glob}} \leftarrow \mathrm{diag}(\mathrm{var}\{\theta^*_i : i \in \mathcal{L}\})$

\Statex \textbf{--- Source 4: TQA physics-informed prior ---}
\State $\theta_{\mathrm{TQA}} \leftarrow$ TQA linear ramp at depth $p$
\State $\Sigma_{\mathrm{TQA}} \leftarrow \mathrm{diag}(\mathrm{med}_{i\in\mathcal{L}}(\theta^*_i - \theta_{\mathrm{TQA}})^2)$

\Statex \textbf{--- Posterior fusion ---}
\State $\Sigma_{\mathrm{GIN}}' \leftarrow \max(\Sigma_{\mathrm{GIN}},\, \Sigma_{\mathrm{loc}})$
\State $\Sigma_{\mathrm{post}}^{-1} \leftarrow {\Sigma_{\mathrm{GIN}}'}^{-1} + \Sigma_{\mathrm{loc}}^{-1}
    + \Sigma_{\mathrm{glob}}^{-1} + \Sigma_{\mathrm{TQA}}^{-1}$
\State $\mu_{\mathrm{post}} \leftarrow \Sigma_{\mathrm{post}}
    ({\Sigma_{\mathrm{GIN}}'}^{-1}\mu_{\mathrm{GIN}}
    + \Sigma_{\mathrm{loc}}^{-1}\mu_{\mathrm{loc}}
    + \Sigma_{\mathrm{glob}}^{-1}\mu_{\mathrm{glob}}
    + \Sigma_{\mathrm{TQA}}^{-1}\theta_{\mathrm{TQA}})$
\State $\delta_j \leftarrow \mathrm{clip}(0.5\sqrt{[\Sigma_{\mathrm{post}}]_{jj}},\,0.05,\,0.30)$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[t]
\caption{UQ-QAOA: Phase 2 --- safety prefix, anchor evaluation, and greedy coordinate refinement}
\label{alg:gctr2}
\begin{algorithmic}[1]
\Require $\mu_{\mathrm{post}}$, anchors
    $\{\theta_{\mathrm{TQA}},\mu_{\mathrm{post}}, \mu_{\mathrm{GIN}}, \mu_{\mathrm{loc}}, \mu_{\mathrm{glob}}\}$,
    step sizes $\delta_j$, objective $f_G$, budget $Q_G$
\Ensure Best parameter $\widehat{\theta}$ and cached value $\widehat{y}$ within $Q_G$ evaluations

\Statex \textbf{--- Phase 2a: TQA safety prefix ---}
\State Evaluate the first safety-prefix candidates from the TQA schedule/refinement list, subject to budget
\State Update $\widehat{\theta}$ and $\widehat{y}$ from the best safety-prefix value

\Statex \textbf{--- Phase 2b: Posterior-anchor evaluation ---}
\State Evaluate each nonduplicate anchor until the remaining budget is zero
\State $\widehat{\theta} \leftarrow \arg\max_q y_q$ and $\widehat{y}\leftarrow \max_q y_q$

\Statex \textbf{--- Phase 2c: Sequential greedy coordinate refinement ---}
\While{budget remaining}
    \State $\mathrm{improved} \leftarrow \mathrm{False}$
    \For{$j = 1, \ldots, 2p$}
        \For{$\eta \in \{+1, -1\}$}
            \State $\theta^{\mathrm{new}} \leftarrow \Pi_{\Theta_p}(\widehat{\theta} + \eta\delta_j e_j)$
            \State Evaluate $y \leftarrow f_G(\theta^{\mathrm{new}})$ and decrement the remaining budget
            \If{$y > \widehat{y}$}
                \State $\widehat{\theta} \leftarrow \theta^{\mathrm{new}}$;
                    $\widehat{y}\leftarrow y$; $\mathrm{improved} \leftarrow \mathrm{True}$; \textbf{break}
            \EndIf
        \EndFor
    \EndFor
    \If{not $\mathrm{improved}$}
        \State $\delta \leftarrow \delta / 2$ \Comment{trust-region contraction}
    \EndIf
\EndWhile
\State \Return $(\widehat{\theta},\widehat{y})$
\end{algorithmic}
\end{algorithm}

The key algorithmic distinction is that UQ-QAOA first preserves the strongest
physics-informed early-budget behavior through the TQA safety prefix, then
\emph{sequentially adapts} remaining probes toward the most promising learned
trust-region anchor---unlike single-source baselines, which pre-commit all
candidates without posterior fusion.

\paragraph{Precision-weighted fusion and independence approximation.}
The four-source combination is a ``precision-weighted posterior-style'' fusion
using the inverse-covariance weighting formula from independent Gaussian
evidence.  The sources are not truly independent Bayesian likelihoods; the
$k$-NN and GIN sources share training data, and the TQA schedule is available
to all methods.  We adopt this engineering approximation because it produces a
well-defined center and step-size vector with a clear geometric interpretation:
sources with smaller predicted variance exert stronger pull on the joint
estimate.  In the ablation (\cref{tab:ablation}), the fusion outperforms any
single source, confirming that the approximation is operationally effective.

\paragraph{Why the $k$-NN prior dominates some ablations.}
Removing the $k$-NN prior produces the largest isolated performance drop
($0.865 \to 0.644$, \cref{tab:ablation}).  This occurs because the $k$-NN
median provides a strong anchor in structured graph families: when the test
graph has nearby training neighbors, their optimized parameters are already
near-optimal.  The GIN alone cannot reliably recover this information without
the nonparametric neighborhood; the fusion framework allows both to contribute.

\paragraph{Diagonal covariance and angle periodicity.}
The covariance $\Sigma_{\phi}(G)$ is diagonal throughout: both the GIN
prediction head and the posterior fusion operate coordinate-wise.  This is a
simplification; QAOA angle correlations across layers exist~\cite{zhou2020}
and a full-covariance extension is a natural direction.  Additionally, the
Gaussian model does not respect the torus topology of $\Theta_p$: wrapping a
Gaussian at domain boundaries introduces a minor model--data mismatch.  In
practice, the trained covariances are compact ($\sigma_k \ll \pi$), so
boundary effects are negligible for the tested depths.  Torus-aware modeling
(e.g., wrapped normal or von Mises products) is listed as future work.

\paragraph{Implementation invariants.}
The following deterministic invariants are maintained across all evaluator
paths: (i)~candidate ordering is fixed by seed and method; (ii)~duplicate
anchors are detected and removed before budget expenditure; (iii)~angles
leaving $\Theta_p$ are projected via modular wrapping before evaluation;
(iv)~the budget counter is decremented exactly once per unique evaluation;
(v)~the incumbent update uses strict inequality.  These invariants ensure
that algorithmic comparisons are not confounded by implementation-level
nondeterminism.

\begin{remark}[Conformal trust-region radius]
\label{rem:conformal_tr}
At inference time, the $\chi^2_{2p}(\alpha)$ radius can be replaced by a
validation-set conformal quantile, yielding a trust region with finite-sample
coverage under exchangeability. In the present work this is a calibration
option rather than a central empirical claim.
\end{remark}

\subsection{Baselines induced by ablation}

The framework defines several ablations: (i)~mean-only search
($\theta_1 = \mu_{\phi}(G)$), (ii)~isotropic trust region
($\Sigma_{\phi}(G) = \sigma^2 I$), (iii)~fixed box around the mean,
(iv)~no adaptive query allocation, and (v)~no calibration regularization.
These distinguish the value of graph-conditioned uncertainty from a
learned warm start alone; the corresponding ablation results appear in
\cref{tab:ablation}.

% ============================================================
\section{Computational Framework}
\label{sec:hpc_framework}
% ============================================================

The computational framework separates algorithmic policy from backend
evaluation.  Reproducible Python scripts (NumPy/SciPy) generate all reported
figures and tables; a C++20 reference file specifies kernel interfaces for
future optimized backends.

The implementation is organized around a narrow kernel boundary: the
algorithmic layer emits a deterministic stream of candidate parameter vectors;
the backend layer evaluates those candidates and returns objective values.
This separation makes the scientific comparison independent of the evaluator
implementation and allows replacement by optimized CPU-threaded, GPU, or
task-runtime backends without changing the algorithmic comparison.  Any
replacement backend must preserve the same candidate sets, seeds,
floating-point reduction policy, and output schemas.

The contract documents what an optimized backend must satisfy to be a valid
drop-in replacement for the reference evaluator: a faster backend is useful
only if it returns the same candidate ordering under the same seeds and the
same query budget.

% Contract specification; see code/cpp/ for C++20 reference implementation.
\begin{table}[t]
\caption{Backend contract invariants for reproducible statevector evaluation.
\label{tab:backend_contract}}
\scriptsize
\begin{tabular}{@{}p{0.24\columnwidth}p{0.34\columnwidth}p{0.30\columnwidth}@{}}
\toprule
Interface element & HPC concern & Reproducibility invariant \\
\midrule
Candidate batch & Parallel work distribution across graph--angle pairs &
Same candidate list, order, seeds, and budget accounting \\
Cost-phase kernel & Memory bandwidth, vectorized complex multiply, cache locality &
Same diagonal phase convention and graph edge ordering \\
Mixer kernel & Pairwise bit-flip updates, SIMD/vector width, thread scheduling &
Same angle convention and amplitude update order within a deterministic kernel \\
Expectation reduction & Floating-point associativity, thread-local accumulators &
Fixed merge order and identical objective schema \\
Output trace & Auditability, regression testing, benchmark replay &
Per-query graph id, method id, angle vector, value, seed, and incumbent \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\caption{Reproducibility platform.
\label{tab:platform}}
\scriptsize
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Component} & \textbf{Specification} \\
\midrule
CPU            & Apple M2 Pro, 10-core, 3.49~GHz \\
Memory         & 16~GB unified LPDDR5 \\
Python         & 3.11.7; NumPy 1.26.4; SciPy 1.12.0 \\
C++ compiler   & Apple Clang~17.0.0, \texttt{-O3 -std=c++20 -march=native -pthread} \\
Global seed    & \texttt{260424803} \\
\bottomrule
\end{tabular}
\end{table}

The C++20 reference benchmark achieves $7.91\times$ speedup at 8 threads for
batched $n{=}14$ evaluation and identifies the mixer kernel as the dominant CPU
cost ($1.9\times$ the phase-kernel time per layer).  Full microbenchmark
methodology, kernel profiles, evaluator validation tests, and CSV logs are
provided in the supplementary artifact.

% ============================================================
\section{Guarantees for Learned Trust-Region Search}
\label{sec:theory}
% ============================================================

This section provides the supporting statements used to interpret the
empirical results.  The theoretical content formalizes when a learned trust
region reduces the number of objective queries needed to reach a given solution
quality.

\subsection{Best-of-$K$ sampling inside a trust region}

\begin{theorem}[Exact-query best-of-$K$ guarantee]
\label{thm:bestofk}
\textbf{Assumptions:} (A1)~The $K$ candidate samples are drawn independently
from a proposal distribution supported on the trust region
$\mathcal{T}_{\phi,\rho}(G)$.  (A2)~The proposal assigns probability mass at
least $\alpha_G(\varepsilon) > 0$ to the $\varepsilon$-optimal subset
$\mathcal{A}_{G,\varepsilon} = \{\theta \in \mathcal{T}: f_G(\theta) \geq
f_G^{\mathcal{T}} - \varepsilon\}$.

Fix a graph $G$.
If $K \geq \log(1/\delta)/\alpha_G(\varepsilon)$,
then with probability at least $1-\delta$,
\begin{equation}
    f_G(\widehat{\theta}_K) \geq f_G^{\mathcal{T}}-\varepsilon.
\end{equation}
More precisely,
$\Pr[f_G(\widehat{\theta}_K) < f_G^{\mathcal{T}}-\varepsilon]
\leq (1-\alpha_G(\varepsilon))^K$.
\end{theorem}

\begin{proof}
The failure event requires all $K$ samples to miss
$\mathcal{A}_{G,\varepsilon}$.  By independence,
$(1-\alpha_G(\varepsilon))^K \leq \exp(-K\alpha_G(\varepsilon)) \leq \delta$.
\end{proof}

In plain language, a calibrated trust region helps only if it concentrates the
proposal distribution near good parameters. The benefit is therefore geometric:
shrinking search volume is useful when the shrinkage preserves near-optimal
mass rather than excluding it.

For an ellipsoidal trust region with locally $L_G$-Lipschitz objective
(a heuristic lower bound, valid when the landscape is smooth near the optimum),
\begin{equation}
    \alpha_G(\varepsilon)
    \gtrsim
    \frac{(\varepsilon/L_G)^{d}}{\rho^{d}\det(\Sigma_{\phi}(G)+\lambda I)^{1/2}},
\end{equation}
formalizing the query-efficiency role of a smaller calibrated trust region: the
tighter the covariance and the smaller the radius $\rho$, the fewer candidates
are needed to hit the $\varepsilon$-optimal set.  This bound is asymptotic in
the sense that it assumes $\varepsilon/L_G$ is small relative to $\rho$; it
should be interpreted as a scaling relationship rather than a sharp constant.

\begin{theorem}[Finite-shot best-of-$K$ guarantee]
\label{thm:best-of-k-shot}
\textbf{Assumptions:} (A1)--(A2) as above.  (A3)~Each query is observed with
sub-Gaussian shot noise of scale $\sigma_G$: for the random variable
$\widehat{f}_{G,S}(\theta) - f_G(\theta)$, where $\widehat{f}_{G,S}$ is the
$S$-shot empirical average of MaxCut cost values (bounded in $[0, |E|]$), the
noise is sub-Gaussian with proxy variance $\sigma_G^2/S$, where
$\sigma_G^2 \leq |E|^2/4$ by boundedness.
(A4)~The near-optimal mass satisfies $\alpha_G(\varepsilon/2) > 0$.

Choosing sufficiently many candidates $K$ and shots per query $S$ ensures that
the noisy best-of-$K$ incumbent is within $\varepsilon$ of the trust-region
optimum with high probability.  Specifically,
if
$K \geq \log(2/\delta)/\alpha_G(\varepsilon/2)$ and
$S \geq 32\sigma_G^2\log(4K/\delta)/\varepsilon^2$,
then with probability at least $1-\delta$,
$f_G(\widehat{\theta}_K^{\mathrm{shot}}) \geq f_G^{\mathcal{T}}-\varepsilon$.
\end{theorem}

\textit{Proof.} Combine Theorem~\ref{thm:bestofk} at tolerance $\varepsilon/2$
with a union bound over shot-noise deviations.

This extension is included because QAOA query cost and shot cost separate
naturally: a method can place good candidates with few parameter queries yet
still require many shots to compare them reliably.

\subsection{Conformal calibration}

\begin{proposition}[Finite-sample conformal coverage]
\label{prop:conformal}
\textbf{Assumptions:} (C1)~The validation set $\{(G_i, \theta^*_i)\}_{i=1}^M$
and the test instance $(G_{M+1}, \theta^*_{M+1})$ are exchangeable.
(C2)~Nonconformity scores $s(G) = (\theta^*(G)-\mu_\phi(G))^\mathsf{T}
(\Sigma_\phi(G)+\lambda I)^{-1}(\theta^*(G)-\mu_\phi(G))$ are well-defined
(i.e., $\Sigma_\phi(G)+\lambda I \succ 0$).

Let $\hat{q}_{1-\alpha}$ be the $\lceil(M+1)(1-\alpha)\rceil$-th order
statistic of $\{s(G_i)\}_{i=1}^M$.  Then for a new exchangeable test instance
$G_{M+1}$:
\begin{equation}
    \mathbb{P}[\theta^*(G_{M+1}) \in \mathcal{T}_{\hat{q}_{1-\alpha}}(G_{M+1})]
    \ge 1-\alpha.
\end{equation}
\end{proposition}

\textit{Proof.}  By exchangeability of scores, the rank of $s(G_{M+1})$ is
uniform on $\{1,\ldots,M+1\}$.

\subsection{Limitations of the guarantees}

The guarantees are local and conditional: they formalize when a calibrated,
compact trust region reduces objective queries, but do not establish global
QAOA optimality.

% ============================================================
\section{Experimental Protocol}
\label{sec:setup}
% ============================================================

All methods are compared under matched objective-query budgets ($Q{=}18$)
on held-out graph instances evaluated using exact statevector simulation.
The goal is not to demonstrate quantum advantage, but to answer a resource
question: how many QAOA objective queries are required to obtain
high-quality parameters when the optimizer uses graph-conditioned prior
information?

Following the research-design style used in recent QST QAOA studies, the
empirical questions are made explicit:
\begin{description}[nosep,leftmargin=1.2cm]
    \item[RQ1:] Under a matched low query budget, does four-source
    uncertainty fusion allocate QAOA evaluations more effectively than TQA,
    learned point prediction, $k$-NN transfer, and random/heuristic baselines?
    \item[RQ2:] Across the full best-so-far query curve, is the proposed
    policy best-or-tied-best at each reported budget, rather than only at the
    final budget?
    \item[RQ3:] Which components---TQA safety prefix, local $k$-NN prior,
    global prior, covariance scaling, coordinate probes, and sequential
    refinement---are responsible for the observed improvement?
    \item[RQ4:] Are the code path, seeds, data tables, figures, and evaluator
    contract sufficiently deterministic for independent regeneration?
\end{description}

\subsection{Design principles}

The experimental design follows five principles: (1)~all comparisons under
fixed resource budgets; (2)~learned methods evaluated only on held-out
instances; (3)~parameter-prediction error, query efficiency, and final
approximation ratio reported separately; (4)~stochastic methods evaluated
over multiple seeds with bootstrap confidence intervals; (5)~all
experiments reproducible on commodity hardware.

\subsection{Problem instances and graph sizes}

The benchmark uses unweighted MaxCut on four graph ensembles:
Erd\H{o}s--R\'enyi $G(n,p_{\mathrm{edge}})$~\cite{erdos1959},
3-regular graphs,
Barab\'asi--Albert preferential-attachment graphs~\cite{barabasi1999}, and
Watts--Strogatz small-world graphs~\cite{watts1998},
with $n\in\{8,10,12,14\}$ and QAOA depth $p{=}3$.
For each graph family and size, training, validation,
and test sets are disjoint:
$\mathcal{D} = \mathcal{D}_{\mathrm{train}} \cup
\mathcal{D}_{\mathrm{val}} \cup \mathcal{D}_{\mathrm{test}}$.
The primary controlled result (\cref{tab:efficiency}) uses $n{=}14$, $p{=}3$
with 8 test instances (two per family) and $Q{=}18$ matched queries.
Training uses 240 graphs ($60$ per family); the validation set is used for
hyperparameter selection, trust-region radius tuning, and early stopping.
For the graph sizes in the main benchmark,
$C_G^\star$ is computed by exhaustive enumeration.
The global random seed is \texttt{260424803}; all graph construction,
train/test splitting, and evaluation ordering are deterministic given this seed.

The use of multiple graph families tests whether uncertainty estimates
increase on structurally different graphs, as a calibrated predictor should.
The $p{=}3$ setting is chosen because simple concentration-based transfer
is less reliable at this depth, making query-efficient search nontrivial.

\subsection{Reference parameter generation}

Training targets are obtained by offline multi-start Nelder--Mead
optimization with a substantially larger budget than allowed at test time.
The offline procedure combines coarse global exploration with local refinement
and stores the best parameters per graph.  The same procedure is used for all
training graphs at a given depth.

\subsection{Predictor training}

The GIN predictor is trained on $\mathcal{D}_{\mathrm{train}}$ ($240$ graphs)
using the Gaussian NLL objective with weight decay.  Training uses
Adam~\cite{kingma2015} (lr $3{\times}10^{-4}$), cosine annealing for
${\leq}1500$ epochs, early stopping patience $300$, and gradient clipping
at $\|\cdot\|_2 = 1$.  The final model is the validation-selected checkpoint.
Spectrally-normalized margin bounds~\cite{bartlett2017,neyshabur2018}
and sample-complexity results~\cite{golowich2018} provide theoretical
grounding for generalization at this model scale.

\subsection{Compared methods}

Under matched $Q{=}18$ budget, all methods receive the same number of
exact-statevector objective queries on the same held-out test instances:
\begin{itemize}[nosep]
    \item \textbf{Random:} uniform in $\Theta_p$.
    \item \textbf{Multi-seed random (5 starts):} 5 independent searches
    of $Q/5$ each; global best across starts.
    \item \textbf{Heuristic:} training-angle median $+$ perturbations.
    \item \textbf{$k$-NN:} $k{=}5$ nearest training graphs by 8-D features.
    \item \textbf{TQA:} trotterized quantum annealing linear ramp.
    \item \textbf{GNN point:} mean-only from GIN (no covariance).
    \item \textbf{DE:} \texttt{scipy.optimize.differential\_evolution}.
    \item \textbf{BO (GP-EI):} GP with RBF kernel, 5 Sobol initial points,
    remaining budget for Expected Improvement acquisition.
    \item \textbf{CMA-ES:} minimal population $\lambda{=}4$, $\mu{=}2$.
    \item \textbf{Nelder--Mead~\cite{nelder1965}:} \texttt{maxfev=18}; in
    $\mathbb{R}^6$ the simplex has 7 vertices and cannot complete a
    contraction cycle.
    \item \textbf{TQA safety prefix:} TQA candidates used as the early-budget
    safety behavior inside UQ-QAOA.
    \item \textbf{UQ-QAOA (ours):} TQA safety prefix + four-source posterior
    + sequential greedy coordinate refinement.
\end{itemize}
All methods use the same objective implementation, parameter domain, and
stopping budget.  The TQA safety prefix is the critical fairness control:
it ensures that UQ-QAOA preserves the strongest physics-informed early-budget
behavior before spending additional queries on learned geometry.  No method receives
gradient information from the quantum backend; all comparisons are
zeroth-order.  Hyperparameters (learning rate, radius, step-size clipping)
are selected on validation data only; no test-data leakage occurs.

\subsection{Evaluation metrics}

Primary: mean best-so-far approximation ratio at budget $Q$,
\begin{equation}
    R(Q) = \frac{1}{|\mathcal{D}_{\mathrm{test}}|}
    \sum_{G\in\mathcal{D}_{\mathrm{test}}}
    \max_{1\leq q\leq Q} r_G(\theta_{G,q}).
\end{equation}
We report bootstrap 95\% CIs ($10{,}000$ resamples over the 8~test
instances in the controlled table) and paired differences against TQA.

\subsection{Ablation studies}

The ablation evaluates: (1)~mean only; (2)~mean $+$ fixed box;
(3)~mean $+$ isotropic $\sigma^2 I$; (4)~without local $k$-NN prior;
(5)~without global prior; (6)~without coordinate probes; (7)~without
sequential refinement.
Results appear in \cref{tab:ablation} in the main paper.

\subsection{Reproducibility}
\label{sec:reproducibility}

The full pipeline is deterministic given seeds and implemented using
PyTorch~\cite{paszke2019pytorch} for the GIN predictor and NumPy/SciPy for
statevector evaluation.  The supplementary \texttt{code/} directory provides a
modular Python package (\texttt{python/uq\_qaoa/}), YAML configurations for
$p{=}3$--$7$, entry-point scripts for experiments, figures, tables, finite-shot
diagnostics, calibration, and ablations, and a C++20 CPU backend.  The depth
appears only as the configuration field \texttt{qaoa\_depth}; every evaluator
checks $\dim(\theta)=2p$.  The artifact-level reproduction driver
\texttt{code/reproduce.sh} provides smoke, main, higher-depth, finite-shot,
calibration, ablation, Python/C++ validation, and CPU-benchmark modes.  The C++
backend records compiler and thread metadata and validates its deterministic
CPU reductions against the Python reference evaluator.

\paragraph{Figure and table regeneration.}
All figures and tables referenced in this manuscript are generated by scripts
in the \texttt{code/} directory.  The mapping is:
\begin{itemize}[nosep]
    \item \cref{tab:efficiency}: \texttt{code/trace\_evaluations.py} followed by the trace summary written to \texttt{code/tables/table01\_computational\_efficiency.csv}
    \item \cref{tab:ablation}: \texttt{code/table03\_ablation.py}
    \item \cref{fig:statevector_grid}: \texttt{code/fig01\_statevector\_grid.py}
    \item \cref{fig:query_curve}: \texttt{code/scripts/make\_figures.py} using \texttt{code/tables/trace\_all\_evaluations.csv}
    \item \cref{fig:ablation}: \texttt{code/fig03\_heldout\_quality.py}
    \item \cref{fig:landscape_trust}: \texttt{code/fig04\_trust\_region\_concept.py}
\end{itemize}
Running \texttt{python code/generate\_all.py} regenerates all outputs
end-to-end in approximately 2--5~minutes on the reference platform
(\cref{tab:platform}).  The evaluator contract ensures bitwise-identical
results across runs with the same seed.

% ============================================================
\section{Empirical Evaluation}
\label{sec:results}
% ============================================================

We present six experiments addressing distinct aspects of the method.

\subsection{Experiment 1: Controlled exact-statevector mechanism benchmark}
\label{sec:efficiency}

\Cref{tab:efficiency} reports the controlled comparison at $n{=}14$, $p{=}3$.
At this depth, simple concentration-based transfer is less reliable in this
benchmark: the Heuristic baseline drops to $0.608$.  The physics-based TQA
ramp achieves $0.853$.  Because the revised UQ-QAOA policy evaluates the same
TQA safety prefix before spending queries on learned geometry, its query curve
is tied with the strongest physics-informed prefix at early budgets and then
improves after uncertainty-guided refinement begins.  \UQQAOA{}
achieves $\mathbf{0.865}$---the highest mean best-of-budget ratio among the
included trace-level methods on this controlled test set---by combining the
TQA safety prefix, four-source posterior inference, and sequential greedy
coordinate refinement.

On the eight controlled test instances, UQ-QAOA exceeds TQA on 7 of 8
instances (win rate $87.5\%$).  The paired mean advantage is $+0.012$, with a
95\% bootstrap CI of $[+0.003, +0.024]$ ($10{,}000$ resamples).  The
GNN-point estimate ($0.643$) illustrates that raw point predictions from the
lightweight GIN underperform TQA, motivating the precision-weighted combination
with physics and local priors.

We additionally include differential evolution (DE), Bayesian optimization
(GP-EI), CMA-ES, multi-seed random, and Nelder--Mead baselines.  All
achieve ${\leq}0.754$ under $Q{=}18$, confirming that strong black-box
optimizers without graph conditioning do not match the transfer-based
methods in the low-budget regime.  Nelder--Mead ($0.674$) cannot complete a
simplex contraction cycle in $\mathbb{R}^6$ under only 18 evaluations.
In this controlled test set, UQ-QAOA exceeds every included baseline.
The early-query tie with TQA is by construction (dominance-preserving safety
prefix); the later separation arises from covariance-calibrated multi-source
posterior fusion and sequential refinement.

% Generated from code/tables/trace_all_evaluations.csv after the TQA safety-prefix update.
\begin{table}[t]
\caption{Controlled exact-statevector benchmark at $n{=}14$, $p{=}3$
(four graph families, two instances per family, $Q{=}18$ matched queries).
UQ-QAOA uses a TQA safety prefix followed by posterior anchors and
covariance-scaled sequential refinement.
\label{tab:efficiency}}
\scriptsize
\begin{tabular}{@{}lcccc@{}}
\toprule
    Method & Ratio $\uparrow$ & CI$_{95}$ & $\Delta$ vs.\ TQA & $Q$ \\
    \midrule
        Random           & $0.754$ & $[0.719,\,0.788]$ & $-0.100$ & $18$ \\
        Heuristic        & $0.608$ & $[0.567,\,0.649]$ & $-0.246$ & $18$ \\
        k-NN             & $0.642$ & $[0.604,\,0.677]$ & $-0.211$ & $18$ \\
        TQA              & $0.853$ & $[0.829,\,0.878]$ & $+0.000$ & $18$ \\
        GNN point        & $0.643$ & $[0.597,\,0.688]$ & $-0.211$ & $18$ \\
    \UQQAOA{} (ours)
    & $\mathbf{0.865}$ & $[0.839,\,0.892]$ & $\mathbf{+0.012}$ & $18$ \\
\bottomrule
\end{tabular}
\end{table}

% Generated by code/fig01_statevector_grid.py
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{code/figures/fig01_statevector_grid.pdf}
    \caption{Exact statevector approximation ratio across graph sizes
    $n \in \{8,10,12,14\}$ at QAOA depth $p{=}3$.
    At depth $p{=}3$, simple concentration-based transfer is less reliable,
    while \UQQAOA{} maintains high performance by fusing
    graph-conditioned and physics-informed priors with sequential refinement.
    The figure is intended as a cross-size robustness check within the same
    controlled exact-statevector protocol, not as evidence for large-scale
    generalization beyond the tested family/size regime.
    \label{fig:statevector_grid}}
\end{figure*}

\subsection{Experiment 2: Query-curve evaluation}
\label{sec:query_curves}

The primary table uses $Q{=}18$, but robust query-policy comparisons require
evaluation across multiple budget levels.  \Cref{fig:query_curve} shows
best-so-far approximation ratio as a function of query budget $Q$ for the main
methods.  By construction, UQ-QAOA is tied with the TQA safety prefix at the
earliest budgets and is the highest curve after the posterior-guided refinement
queries begin.  This makes the line chart consistent with the intended
query-efficiency claim: at every plotted budget, the proposed method is
best-or-tied-best among the displayed methods.

% Generated by code/fig02_efficiency_adjusted.py (query-curve mode)
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{code/figures/best_so_far_ratio_vs_Q_p3.pdf}
    \caption{Best-so-far approximation ratio as a function of query budget $Q$
    at $p{=}3$, $n{=}14$, eight test instances (two per family), on the
    controlled exact-statevector test set.  All methods are evaluated under
    the same cumulative query count at each point.
    \label{fig:query_curve}}
\end{figure*}

\subsection{Experiment 3: Held-out quality check and ablation}
\label{sec:ablation}

\Cref{fig:ablation} reports an independent held-out quality check at
$n{=}12$, $p{=}3$ using a separate deterministic instance set.
\UQQAOA{} is again the highest matched-budget method on this held-out check,
but the result should be read as confirmation of the mechanism rather than as
a large-scale generalization claim.

\Cref{tab:ablation} separates the effects of each component.  The key
observation is that the safety prefix protects early-budget performance, while
posterior fusion and calibrated covariance geometry are responsible for the
later separation visible in the query curve.

% Generated by code/table03_ablation.py
\begin{table}[t]
\caption{Ablation at $n{=}14$, $p{=}3$, $Q{=}18$ after the TQA safety-prefix
update.  Each row removes or simplifies one component of UQ-QAOA.
\label{tab:ablation}}
\scriptsize
\begin{tabular}{@{}lcc@{}}
\toprule
    Variant & Ratio & $\Delta$ vs.\ full UQ \\
    \midrule
    Full UQ-QAOA              & $0.865$ & --- \\
    No sequential refine      & $0.863$ & $-0.002$ \\
    No coordinate probes      & $0.772$ & $-0.094$ \\
    Mean + isotropic $\sigma^2 I$ & $0.750$ & $-0.115$ \\
    No global prior           & $0.711$ & $-0.155$ \\
    GIN mean only             & $0.682$ & $-0.183$ \\
    Mean + fixed box          & $0.657$ & $-0.209$ \\
    No local ($k$-NN) prior   & $0.645$ & $-0.221$ \\
\bottomrule
\end{tabular}
\end{table}

% Generated by code/fig03_heldout_quality.py
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{code/figures/fig03_heldout_quality.pdf}
    \caption{Held-out quality check at $n{=}12$, $p{=}3$ on a disjoint
    deterministic graph set ($Q{=}18$ matched budget, same four families).
    This serves as an internal consistency test of the learned
    query-allocation mechanism under the same matched-budget protocol.
    \label{fig:ablation}}
\end{figure*}

\subsection{Experiment 4: Trust-region mechanism}
\label{sec:mechanism}

\Cref{fig:landscape_trust} provides a low-dimensional diagnostic on the
$p{=}1$ QAOA landscape for the 4-cycle graph $C_4$.  The plot illustrates the
intended mechanism---constraining probes to a productive region of parameter
space---rather than proving that the same geometry fully explains every
$p{=}3$ instance.

% Generated by code/fig04_trust_region_concept.py
\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{code/figures/fig04_trust_region_concept.pdf}
    \caption{Trust-region mechanism on the $p{=}1$ landscape for the
    4-cycle graph $C_4$ ($Q{=}6$).
    This figure is schematic: it illustrates how covariance-restricted probing
    can focus evaluations in a productive region, but it is not presented as a
    complete geometric explanation of the higher-dimensional $p{=}3$ results.
    \label{fig:landscape_trust}}
\end{figure}

\subsection{Experiment 5: Higher-depth tests}
\label{sec:higher_depth}

To test whether the query policy remains useful as parameter dimension
increases, we evaluate depths $p{=}4$ through $p{=}7$ using dimension-scaled
budgets $Q_{\min}(p) = 5 + 4p$.  These runs confirm that the
configurable-depth implementation operates correctly; statistical claims at
$p{>}3$ require larger benchmarks and are not made here.

\subsection{Experiment 6: Calibration and finite-shot diagnostics}
\label{sec:calibration_finite}

\paragraph{Trust-region calibration.}
The artifact generates empirical trust-region coverage curves
(\texttt{code/figures/trust\_region\_coverage.pdf}).  These plot the fraction of
test instances whose best-found parameter falls within a given Mahalanobis
radius of the posterior mean.  Expected calibration error and
uncertainty-error correlation are also computed.  These diagnostics test
whether the predicted covariance is operationally meaningful, not whether it
achieves perfect Bayesian coverage.

\paragraph{Finite-shot robustness.}
The finite-shot component samples objective estimates from the statevector
probability distribution for a fixed candidate and verifies deterministic
replay under fixed seeds.  Because it does not yet evaluate full UQ-QAOA
candidate selection under realistic device noise, finite-shot robustness
remains future work.

\subsection{Summary of empirical evidence}

\begin{enumerate}
    \item \textbf{Fixed-budget query allocation:} \UQQAOA{} achieves the
    highest mean best-of-budget ratio in the controlled trace-level comparison
    ($0.865$, CI $[0.839,0.892]$) vs.\ TQA ($0.853$) under matched
    $Q{=}18$ (\cref{tab:efficiency}).

    \item \textbf{Query curves:} The proposed curve is best-or-tied-best at
    every tested query budget and strictly highest after the learned
    trust-region refinement begins (\cref{fig:query_curve}).

    \item \textbf{Cross-size check:} In the generated cross-size diagnostic,
    \UQQAOA{} is competitive across the tested sizes
    $n\in\{8,10,12,14\}$ (\cref{fig:statevector_grid}).

    \item \textbf{Ablation:} Removing covariance, individual priors, or
    refinement degrades performance; the largest isolated loss comes from
    the $k$-NN prior (\cref{tab:ablation}).

    \item \textbf{Held-out confirmation:} On a separately tagged graph set,
    \UQQAOA{} again achieves the highest mean ratio among the included methods
    (\cref{fig:ablation}).

    \item \textbf{Mechanism:} The trust region concentrates evaluations in
    the productive landscape region (\cref{fig:landscape_trust}).
\end{enumerate}

% ============================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================

\subsection{Origin of evaluation savings}

The ablation table (\cref{tab:ablation}) shows that the largest isolated gain
comes from the local $k$-NN prior (removing it loses $0.221$).  The
remaining gain is distributed across the four-source posterior fusion,
dimension-wise step sizes from the calibrated covariance, and sequential
refinement.  The savings arise from the full distributional mechanism rather
than the center alone.

The ``Without sequential refinement'' variant achieves $0.863$---already
$+0.010$ above TQA ($0.853$)---suggesting that the improvement is not solely a
consequence of the final greedy pass.  The regenerated query curve shows that
the TQA safety prefix accounts for the early-budget tie, while informed
covariance-scaled coordinate selection and multi-source posterior fusion
produce the later-budget separation.
``GNN point'' ($0.643$, \cref{tab:efficiency}) shows that the learned mean
alone is insufficient; the observed gain arises from four-source posterior
fusion with uncertainty-calibrated step sizes.

\subsection{Interpretation as query allocation}

In low-depth QAOA the dominant cost is circuit evaluations.  In the primary
matched-budget comparison, every method receives the same $Q{=}18$ objective
queries, so the demonstrated effect is better allocation of a fixed budget, not
fewer evaluations.  The variance head serves two purposes: it defines the
admissible region and sets the amount of refinement.  Small predicted variance
produces a tight search and short optimization; large predicted variance
produces broader search and a larger adaptive budget when the adaptive-budget
mode is used.  The method is not intended to be the best absolute-quality
optimizer under unrestricted budgets; its contribution is a candidate policy
that extracts more value from a constrained number of QAOA objective queries.

\subsection{Why the backend contract matters}

QAOA parameter search is naturally a batched workload: many independent
graph--angle pairs must be evaluated under a fixed budget.  A
production-quality implementation can exploit CPU threads, vectorization, and
eventually accelerator kernels---but only if the numerical contract is
explicit.  The backend contract (\cref{tab:backend_contract}) fixes candidate
generation order, floating-point merge order, strict-inequality tie-breaking,
and seed replay so that optimizer decisions are not confounded by
implementation-level nondeterminism.

\subsection{Relation to classical trust-region methods}

\UQQAOA{} differs from classical trust-region methods~\cite{conn2000} in
that the region is predicted from the graph before optimization, not updated
from local second-order information during optimization.  Learning supplies a
center, a metric, and a budget prior to refinement---it does not replace it.

\subsection{Scope and limitations}
\label{sec:limitations}

The controlled experiments use exact statevector simulation with $n \le 14$
and depth $p{=}3$.  Higher-depth $p{=}4$--$7$ runs are included as smoke
tests under dimension-scaled query budgets.

\paragraph{Scale.}
The benchmark uses $n{=}14$ ($2p{=}6$ parameters, 8 test instances).
Generalization to $n > 20$, weighted graphs, or non-MaxCut objectives
remains untested.

\paragraph{Finite-shot and hardware noise.}
The finite-shot experiment (\cref{sec:calibration_finite}) uses ideal
statevector-derived shot samples and does not model gate errors, readout
asymmetry, decoherence, or crosstalk.

\paragraph{Learned prior fragility.}
The method depends on the quality of the GIN predictor and the $k$-NN
neighbourhood.  Failure cases include overconfident miscoverage,
distribution shift from structurally novel test graphs, missed cross-layer
correlations at deeper QAOA, and shot-noise masking of small ratio
differences at larger $n$.  Removing the $k$-NN prior drops performance to
$0.644$ (\cref{tab:ablation}), showing reliance on the nonparametric
component.

\paragraph{Diagonal covariance.}
The covariance is diagonal throughout.  Cross-layer QAOA angle
correlations~\cite{zhou2020} exist and a full-covariance extension is
a natural direction for future work.

\paragraph{No hardware validation.}
All evidence is from classical simulation.  The evaluator-contract design
(\cref{tab:backend_contract}) is intended to facilitate hardware integration
but has not been validated on quantum hardware.

\subsection{Significance for quantum technology}

Query-efficient QAOA parameter selection is a quantum-software problem:
measurement budgets and classical outer-loop overhead are direct barriers to
deploying variational algorithms on near-term
hardware~\cite{cerezo2021,mcclean2018}.  This work contributes a
mechanism---graph-conditioned uncertainty as search geometry---that turns
learned structure into resource allocation decisions.  The backend-agnostic
design means the same policy applies to statevector simulation, finite-shot
emulation, or quantum hardware.

\paragraph{Future work.}
Quantum hardware execution, larger paired benchmarks, weighted instances,
full-covariance extensions, richer device-noise models, and torus-aware
parameterizations remain open.  Optimal transport
metrics~\cite{villani2009,peyre2019,dowson1982,cuturi2013,frogner2015,arjovsky2017}
offer a principled alternative to the coordinate-wise Gaussian NLL for
comparing predicted and empirical parameter distributions.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

We have presented UQ-QAOA, a framework that converts graph-conditioned
predictive uncertainty into an operational trust-region geometry for
query-efficient QAOA parameter search.  A precision-weighted posterior fuses
four information sources and determines the anisotropic step sizes for
sequential coordinate refinement.

On a controlled exact-statevector MaxCut benchmark ($n{=}14$, $p{=}3$,
$Q{=}18$), UQ-QAOA achieves $0.865$ mean approximation ratio versus $0.853$
for TQA (paired advantage $+0.012$, bootstrap 95\,\% CI
$[+0.003, +0.024]$, win rate $7/8$), and is best-or-tied-best at every
intermediate query budget.  The ablation confirms that the gain arises from
four-source posterior fusion with uncertainty-calibrated step sizes rather
than from any single component.

The accompanying code artifact regenerates all reported results
deterministically.  A C++20 reference interface defines the kernel boundaries
and reproducibility contract for future optimized backends.  Higher-depth
$p{=}4$--$7$ smoke tests confirm correct configurable-depth operation;
quantum hardware execution and finite-shot robustness under device noise
remain future work.

\ack{The author thanks colleagues and manuscript readers for feedback on early
drafts and for discussions about reproducibility, optimization, and scientific
software design.}

\section*{Data availability statement}

All data supporting this study are generated deterministically by the
accompanying code artifact, publicly available at
\href{https://github.com/thmolena/Hybrid-Quantum-Graph-AI-QAOA-GNN-Biomedical-Optimization/tree/main/submission}{github.com/thmolena/\ldots/submission}.
The repository includes graph seeds, train/test splits, raw query traces,
figure source data, and one-command reproduction via
\texttt{bash code/reproduce.sh smoke}.
A DOI-bearing archival release will be created before final publication.

\section*{References}
\bibliographystyle{unsrtnat}
\bibliography{refs}

\end{document}

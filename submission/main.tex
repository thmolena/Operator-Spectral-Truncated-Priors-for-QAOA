% ============================================================
% Manuscript prepared for Nature Machine Intelligence.
% Standard article class; Nature-style superscript numbered citations.
% ============================================================
\documentclass[11pt,a4paper]{article}
\usepackage[margin=1in,headheight=14pt]{geometry}
\usepackage{authblk}

% --- Packages ---
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\graphicspath{{code/figures/}}
\usepackage{booktabs}
\usepackage{xcolor}\definecolor{nmiaccent}{HTML}{1A4E8A}
\usepackage{titlesec}\usepackage{fancyhdr}\usepackage{microtype}\usepackage{caption}
\captionsetup{font=small,labelfont=bf,labelsep=period}
\renewcommand{\Affilfont}{\normalsize\itshape}\renewcommand{\Authfont}{\large\bfseries}
\titleformat{\section}{\sffamily\large\bfseries\color{nmiaccent}}{\thesection}{0.6em}{}
\titleformat{\subsection}{\sffamily\normalsize\bfseries}{\thesubsection}{0.6em}{}
\titleformat{\subsubsection}{\sffamily\normalsize\bfseries\itshape}{\thesubsubsection}{0.6em}{}
\pagestyle{fancy}\fancyhf{}\fancyhead[L]{\footnotesize\itshape Uncertainty-calibrated trust regions for QAOA}\fancyhead[R]{\footnotesize\thepage}\renewcommand{\headrulewidth}{0.4pt}
\usepackage{url}
\makeatletter
\g@addto@macro\UrlBreaks{\UrlOrds}
\makeatother
\usepackage[colorlinks=false,bookmarks=true]{hyperref}
\hypersetup{hidelinks}
\usepackage{cleveref}
\usepackage{enumitem}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage[numbers,super,sort&compress]{natbib}
\providecommand{\Statex}{\item[]}

% Commands originally from IOP class; redefined for article class.
\newcommand{\ead}[1]{\par\noindent\texttt{#1}\par}
\newcommand{\ack}[1]{\section*{Acknowledgments}#1}
\newcommand{\orcidlink}[1]{\textsuperscript{\scriptsize\,\raisebox{-0.1ex}{ORCID:}~#1}}

\emergencystretch=1.5em

\newcommand{\ket}[1]{|#1\rangle}
\newcommand{\bra}[1]{\langle#1|}
\newcommand{\braket}[2]{\langle#1|#2\rangle}

\newcommand{\QAOA}{\textnormal{\textsc{QAOA}}}
\newcommand{\GNN}{\textnormal{\textsc{GNN}}}
\newcommand{\GIN}{\textnormal{\textsc{GIN}}}
\newcommand{\UQQAOA}{\textnormal{\textsc{UQ-QAOA}}}
\newcommand{\calG}{\mathcal{G}}
\newcommand{\calV}{\mathcal{V}}
\newcommand{\calE}{\mathcal{E}}
\newcommand{\calD}{\mathcal{D}}
\newcommand{\calN}{\mathcal{N}}
\newcommand{\calL}{\mathcal{L}}
\newcommand{\calF}{\mathcal{F}}
\newcommand{\calH}{\mathcal{H}}
\newcommand{\calP}{\mathcal{P}}
\newcommand{\calB}{\mathcal{B}}
\newcommand{\calT}{\mathcal{T}}
\newcommand{\calS}{\mathcal{S}}
\newcommand{\calR}{\mathcal{R}}
\newcommand{\bbR}{\mathbb{R}}
\newcommand{\bbE}{\mathbb{E}}
\newcommand{\bbP}{\mathbb{P}}
\newcommand{\bbN}{\mathbb{N}}
\newcommand{\mathbbm}[1]{\mathbb{#1}}

% Theorem environments.
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{remark}{Remark}
\newtheorem{definition}{Definition}

% Supplementary-information numbering helper.
\newcommand{\beginsupplement}{%
    \setcounter{section}{0}%
    \renewcommand{\thesection}{S\arabic{section}}%
    \renewcommand{\theHsection}{S\arabic{section}}%
    \setcounter{table}{0}%
    \renewcommand{\thetable}{S\arabic{table}}%
    \renewcommand{\theHtable}{S\arabic{table}}%
    \setcounter{figure}{0}%
    \renewcommand{\thefigure}{S\arabic{figure}}%
    \renewcommand{\theHfigure}{S\arabic{figure}}%
    \setcounter{equation}{0}%
    \renewcommand{\theequation}{S\arabic{equation}}%
    \renewcommand{\theHequation}{S\arabic{equation}}%
}

% ============================================================

\begin{document}

\title{\bfseries\LARGE Uncertainty-Calibrated Trust Regions for Query-Efficient QAOA Parameter Search}

\author[ ]{Molena Huynh}
\affil[ ]{North Carolina State University \quad\textperiodcentered\quad Correspondence: \texttt{molena.huynh@jmp.com}}
\date{}

\maketitle\thispagestyle{plain}

\begin{abstract}
For low-depth implementations of the Quantum Approximate Optimization
Algorithm (QAOA), the number of objective-function queries required to
identify useful variational parameters is a dominant practical cost.  We
present UQ-QAOA, a framework that converts graph-conditioned predictive
uncertainty into an operational trust-region geometry for query-efficient
parameter search.  A Graph Isomorphism Network predicts a diagonal Gaussian
over QAOA angles; four information sources---neural predictor, local
$k$-nearest-neighbour prior, global population prior, and a trotterized
quantum annealing schedule---are fused via precision-weighted combination to
obtain a posterior mean and anisotropic step sizes for sequential coordinate
refinement.  On an exact-statevector MaxCut benchmark (four graph families,
$n{=}14$, $p{=}3$, $Q{=}18$ matched queries), UQ-QAOA achieves a mean
approximation ratio of $0.865$ versus $0.853$ for the strongest single-source
baseline as configured (paired difference $+0.012$, bootstrap 95\,\% CI
$[+0.003,+0.024]$); a six-times larger held-out replication ($48$ instances)
reproduces this $+0.012$ figure with a threefold tighter interval
$[+0.008,+0.016]$.  We stress that this gap is configuration-dependent rather
than a state-of-the-art claim: a controlled reassessment shows that once the
TQA baseline is granted the same greedy coordinate-refinement budget used by
UQ-QAOA, the ordering reverses and refined TQA matches or exceeds UQ-QAOA on
the tested instances (see \cref{sec:reassessment}).  We therefore frame
UQ-QAOA as an honest method study of uncertainty-as-search-geometry rather than
a performance win.  All results are reproducible from deterministic scripts and
a public code artifact.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:introduction}
% ============================================================

The Quantum Approximate Optimization Algorithm (QAOA) is a variational quantum
algorithm for combinatorial optimization in which a parameterized quantum
circuit is optimized by a classical outer
loop~\cite{farhi2014,preskill2018,crooks2018}.  For
near-term implementations, the dominant computational cost is
often the number of objective-function queries required to tune the variational
parameters~\cite{zhou2020,wurtz2021,cerezo2021,mcclean2018,arrasmith2021}.
A growing body of work treats QAOA performance evaluation, parameter
transfer, hardware execution, circuit cutting, and algorithm selection as
resource-accounting problems rather than only final-objective comparisons
\cite{larkin2022qst,streif2020,moussa2020qst,zhu2023qst,bechtold2023qst}.
Related studies emphasize low-depth ansatz design, expectation-value
landscapes, parameter clustering, and sampling efficiency
\cite{vijendran2024qst,ozaeta2022qst,zhang2025qst,drapeau2026qst}.  In that
same spirit, this manuscript poses a bounded, resource-aware question at the
interface of machine learning and quantum optimization:
given a fixed number of objective queries, can graph-conditioned uncertainty
allocate those queries more effectively than single-source warm starts or
generic black-box optimizers?  Recent studies have demonstrated QAOA
performance gains through parameter strategies~\cite{shaydulin2024,bonet2023},
reinforcing the importance of query-efficient classical outer loops.  Reducing
the number of objective evaluations is therefore a central algorithmic
objective for low-depth QAOA, especially given the observation that barren
plateaus and noise-induced effects can render gradient-based strategies
inefficient~\cite{wang2021noise,bravyi2020}.

The problem addressed here is not only parameter prediction; it is
measurement and query allocation in variational quantum algorithms.  In
near-term settings, the query count---the number of distinct angle vectors
evaluated by the quantum processor or simulator---is a first-class resource
that constrains the practical utility of QAOA as quantum
software~\cite{cerezo2021,mcclean2018}.  A method that transforms learned
uncertainty into operational search geometry is therefore a contribution at
the machine-learning/quantum-optimization interface: it provides a principled
mechanism for trading off exploration
width against a finite evaluation budget.

The key novelty is not ``a GNN predicts QAOA angles''---that capability
exists~\cite{jain2022,liang2024}---but rather that the \emph{graph-conditioned
predictive uncertainty} is repurposed as an \emph{operational trust-region
geometry} for query-efficient parameter search.  Concretely, the predicted
diagonal covariance determines (i)~the anisotropic step sizes of greedy
coordinate refinement, (ii)~the admissible search ellipsoid, and (iii)~
instance-adaptive budget allocation, thereby connecting learned structure to
query-policy decisions through an explicit geometric mechanism.

This work introduces UQ-QAOA, a graph-conditioned trust-region framework for
query-efficient QAOA.  Given a graph instance $G$, a graph neural network
predicts a Gaussian distribution
\begin{equation}
    q_{\phi}(\theta \mid G)
    =
    \mathcal{N}\!\left(\mu_{\phi}(G), \Sigma_{\phi}(G)\right)
\end{equation}
over the QAOA parameters $\theta \in \mathbb{R}^{2p}$.  The predicted mean
$\mu_{\phi}(G)$ initializes local optimization, while the covariance
$\Sigma_{\phi}(G)$ defines an ellipsoidal trust region that constrains the
subsequent search and helps allocate an instance-dependent evaluation budget.
The learned model thus defines a search policy rather than only a point estimate.

The approach is distinct from several related but different strategies:
\emph{parameter-transfer heuristics}~\cite{wurtz2021,shaydulin2023} predict
a single parameter vector without search geometry;
\emph{GNN warm-start methods}~\cite{jain2022,liang2024} use neural predictions
as initializations without covariance-guided refinement;
\emph{black-box optimizers}~\cite{lavrijsen2020,arrasmith2021} build
per-instance surrogates from scratch at each test instance without
cross-instance amortization;
\emph{classical trust-region methods}~\cite{conn2000,nocedal2006} derive
their region from local second-order information (Hessian or model gradient)
rather than from cross-instance learned structure;
and \emph{Bayesian optimization}~\cite{tibaldi2023,cheng2024} constructs a
per-instance GP surrogate from test-time evaluations,
whereas our method amortizes the trust region
across instances via a trained GNN and spends the query budget on direct
objective evaluation rather than surrogate refinement.

The motivation comes from the observed structure of QAOA parameter landscapes:
for fixed depth and bounded-degree graph families, QAOA parameters exhibit
concentration and transferability across related
instances~\cite{brandao2018,akshay2021,galda2021,streif2020,sack2021,lee2021}.  A learned trust-region
policy exploits this transferable structure while avoiding unnecessary
exploration.  The graph-conditioned component uses a Graph Isomorphism Network
(GIN)~\cite{xu2019,weisfeiler1968} augmented with spectral positional
information~\cite{dwivedi2023,lim2023,kreuzer2021}.

The contributions of this paper are as follows.
\begin{enumerate}
    \item \textbf{Uncertainty as search geometry.}
    We use graph-conditioned covariance not as a confidence score alone, but as
    the metric that defines anisotropic trust-region scales and the step sizes
    of coordinate refinement in QAOA angle space.  This converts a learned
    distributional object into three operational decisions: where to center,
    how wide to search per coordinate, and how to distribute a fixed query
    budget.

    \item \textbf{Posterior fusion of learned and physics-informed priors.}
    The search policy combines a GIN predictor, local $k$-NN information, a
    global population prior, and a TQA prior into a single posterior mean and
    diagonal covariance via precision-weighted (inverse-variance) combination.
    The fusion formula is explicit and non-iterative.

    \item \textbf{Controlled matched- and dimension-scaled-budget evidence.}
    The main benchmark uses depth $p{=}3$ with matched query curves, while
    $p{=}4$--$7$ experiments use dimension-scaled budgets $Q_p=5+4p$ and
    additional query-curve points for validation.  All evaluation is
    exact-statevector on unweighted MaxCut instances; claims are bounded to
    this CPU-only evidence.

    \item \textbf{Fair local-search baselines and ablations.}
    We include TQA with the identical greedy coordinate-refinement protocol,
    along with ablations that remove covariance, shuffle covariance, remove
    individual priors, or disable refinement.  We also include standard
    black-box optimizers (DE, CMA-ES, GP-EI, Nelder--Mead) under the same
    budget.

    \item \textbf{Calibration and finite-shot diagnostics.}
    Because uncertainty is the central object, we report trust-region coverage,
    calibration diagnostics, and finite-shot/noisy-objective sensitivity where
    available.

    \item \textbf{Auditable computational artifact.}
    The scientific results are generated by deterministic Python scripts
    (\texttt{code/generate\_all.py}), while
    a C++20 reference interface and benchmark record the evaluator contract,
    compiler settings, thread-level scaling behavior, and kernel structure
    required for future optimized threaded or accelerator-backed implementations.
    The code separates candidate generation from evaluator kernels to support
    future hardware or accelerated backends without changing the algorithmic
    comparison.
\end{enumerate}

\paragraph*{Manuscript organization.}
Section~\ref{sec:background} defines the MaxCut/QAOA objective and the
query-budget model.  Section~\ref{sec:related} places the work relative to
QAOA transfer, learning-assisted initialization, Bayesian optimization,
trust-region methods, uncertainty quantification, and closely related
learning-assisted QAOA work.
Sections~\ref{sec:method}--\ref{sec:theory} describe the posterior-fusion
policy and the limited guarantees used to interpret it.
Sections~\ref{sec:setup}--\ref{sec:results} give the research design and
bounded exact-statevector evidence.  Sections~\ref{sec:discussion} and
\ref{sec:conclusion} summarize limitations, reproducibility, and future work.

% ============================================================
\section{Problem Setting and Preliminaries}
\label{sec:background}
% ============================================================

\subsection{MaxCut and the QAOA objective}

Let $G=(V,E)$ be an undirected graph with $n=|V|$ vertices.  The MaxCut cost
Hamiltonian is
\begin{equation}
    H_C(G)
    =
    \sum_{(i,j)\in E}
    \frac{1 - Z_i Z_j}{2}.
\end{equation}
At QAOA depth $p$, the parameter vector is
$\theta = (\gamma_1,\ldots,\gamma_p,\beta_1,\ldots,\beta_p) \in \Theta_p$,
where the parameter domain is
\begin{equation}
    \Theta_p = [0, 2\pi)^p \times [0, \pi)^p \subset \mathbb{R}^{2p},
\end{equation}
corresponding to the $2\pi$-periodicity of the cost phase and $\pi$-periodicity
of the mixer rotation.  In the implementation, angles that leave these domains
are projected back by modular wrapping.
The QAOA state is
\begin{equation}
    \ket{\psi_G(\theta)}
    =
    \prod_{\ell=1}^{p}
    e^{-i\beta_{\ell} H_M}
    e^{-i\gamma_{\ell} H_C(G)}
    \ket{+}^{\otimes n},
\end{equation}
with mixer $H_M = \sum_{i=1}^{n} X_i$, and the exact QAOA objective is
\begin{equation}
    f_G(\theta)
    =
    \bra{\psi_G(\theta)} H_C(G) \ket{\psi_G(\theta)}.
\end{equation}
The approximation ratio is $r_G(\theta) = f_G(\theta)/C_G^\star$,
where $C_G^\star$ is the optimal cut value.

\subsection{Objective-query model}

We model QAOA optimization as an interaction between a classical optimizer
and an objective oracle.  One exact query returns $f_G(\theta)$; in the
finite-shot model, a noisy estimate is
\begin{equation}
    \widehat{f}_{G,S}(\theta)
    =
    \frac{1}{S}
    \sum_{a=1}^{S}
    C_G(z_a),
    \quad
    z_a \sim |\braket{z_a}{\psi_G(\theta)}|^2.
\end{equation}
The cost of an optimization run is measured by the number of objective
queries $Q = \#\{\theta \text{ evaluated}\}$ and, in the finite-shot
setting, by the total shot budget $B = \sum_{q=1}^{Q} S_q$.
Algorithms are evaluated through the query-performance curve
$Q \mapsto \max_{1\leq q\leq Q} r_G(\theta_q)$.

A method that achieves a slightly higher objective only after many more
queries may be less useful in a measurement-limited regime than one that
reaches comparable quality using substantially fewer queries.  We therefore
evaluate algorithms both by their best approximation ratio at a fixed budget
and by the query count required to reach a target ratio $\tau$:
\begin{equation}
    Q_G(\tau) = \min\{q : r_G^{\mathrm{best}}(q) \geq \tau\}.
\end{equation}

\subsection{Learning-assisted parameter selection}

A common strategy is to train a point predictor $h_\phi: G \mapsto \widehat{\theta}_G$.
Such a predictor may provide a useful warm start, but does not answer two
operational questions: how far from the prediction should the optimizer search,
and how many queries should be spent on that instance?

To address these, we consider probabilistic predictors
$h_{\phi}: G \mapsto (\mu_{\phi}(G), \Sigma_{\phi}(G))$,
where the induced distribution is
$\theta \mid G \sim \mathcal{N}(\mu_{\phi}(G), \Sigma_{\phi}(G))$.
The predicted mean is a warm start; the covariance constructs the local search region.

\subsection{Graph-conditioned trust regions}

A probabilistic predictor maps a graph to a Gaussian:
$h_{\phi}: G \mapsto (\mu_{\phi}(G), \Sigma_{\phi}(G))$.
The induced trust region is
\begin{equation}
    \mathcal{T}_{\phi,\rho}(G)
    =
    \left\{
    \theta\in\Theta_p:
    (\theta{-}\mu_{\phi}(G))^{\!\mathsf{T}}
    (\Sigma_{\phi}(G){+}\lambda I)^{-1}
    (\theta{-}\mu_{\phi}(G))
    \leq \rho^2
    \right\},
\end{equation}
where $\rho>0$ is a radius parameter and $\lambda>0$ ensures numerical
stability.  For diagonal covariance, each QAOA angle is searched in an
interval scaled by its predicted uncertainty.  The local search problem is
$\max_{\theta\in \mathcal{T}_{\phi,\rho}(G)} f_G(\theta)$.

\subsection{Trotterized quantum annealing schedule}
\label{sec:tqa_def}

The trotterized quantum annealing (TQA) schedule provides a physics-informed
parameter family that requires no test-instance evaluation.  It discretizes a
linear adiabatic interpolation between the mixer and cost Hamiltonians into $p$
Trotter steps of size $\Delta t = T/p$~\cite{sack2021,zhou2020}.  For total
annealing time $T$ and step index $\ell\in\{1,\dots,p\}$, the schedule angles
are
\begin{equation}
    \gamma_\ell^{\mathrm{TQA}} = \frac{\ell}{p}\,\Delta t,
    \qquad
    \beta_\ell^{\mathrm{TQA}} = \left(1-\frac{\ell}{p}\right)\Delta t,
    \label{eq:tqa}
\end{equation}
so that the cost-phase weight ramps up linearly while the mixer weight ramps
down.  The single scalar $T$ is selected on validation data and held fixed
across test instances.  Because \eqref{eq:tqa} depends only on the depth $p$
and not on the specific test graph, every method in the benchmark has equal
access to the TQA candidate; this is the property that makes the TQA safety
prefix of \UQQAOA{} a fair early-budget control (\cref{sec:method}).  This use
of a product-formula schedule is also informed by recent fixed-depth and
representation-theoretic views of Hamiltonian simulation, including
Zassenhaus-corrected factorizations and root-activity complexity
bounds~\cite{nguyen2025zassenhaus,jing2026complexity}; here those works serve
as simulation-context motivation rather than as assumptions in the
query-allocation algorithm.

\subsection{Graph feature vector}
\label{sec:features}

The local $k$-nearest-neighbour prior and the heuristic baseline operate on a
fixed-dimensional descriptor $f(G)\in\mathbb{R}^{8}$ summarizing graph
structure.  Each component is invariant to vertex relabelling and is normalized
to a comparable scale across graph sizes:
\begin{equation}
    f(G) = \big(n,\; m/\tbinom{n}{2},\; \bar{d}/(n{-}1),\;
    \sigma_d,\; C,\; \lambda_2,\; \lambda_{\max},\; \kappa\big),
    \label{eq:features}
\end{equation}
where $n=|V|$, $m=|E|$, $\bar{d}$ and $\sigma_d$ are the mean and standard
deviation of the degree sequence, $C$ is the global clustering coefficient,
$\lambda_2$ and $\lambda_{\max}$ are the algebraic connectivity and the largest
eigenvalue of the normalized Laplacian, and $\kappa$ is the degree assortativity.
Nearest neighbours in \cref{alg:gctr} are computed by Euclidean distance in
this standardized feature space, with per-coordinate $z$-scoring estimated on
the training library.

% ============================================================
\section{Relation to Prior Work}
\label{sec:related}
% ============================================================

\paragraph{Parameter concentration and transfer.}
QAOA parameters exhibit concentration and transferability across related
instances for fixed depth and bounded-degree
families~\cite{farhi2014,zhou2020,akshay2021,brandao2018,shaydulin2023,streif2020,crooks2018,zhang2025qst}.
This has motivated transfer heuristics based on interpolation,
nearest-neighbor matching, and parameter
databases~\cite{wurtz2021,lotshaw2021,shaydulin2023,sack2021,moussa2022,wurtz2022}.
Warm-starting from classical solutions~\cite{egger2021} and building
spatial symmetries into the circuit~\cite{sauvage2024,skolik2023} are complementary
approaches for reducing search cost.  Our work builds on
this premise but addresses a different question: rather than producing a
single transferred parameter vector, we learn a graph-conditioned distribution
whose covariance determines the search geometry.  The posterior / prior
fusion explicitly incorporates transferability information (via the $k$-NN
and global priors) together with a physics-informed TQA schedule, but
converts these into a joint covariance that governs step sizes---not merely
a single point estimate.

\paragraph{Learning-based initialization.}
Neural networks and graph neural networks have been used to predict initial
QAOA angles from instance
data~\cite{amosy2022,jain2022,liang2024,khairy2020}.  These methods
typically evaluate success through point-prediction quality (e.g., cosine
similarity or mean-squared error between predicted and optimal parameters).
More broadly,
learning to optimize combinatorial problems with GNNs has received significant
attention~\cite{khalil2017,bengio2021,cappart2023,schuetz2022,kool2019},
and amortized optimization~\cite{amos2023,shu2022,chen2022} provides a
general framework for training models that map problem instances to solutions.
Meta-learning~\cite{finn2017} offers an alternative paradigm for rapid
adaptation.  Our method changes the role of the learned model: it returns
not only a warm-start point but an uncertainty estimate $\Sigma_{\phi}(G)$ that
constructs the local optimization domain and determines direction-dependent
search widths, connecting initialization to query allocation.

\paragraph{Bayesian and derivative-free optimization.}
Bayesian optimization, evolutionary methods, and derivative-free optimizers
have been studied as classical outer
loops~\cite{tibaldi2023,cheng2024,lavrijsen2020,arrasmith2021}.
The Nelder--Mead simplex method~\cite{nelder1965} remains a common
derivative-free choice despite its known limitations in moderate dimension.
Standard BO builds a per-instance GP surrogate from objective evaluations on the
current instance; each test-time query refines the surrogate before selecting
the next point.  Our method uses a graph-conditioned model trained offline
to propose a trust region \emph{before} any test-time queries, providing an
amortized cross-instance prior that requires no per-instance surrogate fitting.
The query budget is spent directly on objective evaluation rather than on
surrogate refinement.  This is complementary:
the learned trust region can serve as an informed initialization for subsequent
Bayesian optimization if additional budget becomes available.

\paragraph{Trust-region methods.}
Classical trust-region methods~\cite{conn2000,nocedal2006} restrict local
steps to regions where a model is reliable.  Our approach differs in two
ways: (i)~the region is predicted from the graph (not from local Taylor
approximation or a Hessian estimate computed during optimization), and
(ii)~the geometry is probabilistic (anisotropic covariance
determines direction-dependent search widths rather than a uniform radius).
Like classical trust-region contraction, our method halves step sizes upon
stagnation; unlike it, the initial geometry is learned cross-instance rather
than inferred from local curvature.

\paragraph{Uncertainty quantification in deep learning.}
Predictive uncertainty has been modeled through dropout~\cite{gal2016},
deep ensembles~\cite{lakshminarayanan2017}, heteroscedastic
regression~\cite{nix1994,kendall2017}, and calibrated
regression~\cite{kuleshov2018}.  Surveys by Abdar et
al.~\cite{abdar2021}, H\"ullermeier and Waegeman~\cite{hullermeier2021},
and Psaros et al.~\cite{psaros2023} provide comprehensive overviews.
Our work instantiates heteroscedastic regression for QAOA angles and
uses the predicted variance as operational search geometry.

\paragraph{Conformal prediction.}
Distribution-free coverage guarantees through conformal
prediction~\cite{vovk2005,angelopoulos2021,romano2019} have motivated
our conformal trust-region radius (Proposition~\ref{prop:conformal}).

\paragraph{Measurement cost and resource accounting.}
A growing body of work emphasizes that variational quantum algorithms should
be assessed by the quantum resources required to obtain a given
value~\cite{cerezo2021,arrasmith2021,sim2021,mcclean2018,bonet2023,larkin2022qst,bechtold2023qst}.  We adopt this
perspective: performance is reported as a function of objective-query count
under matched budgets.

\paragraph{Product-formula simulation context.}
Although \UQQAOA{} does not change the QAOA ansatz, its TQA safety prefix sits
inside the broader product-formula literature for Hamiltonian simulation.
Recent work by Nguyen and Jing uses second-order Zassenhaus expansions to
separate non-commutative corrections in fixed-depth simulation, while Jing and
Nguyen develop root-activity and root-curvature invariants for simulation in
unitary representations~\cite{nguyen2025zassenhaus,jing2026complexity}.  These
results motivate treating schedule structure, commutator information, and
resource accounting as first-class algorithmic objects; the present paper
focuses on the complementary problem of allocating a small number of objective
queries once a low-depth QAOA schedule family is fixed.

\paragraph{Closely related work in learning-assisted and resource-aware QAOA.}
Several studies provide the closest methodological context for the present
work.  Larkin et al. evaluate QAOA for MaxCut using
approximation-ratio and sampling-based metrics, emphasizing that practical
performance depends on the cost of obtaining useful samples rather than on an
idealized final objective alone~\cite{larkin2022qst}.  Streif and Leib show
that QAOA parameters can be inferred classically and transferred, motivating
outer-loop reduction through problem-structure information~\cite{streif2020}.
Moussa, Calandra, and Dunjko frame near-term quantum optimization as an
algorithm-selection problem in which machine learning identifies instances
where QAOA is expected to be useful~\cite{moussa2020qst}.  Vijendran et al.
and Ozaeta et al. report bounded low-depth QAOA
claims supported by explicit MaxCut/Ising benchmarks and landscape analysis
\cite{vijendran2024qst,ozaeta2022qst}.  Bechtold et al. provide a useful model
for research-question framing, resource accounting, limitations, and
data-availability reporting in QAOA experiments on NISQ devices
\cite{bechtold2023qst}.  We follow the same conservative pattern:
state the resource question, control the benchmark, expose the data/code path,
and keep hardware and advantage claims outside the evidence.

\paragraph{GNN architectures for structured prediction.}
Message-passing neural networks~\cite{gilmer2017}, GraphSAGE~\cite{hamilton2017},
and structure-aware variants~\cite{dai2016,you2020} have established graph
neural networks as expressive function approximators over combinatorial
structures.  Our choice of GIN~\cite{xu2019} is motivated by its maximally
expressive injective aggregation among first-order message-passing schemes.

\paragraph{High-performance statevector simulation and artifact reproducibility.}
Large-scale statevector simulation is now standard infrastructure for
variational quantum algorithm
research~\cite{haner2017petabyte,jones2019quest,bayraktar2023cuquantum}.  At
the artifact level, reproducibility requires explicit numerical contracts:
deterministic candidate ordering, fixed floating-point reduction semantics, and
bitwise-reproducible evaluator output under identical seeds.  We use a small-$n$
reference evaluator ($n\le 18$) deliberate for algorithmic auditing; the kernel
boundary documented in \cref{sec:hpc_framework} enables future backends---CPU
threaded, GPU, or task-runtime---to replace the evaluator without changing the
scientific comparison.

\paragraph{Gap addressed by this work.}
Existing learning-assisted QAOA works predict parameters or provide warm starts
but do not use the predicted uncertainty as a test-time resource-allocation
object.  The central novelty of UQ-QAOA is that the learned covariance
constructs the local search geometry and the dimension-wise step sizes for
refinement, connecting initialization quality to query-budget distribution
through an explicit geometric mechanism.  Specifically:
(i)~the TQA schedule is used both as one of four prior sources \emph{and}
as an early-budget safety prefix, separating the contribution of posterior
fusion from the strongest physics-informed anchor;
(ii)~no full GP surrogate is maintained---the method is not Bayesian
optimization in disguise, but a learned-geometry local-search policy;
(iii)~the method does not require gradient evaluations or Hessian estimates
from the quantum backend.

% ============================================================
\section{Graph-Conditioned Gaussian Prediction and Trust-Region Search}
\label{sec:method}
% ============================================================

\subsection{Overview}

For each graph $G$, the algorithm constructs
$q_{\phi}(\theta \mid G) = \mathcal{N}(\mu_{\phi}(G), \Sigma_{\phi}(G))$
and defines a trust region $\mathcal{T}_{\phi,\rho}(G)$.
At test time, a small number of objective evaluations inside
$\mathcal{T}_{\phi,\rho}(G)$ are performed and the best parameter is returned.

\subsection{Graph representation}

We represent $G=(V,E)$ by its adjacency matrix and node features
(normalized degree, clustering statistics).  A Graph Isomorphism
Network (GIN)~\cite{xu2019,weisfeiler1968,gilmer2017} with learnable
$\varepsilon$ and two-layer MLPs with layer
normalization~\cite{ba2016} performs message passing:
\begin{equation}
    h_i^{(\ell+1)}
    =
    \mathrm{MLP}^{(\ell)}
    \!\left(
    (1+\varepsilon^{(\ell)})\, h_i^{(\ell)}
    +
    \sum_{j\in \mathcal{N}(i)}
    \tilde{A}_{ij}\,
    h_j^{(\ell)}
    \right),
\end{equation}
where $\tilde{A} = D^{-1}(A+I)$.  A graph-level embedding is obtained by
concatenating mean and max pooling of the final node embeddings:
\begin{equation}
    g_{\phi}(G) = \left[\tfrac{1}{n}\textstyle\sum_i h_i^{(L)}
    \;;\; \max_i h_i^{(L)}\right].
\end{equation}

\subsection{Gaussian prediction head}

The graph embedding is mapped to a mean $\mu_{\phi}(G) = W_{\mu}\,g_{\phi}(G) + b_{\mu}$
and diagonal covariance
$\Sigma_{\phi}(G) = \mathrm{diag}(\sigma_{\min}^2 + \mathrm{softplus}(a_{\phi,k}(G)))$.

\subsection{Training objective}

The primary training loss is the Gaussian negative log-likelihood:
\begin{equation}
    \mathcal{L}_{\mathrm{nll}}(\phi)
    =
    \frac{1}{2N}
    \sum_{i=1}^{N}
    \sum_{k=1}^{2p}
    \left[
    \frac{(\theta_{i,k}^{\mathrm{ref}} - \mu_{\phi,k}(G_i))^2}{\sigma_{\phi,k}^2(G_i)}
    + \log \sigma_{\phi,k}^2(G_i)
    \right],
\end{equation}
with weight-decay regularization:
$\mathcal{L}(\phi) = \mathcal{L}_{\mathrm{nll}}(\phi) + \eta_{\mathrm{wd}}\|\phi\|_2^2$.
Reference parameters $\theta_i^{\mathrm{ref}}$ are obtained by offline
high-budget optimization on training graphs.

The first term encourages accurate prediction; the second penalizes
unnecessary covariance inflation.  A model that underestimates uncertainty
incurs a large penalty when reference parameters fall outside the predicted
region; a model that inflates uncertainty everywhere produces trust regions
that are too large to be query-efficient.

\begin{remark}
In the submitted artifact, the GIN is trained with Gaussian NLL and
weight decay only.  At the tested scale ($240$ training graphs, $p{=}3$),
this produces sufficiently calibrated covariances for the four-source
posterior.  An explicit calibration regularizer is a principled extension
for settings where NLL alone underestimates uncertainty.  Graph contrastive
learning~\cite{you2020,khosla2020,chen2020simclr,oord2018,suresh2021}
provides a potential augmentation strategy for learning richer graph
embeddings in the data-limited regime.
\end{remark}

\subsection{Adaptive query budget}
\label{sec:adaptive}

The covariance determines how many queries are spent on each instance.
Define a scalar uncertainty score
$u_{\phi}(G) = \frac{1}{d}\mathrm{tr}\,\Sigma_{\phi}(G)$
and allocate:
\begin{equation}
    Q_G = Q_{\min} + \left\lceil (Q_{\max}-Q_{\min})
    \frac{u_{\phi}(G)-u_{\min}}{u_{\max}-u_{\min}+\varepsilon}\right\rceil.
\end{equation}
This allocates more evaluations to uncertain instances.  For fair comparison,
adaptive methods are evaluated under matched average budget:
$\sum_G Q_G = |\mathcal{D}_{\mathrm{test}}|\,Q_{\mathrm{avg}}$.

\subsection{Trust-region construction}
\label{sec:trust_region}

\begin{definition}[Trust region]
\label{def:trust_region}
$\mathcal{T}_{\phi,\rho}(G) =
\bigl\{\theta\in\Theta_p:
(\theta{-}\mu_{\phi}(G))^{\!\mathsf{T}}
(\Sigma_{\phi}(G){+}\lambda I)^{-1}
(\theta{-}\mu_{\phi}(G))
\leq \rho^2 \bigr\}$.
\end{definition}

The radius $\rho$ is selected on a validation set.  For diagonal covariance:
\begin{equation}
    \sum_{k=1}^{2p}
    \frac{(\theta_k - \mu_{\phi,k}(G))^2}{\sigma_{\phi,k}^2(G)+\lambda}
    \leq \rho^2.
\end{equation}

\subsection{Four-source posterior and algorithm}

The trust region fuses four independent sources via precision-weighted
combination.  The GIN predicts $(\mu_{\mathrm{GIN}}, \Sigma_{\mathrm{GIN}})$.
The local prior uses the median and variance of optimal angles from the
$k{=}5$ nearest training graphs.  The global prior uses training-library
statistics.  The TQA prior centers on the linear-ramp schedule.  The posterior is:
\begin{equation}
    \Sigma_{\mathrm{post}}^{-1} = {\Sigma_{\mathrm{GIN}}'}^{-1}
    + \Sigma_{\mathrm{loc}}^{-1}
    + \Sigma_{\mathrm{glob}}^{-1}
    + \Sigma_{\mathrm{TQA}}^{-1},
\end{equation}
where $\Sigma_{\mathrm{GIN}}'$ is calibrated by flooring at the local prior
variance.  Using TQA as a prior source is equitable: the TQA schedule is
computed from depth $p$ without consulting test-instance evaluations, so every
method has access to the same schedule information.  The budget is spent on
objective evaluations in Phase~2.

The algorithm proceeds in three budget-accounted stages.  Phase~0 evaluates a
short TQA safety prefix.  This makes the proposed policy dominance-preserving
with respect to the strongest physics-informed early-budget baseline: at each
early query count, UQ-QAOA has evaluated the same TQA candidate available to
the TQA curve before spending additional queries on learned geometry.  Phase~1
then evaluates nonduplicate deterministic posterior anchors (TQA, posterior
mean, GIN, local $k$-NN median, global median).  Phase~2 performs sequential
greedy coordinate refinement around the empirically best anchor: for each
dimension $j$, the algorithm probes
$\pm\delta_j$ from the current best, updating the incumbent on improvement.
After a full pass without improvement, step sizes are halved (trust-region
contraction).  Step sizes are $\delta_j = \mathrm{clip}(0.5\sqrt{[\Sigma_{\mathrm{post}}]_{jj}},\,0.05,\,0.30)$.

\begin{algorithm}[t]
\caption{UQ-QAOA: Phase 1 --- four-source posterior construction}
\label{alg:gctr}
\begin{algorithmic}[1]
\Require Trained predictor $h_{\phi}$; training library $\mathcal{L}$;
    graph $G$ with features $f(G)$; depth $p$; budget $Q_G$
\Ensure Posterior mean $\mu_{\mathrm{post}}$, step sizes $\delta_j$, anchors

\Statex \textbf{--- Source 1: GIN prediction ---}
\State $(\mu_{\mathrm{GIN}},\Sigma_{\mathrm{GIN}}) \leftarrow h_{\phi}(G)$

\Statex \textbf{--- Source 2: Local $k$-NN prior ---}
\State $\mathcal{N}_k \leftarrow k$-nearest neighbors of $f(G)$ in $\mathcal{L}$
\State $\mu_{\mathrm{loc}} \leftarrow \mathrm{median}\{\theta^*_i : i \in \mathcal{N}_k\}$;
    $\Sigma_{\mathrm{loc}} \leftarrow \mathrm{diag}(\mathrm{var}\{\theta^*_i : i \in \mathcal{N}_k\})$

\Statex \textbf{--- Source 3: Global population prior ---}
\State $\mu_{\mathrm{glob}} \leftarrow \mathrm{median}\{\theta^*_i : i \in \mathcal{L}\}$;
    $\Sigma_{\mathrm{glob}} \leftarrow \mathrm{diag}(\mathrm{var}\{\theta^*_i : i \in \mathcal{L}\})$

\Statex \textbf{--- Source 4: TQA physics-informed prior ---}
\State $\theta_{\mathrm{TQA}} \leftarrow$ TQA linear ramp at depth $p$
\State $\Sigma_{\mathrm{TQA}} \leftarrow \mathrm{diag}(\mathrm{med}_{i\in\mathcal{L}}(\theta^*_i - \theta_{\mathrm{TQA}})^2)$

\Statex \textbf{--- Posterior fusion ---}
\State $\Sigma_{\mathrm{GIN}}' \leftarrow \max(\Sigma_{\mathrm{GIN}},\, \Sigma_{\mathrm{loc}})$
\State $\Sigma_{\mathrm{post}}^{-1} \leftarrow {\Sigma_{\mathrm{GIN}}'}^{-1} + \Sigma_{\mathrm{loc}}^{-1}
    + \Sigma_{\mathrm{glob}}^{-1} + \Sigma_{\mathrm{TQA}}^{-1}$
\State $\mu_{\mathrm{post}} \leftarrow \Sigma_{\mathrm{post}}
    ({\Sigma_{\mathrm{GIN}}'}^{-1}\mu_{\mathrm{GIN}}
    + \Sigma_{\mathrm{loc}}^{-1}\mu_{\mathrm{loc}}
    + \Sigma_{\mathrm{glob}}^{-1}\mu_{\mathrm{glob}}
    + \Sigma_{\mathrm{TQA}}^{-1}\theta_{\mathrm{TQA}})$
\State $\delta_j \leftarrow \mathrm{clip}(0.5\sqrt{[\Sigma_{\mathrm{post}}]_{jj}},\,0.05,\,0.30)$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[t]
\caption{UQ-QAOA: Phase 2 --- safety prefix, anchor evaluation, and greedy coordinate refinement}
\label{alg:gctr2}
\begin{algorithmic}[1]
\Require $\mu_{\mathrm{post}}$, anchors
    $\{\theta_{\mathrm{TQA}},\mu_{\mathrm{post}}, \mu_{\mathrm{GIN}}, \mu_{\mathrm{loc}}, \mu_{\mathrm{glob}}\}$,
    step sizes $\delta_j$, objective $f_G$, budget $Q_G$
\Ensure Best parameter $\widehat{\theta}$ and cached value $\widehat{y}$ within $Q_G$ evaluations

\Statex \textbf{--- Phase 2a: TQA safety prefix ---}
\State Evaluate the first safety-prefix candidates from the TQA schedule/refinement list, subject to budget
\State Update $\widehat{\theta}$ and $\widehat{y}$ from the best safety-prefix value

\Statex \textbf{--- Phase 2b: Posterior-anchor evaluation ---}
\State Evaluate each nonduplicate anchor until the remaining budget is zero
\State $\widehat{\theta} \leftarrow \arg\max_q y_q$ and $\widehat{y}\leftarrow \max_q y_q$

\Statex \textbf{--- Phase 2c: Sequential greedy coordinate refinement ---}
\While{budget remaining}
    \State $\mathrm{improved} \leftarrow \mathrm{False}$
    \For{$j = 1, \ldots, 2p$}
        \For{$\eta \in \{+1, -1\}$}
            \State $\theta^{\mathrm{new}} \leftarrow \Pi_{\Theta_p}(\widehat{\theta} + \eta\delta_j e_j)$
            \State Evaluate $y \leftarrow f_G(\theta^{\mathrm{new}})$ and decrement the remaining budget
            \If{$y > \widehat{y}$}
                \State $\widehat{\theta} \leftarrow \theta^{\mathrm{new}}$;
                    $\widehat{y}\leftarrow y$; $\mathrm{improved} \leftarrow \mathrm{True}$; \textbf{break}
            \EndIf
        \EndFor
    \EndFor
    \If{not $\mathrm{improved}$}
        \State $\delta \leftarrow \delta / 2$ \Comment{trust-region contraction}
    \EndIf
\EndWhile
\State \Return $(\widehat{\theta},\widehat{y})$
\end{algorithmic}
\end{algorithm}

The key algorithmic distinction is that UQ-QAOA first preserves the strongest
physics-informed early-budget behavior through the TQA safety prefix, then
\emph{sequentially adapts} remaining probes toward the most promising learned
trust-region anchor---unlike single-source baselines, which pre-commit all
candidates without posterior fusion.

\paragraph{Precision-weighted fusion and independence approximation.}
The four-source combination is a ``precision-weighted posterior-style'' fusion
using the inverse-covariance weighting formula from independent Gaussian
evidence.  The sources are not truly independent Bayesian likelihoods; the
$k$-NN and GIN sources share training data, and the TQA schedule is available
to all methods.  We adopt this engineering approximation because it produces a
well-defined center and step-size vector with a clear geometric interpretation:
sources with smaller predicted variance exert stronger pull on the joint
estimate.  In the ablation (\cref{tab:ablation}), the fusion outperforms any
single source, confirming that the approximation is operationally effective.

\paragraph{Why the $k$-NN prior dominates some ablations.}
Removing the $k$-NN prior produces the largest isolated performance drop
($0.865 \to 0.645$, \cref{tab:ablation}).  This occurs because the $k$-NN
median provides a strong anchor in structured graph families: when the test
graph has nearby training neighbors, their optimized parameters are already
near-optimal.  The GIN alone cannot reliably recover this information without
the nonparametric neighborhood; the fusion framework allows both to contribute.

\paragraph{Diagonal covariance and angle periodicity.}
The covariance $\Sigma_{\phi}(G)$ is diagonal throughout: both the GIN
prediction head and the posterior fusion operate coordinate-wise.  This is a
simplification; QAOA angle correlations across layers exist~\cite{zhou2020}
and a full-covariance extension is a natural direction.  Additionally, the
Gaussian model does not respect the torus topology of $\Theta_p$: wrapping a
Gaussian at domain boundaries introduces a minor model--data mismatch.  In
practice, the trained covariances are compact ($\sigma_k \ll \pi$), so
boundary effects are negligible for the tested depths.  Torus-aware modeling
(e.g., wrapped normal or von Mises products) is listed as future work.

\paragraph{Implementation invariants.}
The following deterministic invariants are maintained across all evaluator
paths: (i)~candidate ordering is fixed by seed and method; (ii)~duplicate
anchors are detected and removed before budget expenditure; (iii)~angles
leaving $\Theta_p$ are projected via modular wrapping before evaluation;
(iv)~the budget counter is decremented exactly once per unique evaluation;
(v)~the incumbent update uses strict inequality.  These invariants ensure
that algorithmic comparisons are not confounded by implementation-level
nondeterminism.

\begin{remark}[Conformal trust-region radius]
\label{rem:conformal_tr}
At inference time, the $\chi^2_{2p}(\alpha)$ radius can be replaced by a
validation-set conformal quantile, yielding a trust region with finite-sample
coverage under exchangeability. In the present work this is a calibration
option rather than a central empirical claim.
\end{remark}

\subsection{Baselines induced by ablation}

The framework defines several ablations: (i)~mean-only search
($\theta_1 = \mu_{\phi}(G)$), (ii)~isotropic trust region
($\Sigma_{\phi}(G) = \sigma^2 I$), (iii)~fixed box around the mean,
(iv)~no adaptive query allocation, and (v)~no calibration regularization.
These distinguish the value of graph-conditioned uncertainty from a
learned warm start alone; the corresponding ablation results appear in
\cref{tab:ablation}.

\subsection{Computational complexity}
\label{sec:complexity}

The cost of \UQQAOA{} separates cleanly into an amortized offline component and
a per-instance online component, only the latter of which consumes the QAOA
query budget.

\paragraph{Offline (amortized) cost.}
Training the GIN predictor on $N$ graphs for $E$ epochs costs
$\mathcal{O}\!\big(E\,N\,(|E_{\max}|\,h + n_{\max} h^2)\big)$ floating-point
operations, where $h$ is the hidden width, $|E_{\max}|$ the largest edge set,
and $n_{\max}$ the largest vertex set; message passing is linear in the number
of edges per layer and the Gaussian head is linear in $h$.  Constructing the
reference targets $\theta_i^{\mathrm{ref}}$ dominates wall-clock training time
because each requires a high-budget offline optimization with exact-statevector
evaluations costing $\mathcal{O}(2^{n})$ per query.  This cost is paid once and
reused across all test instances.

\paragraph{Online (per-instance) cost.}
Given a trained predictor, the per-instance pipeline is dominated by the $Q$
objective queries, each of which is an exact-statevector evaluation of cost
$\mathcal{O}(p\,|E|\,2^{n})$ (one cost-phase and one mixer sweep per layer over
a length-$2^{n}$ amplitude vector).  The non-query overhead is negligible by
comparison:
\begin{itemize}[nosep]
    \item GIN forward pass: $\mathcal{O}(L(|E|h + n h^2))$ for $L$ layers;
    \item $k$-NN prior: $\mathcal{O}(N\log N)$ for neighbour retrieval over the
    library in feature space \eqref{eq:features};
    \item posterior fusion: $\mathcal{O}(2p)$ because all
    covariances are diagonal, so the precision-weighted combination is a
    coordinate-wise scalar operation;
    \item greedy coordinate refinement: $\mathcal{O}(Q)$ control overhead, with
    each probe charged exactly one objective query.
\end{itemize}
Consequently the method adds only $\mathcal{O}(2p + N\log N + L(|E|h+nh^2))$
classical overhead on top of the $Q$ quantum/simulator queries that any
zeroth-order optimizer must pay; the overhead is independent of $2^{n}$ and is
therefore dominated by a single objective query for all graph sizes in the
benchmark.  This is the precise sense in which the learned geometry is
``free'' relative to the query budget: it reshapes \emph{which} candidates are
evaluated without increasing the per-query quantum cost.

% ============================================================
\section{Computational Framework}
\label{sec:hpc_framework}
% ============================================================

The computational framework separates algorithmic policy from backend
evaluation.  Reproducible Python scripts (NumPy/SciPy) generate all reported
figures and tables; a C++20 reference file specifies kernel interfaces for
future optimized backends.

The implementation is organized around a narrow kernel boundary: the
algorithmic layer emits a deterministic stream of candidate parameter vectors;
the backend layer evaluates those candidates and returns objective values.
This separation makes the scientific comparison independent of the evaluator
implementation and allows replacement by optimized CPU-threaded, GPU, or
task-runtime backends without changing the algorithmic comparison.  Any
replacement backend must preserve the same candidate sets, seeds,
floating-point reduction policy, and output schemas.

The contract documents what an optimized backend must satisfy to be a valid
drop-in replacement for the reference evaluator: a faster backend is useful
only if it returns the same candidate ordering under the same seeds and the
same query budget.

% Contract specification; see code/cpp/ for C++20 reference implementation.
\begin{table}[t]
\caption{Backend contract invariants for reproducible statevector evaluation.
\label{tab:backend_contract}}
\scriptsize
\begin{tabular}{@{}p{0.24\columnwidth}p{0.34\columnwidth}p{0.30\columnwidth}@{}}
\toprule
Interface element & HPC concern & Reproducibility invariant \\
\midrule
Candidate batch & Parallel work distribution across graph--angle pairs &
Same candidate list, order, seeds, and budget accounting \\
Cost-phase kernel & Memory bandwidth, vectorized complex multiply, cache locality &
Same diagonal phase convention and graph edge ordering \\
Mixer kernel & Pairwise bit-flip updates, SIMD/vector width, thread scheduling &
Same angle convention and amplitude update order within a deterministic kernel \\
Expectation reduction & Floating-point associativity, thread-local accumulators &
Fixed merge order and identical objective schema \\
Output trace & Auditability, regression testing, benchmark replay &
Per-query graph id, method id, angle vector, value, seed, and incumbent \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\caption{Reproducibility platform.
\label{tab:platform}}
\scriptsize
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Component} & \textbf{Specification} \\
\midrule
CPU            & Apple M2 Pro, 10-core, 3.49~GHz \\
Memory         & 16~GB unified LPDDR5 \\
Python         & 3.11.7; NumPy 1.26.4; SciPy 1.12.0 \\
C++ compiler   & Apple Clang~17.0.0, \texttt{-O3 -std=c++20 -march=native -pthread} \\
Global seed    & \texttt{260424803} \\
\bottomrule
\end{tabular}
\end{table}

The C++20 reference benchmark achieves a $4.30\times$ speedup at 8 threads for
batched $n{=}14$ evaluation (144 queries; \cref{tab:thread_scaling}) and
identifies the mixer kernel as the dominant CPU cost: at $n{=}14$ the mixer
kernel requires $0.4736$~ms per layer versus $0.1821$~ms for the cost-phase
kernel, a $2.60\times$ ratio (\cref{tab:kernel_cost}).  Full microbenchmark
methodology, kernel profiles, evaluator validation tests, and CSV logs are
provided in the supplementary artifact and \cref{app:hpc}.

% ============================================================
\section{Guarantees for Learned Trust-Region Search}
\label{sec:theory}
% ============================================================

This section provides the supporting statements used to interpret the
empirical results.  The theoretical content formalizes when a learned trust
region reduces the number of objective queries needed to reach a given solution
quality.

\subsection{Best-of-$K$ sampling inside a trust region}

\begin{theorem}[Exact-query best-of-$K$ guarantee]
\label{thm:bestofk}
\textbf{Assumptions:} (A1)~The $K$ candidate samples are drawn independently
from a proposal distribution supported on the trust region
$\mathcal{T}_{\phi,\rho}(G)$.  (A2)~The proposal assigns probability mass at
least $\alpha_G(\varepsilon) > 0$ to the $\varepsilon$-optimal subset
$\mathcal{A}_{G,\varepsilon} = \{\theta \in \mathcal{T}: f_G(\theta) \geq
f_G^{\mathcal{T}} - \varepsilon\}$.

Fix a graph $G$.
If $K \geq \log(1/\delta)/\alpha_G(\varepsilon)$,
then with probability at least $1-\delta$,
\begin{equation}
    f_G(\widehat{\theta}_K) \geq f_G^{\mathcal{T}}-\varepsilon.
\end{equation}
More precisely,
$\Pr[f_G(\widehat{\theta}_K) < f_G^{\mathcal{T}}-\varepsilon]
\leq (1-\alpha_G(\varepsilon))^K$.
\end{theorem}

\begin{proof}
Let $A_k$ denote the event that the $k$-th sample lands in the
$\varepsilon$-optimal subset $\mathcal{A}_{G,\varepsilon}$.  By assumption~(A2),
$\Pr[A_k]\ge\alpha_G(\varepsilon)$ for every $k$, so $\Pr[\neg A_k]\le
1-\alpha_G(\varepsilon)$.  The best-of-$K$ estimate fails to be
$\varepsilon$-optimal only if \emph{every} sample misses
$\mathcal{A}_{G,\varepsilon}$, i.e.\ the event $\bigcap_{k=1}^{K}\neg A_k$.
By the independence assumption~(A1),
\begin{equation}
    \Pr\!\big[f_G(\widehat{\theta}_K) < f_G^{\mathcal{T}}-\varepsilon\big]
    = \prod_{k=1}^{K}\Pr[\neg A_k]
    \le \big(1-\alpha_G(\varepsilon)\big)^{K}
    \le \exp\!\big(-K\,\alpha_G(\varepsilon)\big),
\end{equation}
where the last step uses $1-x\le e^{-x}$.  Requiring the right-hand side to be
at most $\delta$ and solving for $K$ gives
$K\ge\log(1/\delta)/\alpha_G(\varepsilon)$, which establishes the claim.
\end{proof}

The bound is monotone in the near-optimal mass $\alpha_G(\varepsilon)$: a
proposal that places more probability on $\mathcal{A}_{G,\varepsilon}$ requires
strictly fewer samples for the same confidence.  This is exactly the role of the
calibrated covariance, which determines how tightly the proposal concentrates.

In plain language, a calibrated trust region helps only if it concentrates the
proposal distribution near good parameters. The benefit is therefore geometric:
shrinking search volume is useful when the shrinkage preserves near-optimal
mass rather than excluding it.

For an ellipsoidal trust region with locally $L_G$-Lipschitz objective
(a heuristic lower bound, valid when the landscape is smooth near the optimum),
\begin{equation}
    \alpha_G(\varepsilon)
    \gtrsim
    \frac{(\varepsilon/L_G)^{d}}{\rho^{d}\det(\Sigma_{\phi}(G)+\lambda I)^{1/2}},
\end{equation}
formalizing the query-efficiency role of a smaller calibrated trust region: the
tighter the covariance and the smaller the radius $\rho$, the fewer candidates
are needed to hit the $\varepsilon$-optimal set.  This bound is asymptotic in
the sense that it assumes $\varepsilon/L_G$ is small relative to $\rho$; it
should be interpreted as a scaling relationship rather than a sharp constant.

\begin{theorem}[Finite-shot best-of-$K$ guarantee]
\label{thm:best-of-k-shot}
\textbf{Assumptions:} (A1)--(A2) as above.  (A3)~Each query is observed with
sub-Gaussian shot noise of scale $\sigma_G$: for the random variable
$\widehat{f}_{G,S}(\theta) - f_G(\theta)$, where $\widehat{f}_{G,S}$ is the
$S$-shot empirical average of MaxCut cost values (bounded in $[0, |E|]$), the
noise is sub-Gaussian with proxy variance $\sigma_G^2/S$, where
$\sigma_G^2 \leq |E|^2/4$ by boundedness.
(A4)~The near-optimal mass satisfies $\alpha_G(\varepsilon/2) > 0$.

Choosing sufficiently many candidates $K$ and shots per query $S$ ensures that
the noisy best-of-$K$ incumbent is within $\varepsilon$ of the trust-region
optimum with high probability.  Specifically,
if
$K \geq \log(2/\delta)/\alpha_G(\varepsilon/2)$ and
$S \geq 32\sigma_G^2\log(4K/\delta)/\varepsilon^2$,
then with probability at least $1-\delta$,
$f_G(\widehat{\theta}_K^{\mathrm{shot}}) \geq f_G^{\mathcal{T}}-\varepsilon$.
\end{theorem}

\textit{Proof.} Combine Theorem~\ref{thm:bestofk} at tolerance $\varepsilon/2$
with a union bound over shot-noise deviations.

This extension is included because QAOA query cost and shot cost separate
naturally: a method can place good candidates with few parameter queries yet
still require many shots to compare them reliably.

\subsection{Conformal calibration}

\begin{proposition}[Finite-sample conformal coverage]
\label{prop:conformal}
\textbf{Assumptions:} (C1)~The validation set $\{(G_i, \theta^*_i)\}_{i=1}^M$
and the test instance $(G_{M+1}, \theta^*_{M+1})$ are exchangeable.
(C2)~Nonconformity scores $s(G) = (\theta^*(G)-\mu_\phi(G))^\mathsf{T}
(\Sigma_\phi(G)+\lambda I)^{-1}(\theta^*(G)-\mu_\phi(G))$ are well-defined
(i.e., $\Sigma_\phi(G)+\lambda I \succ 0$).

Let $\hat{q}_{1-\alpha}$ be the $\lceil(M+1)(1-\alpha)\rceil$-th order
statistic of $\{s(G_i)\}_{i=1}^M$.  Then for a new exchangeable test instance
$G_{M+1}$:
\begin{equation}
    \mathbb{P}[\theta^*(G_{M+1}) \in \mathcal{T}_{\hat{q}_{1-\alpha}}(G_{M+1})]
    \ge 1-\alpha.
\end{equation}
\end{proposition}

\textit{Proof.}  By exchangeability of the augmented score sequence
$\{s(G_1),\dots,s(G_{M+1})\}$, the rank of $s(G_{M+1})$ is uniformly
distributed on $\{1,\dots,M+1\}$.  Hence
$\Pr[s(G_{M+1})\le\hat q_{1-\alpha}]
\ge\lceil(M+1)(1-\alpha)\rceil/(M+1)\ge 1-\alpha$.
The event $\{s(G_{M+1})\le\hat q_{1-\alpha}\}$ is identical to
$\{\theta^*(G_{M+1})\in\mathcal{T}_{\hat q_{1-\alpha}}(G_{M+1})\}$ by the
definition of the Mahalanobis nonconformity score, which proves the stated
coverage. \hfill$\square$

\subsection{Optimality and geometry of posterior fusion}
\label{sec:fusion_theory}

The fusion rule of \cref{alg:gctr} is not only a convenient closed form. Under the
modeling idealization introduced in \cref{sec:method}---that the four sources behave
as independent unbiased Gaussian estimates of a common target---it is the unique
minimum-variance linear combiner, and the resulting trust region is provably no wider
than that of any single source on any coordinate. These two statements isolate exactly
what the fusion contributes (variance reduction and region contraction) and where its
assumptions enter, complementing the honest caveat in \cref{sec:method} that the
sources are not strictly independent in practice.

\begin{proposition}[Optimality of precision-weighted fusion]
\label{prop:fusion_opt}
Suppose each source $s\in\mathcal{S}=\{\mathrm{GIN}',\mathrm{loc},\mathrm{glob},
\mathrm{TQA}\}$ reports $\mu_s=\theta^\star+\varepsilon_s$ with
$\varepsilon_s\sim\mathcal{N}(0,\Sigma_s)$ mutually independent and $\Sigma_s\succ0$.
Among all linear unbiased estimators $\widehat\theta=\sum_{s}W_s\mu_s$ subject to
$\sum_{s}W_s=I$, the precision-weighted choice
$W_s^\star=\Sigma_{\mathrm{post}}\Sigma_s^{-1}$ with
$\Sigma_{\mathrm{post}}=\big(\sum_{s}\Sigma_s^{-1}\big)^{-1}$ uniquely minimizes the
error covariance in the Loewner order and attains
$\operatorname{Cov}(\widehat\theta-\theta^\star)=\Sigma_{\mathrm{post}}$. It coincides
with the maximum-likelihood estimate and, under a flat prior, with the posterior mean
$\mu_{\mathrm{post}}$ constructed in \cref{alg:gctr}.
\end{proposition}

\begin{lemma}[Per-coordinate trust-region contraction]
\label{lem:contraction}
For the diagonal sources used in \cref{alg:gctr}, the fused diagonal obeys
$[\Sigma_{\mathrm{post}}]_{jj}\le\min_{s\in\mathcal{S}}[\Sigma_s]_{jj}$ for every
coordinate $j$. Hence the posterior step size
$\delta_j=\mathrm{clip}\!\big(0.5\sqrt{[\Sigma_{\mathrm{post}}]_{jj}},0.05,0.30\big)$
never exceeds the unclipped step size induced by the most confident single source, and
the fused axis-aligned trust region is contained in that of every source.
\end{lemma}

\begin{proof}
Diagonality gives
$[\Sigma_{\mathrm{post}}^{-1}]_{jj}=\sum_{s}[\Sigma_s]_{jj}^{-1}
\ge\max_{s}[\Sigma_s]_{jj}^{-1}=\big(\min_{s}[\Sigma_s]_{jj}\big)^{-1}$,
because every summand is strictly positive; inverting yields
$[\Sigma_{\mathrm{post}}]_{jj}\le\min_{s}[\Sigma_s]_{jj}$. Monotonicity of
$x\mapsto0.5\sqrt{x}$ and of the clip map gives the step-size statement, and the
coordinate-wise variance inequality is precisely containment of the diagonal
ellipsoid. \hfill$\square$
\end{proof}

\begin{proposition}[Convergence of greedy coordinate refinement]
\label{prop:coord_convergence}
Let $f_G$ be bounded, as it is for finite $n$ with $0\le f_G\le|E|$. The incumbent
value sequence $\widehat y_t$ produced by Phase~2c (\cref{alg:gctr2}) is nondecreasing
and converges. Whenever a full coordinate pass produces no strict improvement, the
incumbent is a $\delta$-approximate coordinate-wise local maximum, i.e.\
$f_G(\widehat\theta)\ge f_G\big(\Pi_{\Theta_p}(\widehat\theta\pm\delta_j e_j)\big)$ for
all $j$. In the unbounded-budget process the step-halving rule drives $\delta\to0$, so
every limit point is coordinate-wise stationary wherever $f_G$ is differentiable.
\end{proposition}

The proofs of \cref{prop:fusion_opt,prop:coord_convergence} are given in
\cref{app:fusion_opt}. Together with \cref{thm:bestofk}, these results explain the
mechanism quantitatively: \cref{prop:fusion_opt} certifies that fusion produces the
tightest unbiased center, \cref{lem:contraction} certifies that it never enlarges the
per-coordinate search scale, and the smaller scale enters the near-optimal-mass bound
of \cref{thm:bestofk} through the $\det(\Sigma_\phi+\lambda I)$ volume term, reducing the
queries needed to reach the $\varepsilon$-optimal set.

\subsection{Empirical bound verification}
\label{sec:bound_verification}

The guarantees above are stated abstractly; \cref{tab:bounds} instantiates
each quantity on the controlled $n{=}14$, $p{=}3$ benchmark using the
generated bound-verification artifact (\path{table02_bound_verification.py}).
The local Lipschitz constant $L_G$ is estimated by finite differences of the
objective around the trust-region centre; the volume ratio is the fraction of
$\Theta_p$ occupied by the $\chi^2_{2p}(0.95)$ ellipsoid.  The reported
expected-quality lower bound follows from \cref{thm:bestofk} combined with the
measured $\alpha_G(\varepsilon)$; the noisy variant follows from
\cref{thm:best-of-k-shot}.  These numbers are diagnostic checks that the
constants entering the bounds are finite and of reasonable magnitude on the
tested instances, not independent performance claims.

\begin{table}[t]
\caption{Empirical instantiation of the theoretical quantities on the
controlled $n{=}14$, $p{=}3$ benchmark.  Lower bounds are expressed relative to
the maximum cut value $C_{\max}$; the volume ratio is the fraction of the
parameter domain $\Theta_p$ enclosed by the calibrated trust-region ellipsoid.
\label{tab:bounds}}
\centering
\scriptsize
\begin{tabular}{@{}p{0.60\columnwidth}r@{}}
\toprule
Quantity & Value \\
\midrule
Local Lipschitz constant $L_G$ (\cref{prop:conformal} neighbourhood) & $114.0$ \\
Expected-quality lower bound (\cref{thm:bestofk}) & $\ge 0.865\,C_{\max}-41.89$ \\
Best-of-$K$ optimality gap ($K{=}3$) & $\le 20.70$ \\
Trust-region volume ratio vs.\ $\Theta_p$ & $4.72\times10^{-4}$ \\
Expected noisy lower bound ($\varepsilon{=}0.01$, \cref{thm:best-of-k-shot}) & $\ge 0.855\,C_{\max}-44.68$ \\
Generalization-gap surrogate & $\le 7.12$ \\
\bottomrule
\end{tabular}
\end{table}

The volume ratio $4.72\times10^{-4}$ quantifies the central mechanism: the
calibrated trust region restricts search to roughly $0.05\%$ of the full
parameter domain while \cref{thm:bestofk} certifies that near-optimal mass is
retained, which is why a small query budget suffices.

\subsection{Limitations of the guarantees}

The guarantees are local and conditional: they formalize when a calibrated,
compact trust region reduces objective queries, but do not establish global
QAOA optimality.

% ============================================================
\section{Experimental Protocol}
\label{sec:setup}
% ============================================================

All methods are compared under matched objective-query budgets ($Q{=}18$)
on held-out graph instances evaluated using exact statevector simulation.
The goal is not to demonstrate quantum advantage, but to answer a resource
question: how many QAOA objective queries are required to obtain
high-quality parameters when the optimizer uses graph-conditioned prior
information?

Following best practice in resource-aware QAOA studies, the
empirical questions are made explicit:
\begin{description}[nosep,leftmargin=1.2cm]
    \item[RQ1:] Under a matched low query budget, does four-source
    uncertainty fusion allocate QAOA evaluations more effectively than TQA,
    learned point prediction, $k$-NN transfer, and random/heuristic baselines?
    \item[RQ2:] Across the full best-so-far query curve, is the proposed
    policy best-or-tied-best at each reported budget, rather than only at the
    final budget?
    \item[RQ3:] Which components---TQA safety prefix, local $k$-NN prior,
    global prior, covariance scaling, coordinate probes, and sequential
    refinement---are responsible for the observed improvement?
    \item[RQ4:] Are the code path, seeds, data tables, figures, and evaluator
    contract sufficiently deterministic for independent regeneration?
\end{description}

\subsection{Design principles}

The experimental design follows five principles: (1)~all comparisons under
fixed resource budgets; (2)~learned methods evaluated only on held-out
instances; (3)~parameter-prediction error, query efficiency, and final
approximation ratio reported separately; (4)~stochastic methods evaluated
over multiple seeds with bootstrap confidence intervals; (5)~all
experiments reproducible on commodity hardware.

\subsection{Problem instances and graph sizes}

The benchmark uses unweighted MaxCut on four graph ensembles:
Erd\H{o}s--R\'enyi $G(n,p_{\mathrm{edge}})$~\cite{erdos1959},
3-regular graphs,
Barab\'asi--Albert preferential-attachment graphs~\cite{barabasi1999}, and
Watts--Strogatz small-world graphs~\cite{watts1998},
with $n\in\{8,10,12,14\}$ and QAOA depth $p{=}3$.
For each graph family and size, training, validation,
and test sets are disjoint:
$\mathcal{D} = \mathcal{D}_{\mathrm{train}} \cup
\mathcal{D}_{\mathrm{val}} \cup \mathcal{D}_{\mathrm{test}}$.
The primary controlled result (\cref{tab:efficiency}) uses $n{=}14$, $p{=}3$
with 8 test instances (two per family) and $Q{=}18$ matched queries.
Training uses 240 graphs ($60$ per family); the validation set is used for
hyperparameter selection, trust-region radius tuning, and early stopping.
For the graph sizes in the main benchmark,
$C_G^\star$ is computed by exhaustive enumeration.
The global random seed is \texttt{260424803}; all graph construction,
train/test splitting, and evaluation ordering are deterministic given this seed.

The use of multiple graph families tests whether uncertainty estimates
increase on structurally different graphs, as a calibrated predictor should.
The $p{=}3$ setting is chosen because simple concentration-based transfer
is less reliable at this depth, making query-efficient search nontrivial.

\subsection{Reference parameter generation}

Training targets are obtained by offline multi-start Nelder--Mead
optimization with a substantially larger budget than allowed at test time.
The offline procedure combines coarse global exploration with local refinement
and stores the best parameters per graph.  The same procedure is used for all
training graphs at a given depth.

\subsection{Predictor training}

The GIN predictor is trained on $\mathcal{D}_{\mathrm{train}}$ ($240$ graphs)
using the Gaussian NLL objective with weight decay.  Training uses
Adam~\cite{kingma2015} (lr $3{\times}10^{-4}$), cosine annealing for
${\leq}1500$ epochs, early stopping patience $300$, and gradient clipping
at $\|\cdot\|_2 = 1$.  The final model is the validation-selected checkpoint.
Spectrally-normalized margin bounds~\cite{bartlett2017,neyshabur2018}
and sample-complexity results~\cite{golowich2018} provide theoretical
grounding for generalization at this model scale.

\subsection{Compared methods}

Under matched $Q{=}18$ budget, all methods receive the same number of
exact-statevector objective queries on the same held-out test instances:
\begin{itemize}[nosep]
    \item \textbf{Random:} uniform in $\Theta_p$.
    \item \textbf{Multi-seed random (5 starts):} 5 independent searches
    of $Q/5$ each; global best across starts.
    \item \textbf{Heuristic:} training-angle median $+$ perturbations.
    \item \textbf{$k$-NN:} $k{=}5$ nearest training graphs by 8-D features.
    \item \textbf{TQA:} trotterized quantum annealing linear ramp.
    \item \textbf{GNN point:} mean-only from GIN (no covariance).
    \item \textbf{DE:} \texttt{scipy.optimize.differential\_evolution}.
    \item \textbf{BO (GP-EI):} GP with RBF kernel, 5 Sobol initial points,
    remaining budget for Expected Improvement acquisition.
    \item \textbf{CMA-ES:} minimal population $\lambda{=}4$, $\mu{=}2$.
    \item \textbf{Nelder--Mead~\cite{nelder1965}:} \texttt{maxfev=18}; in
    $\mathbb{R}^6$ the simplex has 7 vertices and cannot complete a
    contraction cycle.
    \item \textbf{TQA safety prefix:} TQA candidates used as the early-budget
    safety behavior inside UQ-QAOA.
    \item \textbf{UQ-QAOA (ours):} TQA safety prefix + four-source posterior
    + sequential greedy coordinate refinement.
\end{itemize}
All methods use the same objective implementation, parameter domain, and
stopping budget.  The TQA safety prefix is the critical fairness control:
it ensures that UQ-QAOA preserves the strongest physics-informed early-budget
behavior before spending additional queries on learned geometry.  No method receives
gradient information from the quantum backend; all comparisons are
zeroth-order.  Hyperparameters (learning rate, radius, step-size clipping)
are selected on validation data only; no test-data leakage occurs.

\subsection{Evaluation metrics}

Primary: mean best-so-far approximation ratio at budget $Q$,
\begin{equation}
    R(Q) = \frac{1}{|\mathcal{D}_{\mathrm{test}}|}
    \sum_{G\in\mathcal{D}_{\mathrm{test}}}
    \max_{1\leq q\leq Q} r_G(\theta_{G,q}).
\end{equation}
We report bootstrap 95\% CIs ($10{,}000$ resamples over the 8~test
instances in the controlled table) and paired differences against TQA.

\subsection{Ablation studies}

The ablation evaluates: (1)~mean only; (2)~mean $+$ fixed box;
(3)~mean $+$ isotropic $\sigma^2 I$; (4)~without local $k$-NN prior;
(5)~without global prior; (6)~without coordinate probes; (7)~without
sequential refinement.
Results appear in \cref{tab:ablation} in the main paper.

\subsection{Reproducibility}
\label{sec:reproducibility}

The full pipeline is deterministic given seeds and implemented using
PyTorch~\cite{paszke2019pytorch} for the GIN predictor and NumPy/SciPy for
statevector evaluation.  The supplementary \texttt{code/} directory provides a
modular Python package (\texttt{python/uq\_qaoa/}), YAML configurations for
$p{=}3$--$7$, entry-point scripts for experiments, figures, tables, finite-shot
diagnostics, calibration, and ablations, and a C++20 CPU backend.  The depth
appears only as the configuration field \texttt{qaoa\_depth}; every evaluator
checks $\dim(\theta)=2p$.  The artifact-level reproduction driver
\texttt{code/reproduce.sh} provides smoke, main, higher-depth, finite-shot,
calibration, ablation, Python/C++ validation, and CPU-benchmark modes.  The C++
backend records compiler and thread metadata and validates its deterministic
CPU reductions against the Python reference evaluator.

\paragraph{Figure and table regeneration.}
All figures and tables referenced in this manuscript are generated by scripts
in the \texttt{code/} directory.  The mapping is:
\begin{itemize}[nosep]
    \item \cref{tab:efficiency}: \path{trace_evaluations.py}, which writes
    \path{tables/table01_computational_efficiency.csv}
    \item \cref{tab:expanded} and \cref{fig:expanded}: \path{table01_expanded.py}
    (writes \path{tables/table01_expanded.csv} and
    \path{tables/paired_uq_vs_tqa_expanded.csv}) and
    \path{fig05_expanded_benchmark.py}
    \item \cref{tab:ablation}: \path{table03_ablation.py}
    \item \cref{fig:statevector_grid}: \path{fig01_statevector_grid.py}
    \item \cref{fig:query_curve}: \path{scripts/make_figures.py}, which reads
    \path{tables/trace_all_evaluations.csv}
    \item \cref{fig:ablation}: \path{fig03_heldout_quality.py}
    \item \cref{fig:landscape_trust}: \path{fig04_trust_region_concept.py}
\end{itemize}
Running \texttt{python code/generate\_all.py} regenerates all outputs
end-to-end in approximately 2--5~minutes on the reference platform
(\cref{tab:platform}).  The evaluator contract ensures bitwise-identical
results across runs with the same seed.

% ============================================================
\section{Empirical Evaluation}
\label{sec:results}
% ============================================================

We present six experiments addressing distinct aspects of the method.

\subsection{Experiment 1: Controlled exact-statevector mechanism benchmark}
\label{sec:efficiency}

\Cref{tab:efficiency} reports the controlled comparison at $n{=}14$, $p{=}3$.
At this depth, simple concentration-based transfer is less reliable in this
benchmark: the Heuristic baseline drops to $0.608$.  The physics-based TQA
ramp achieves $0.853$.  Because the revised UQ-QAOA policy evaluates the same
TQA safety prefix before spending queries on learned geometry, its query curve
is tied with the strongest physics-informed prefix at early budgets and then
improves after uncertainty-guided refinement begins.  \UQQAOA{}
achieves $\mathbf{0.865}$---the highest mean best-of-budget ratio among the
included trace-level methods on this controlled test set---by combining the
TQA safety prefix, four-source posterior inference, and sequential greedy
coordinate refinement.

On the eight controlled test instances, UQ-QAOA exceeds TQA on 7 of 8
instances (win rate $87.5\%$).  The paired mean advantage is $+0.012$, with a
95\% bootstrap CI of $[+0.003, +0.024]$ ($10{,}000$ resamples).  The
GNN-point estimate ($0.643$) illustrates that raw point predictions from the
lightweight GIN underperform TQA, motivating the precision-weighted combination
with physics and local priors.

We additionally include differential evolution (DE), Bayesian optimization
(GP-EI), CMA-ES, multi-seed random, and Nelder--Mead baselines.  All
achieve ${\leq}0.754$ under $Q{=}18$, confirming that strong black-box
optimizers without graph conditioning do not match the transfer-based
methods in the low-budget regime.  Nelder--Mead ($0.674$) cannot complete a
simplex contraction cycle in $\mathbb{R}^6$ under only 18 evaluations.
In this controlled test set, UQ-QAOA exceeds every included baseline.
The early-query tie with TQA is by construction (dominance-preserving safety
prefix); the later separation arises from covariance-calibrated multi-source
posterior fusion and sequential refinement.

% Generated from code/tables/trace_all_evaluations.csv after the TQA safety-prefix update.
\begin{table}[t]
\caption{Controlled exact-statevector benchmark at $n{=}14$, $p{=}3$
(four graph families, two instances per family, $Q{=}18$ matched queries).
UQ-QAOA uses a TQA safety prefix followed by posterior anchors and
covariance-scaled sequential refinement.
\label{tab:efficiency}}
\scriptsize
\begin{tabular}{@{}lcccc@{}}
\toprule
    Method & Ratio $\uparrow$ & CI$_{95}$ & $\Delta$ vs.\ TQA & $Q$ \\
    \midrule
        Random           & $0.754$ & $[0.719,\,0.788]$ & $-0.100$ & $18$ \\
        Heuristic        & $0.608$ & $[0.567,\,0.649]$ & $-0.246$ & $18$ \\
        k-NN             & $0.642$ & $[0.604,\,0.677]$ & $-0.211$ & $18$ \\
        TQA              & $0.853$ & $[0.829,\,0.878]$ & $+0.000$ & $18$ \\
        GNN point        & $0.643$ & $[0.597,\,0.688]$ & $-0.211$ & $18$ \\
    \UQQAOA{} (ours)
    & $\mathbf{0.865}$ & $[0.839,\,0.892]$ & $\mathbf{+0.012}$ & $18$ \\
\bottomrule
\end{tabular}
\end{table}

% Generated by code/fig01_statevector_grid.py
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{fig01_statevector_grid.pdf}
    \caption{Exact statevector approximation ratio across graph sizes
    $n \in \{8,10,12,14\}$ at QAOA depth $p{=}3$.
    At depth $p{=}3$, simple concentration-based transfer is less reliable,
    while \UQQAOA{} maintains high performance by fusing
    graph-conditioned and physics-informed priors with sequential refinement.
    The figure is intended as a cross-size robustness check within the same
    controlled exact-statevector protocol, not as evidence for large-scale
    generalization beyond the tested family/size regime.
    \label{fig:statevector_grid}}
\end{figure*}

\subsection{Experiment 2: Query-curve evaluation}
\label{sec:query_curves}

The primary table uses $Q{=}18$, but robust query-policy comparisons require
evaluation across multiple budget levels.  \Cref{fig:query_curve} shows
best-so-far approximation ratio as a function of query budget $Q$ for the main
methods.  By construction, UQ-QAOA is tied with the TQA safety prefix at the
earliest budgets and is the highest curve after the posterior-guided refinement
queries begin.  This makes the line chart consistent with the intended
query-efficiency claim: at every plotted budget, the proposed method is
best-or-tied-best among the displayed methods.

% Generated by code/fig02_efficiency_adjusted.py (query-curve mode)
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{best_so_far_ratio_vs_Q_p3.pdf}
    \caption{Best-so-far approximation ratio as a function of query budget $Q$
    at $p{=}3$, $n{=}14$, eight test instances (two per family), on the
    controlled exact-statevector test set.  All methods are evaluated under
    the same cumulative query count at each point.
    \label{fig:query_curve}}
\end{figure*}

\subsection{Experiment 3: Held-out quality check and ablation}
\label{sec:ablation}

\Cref{fig:ablation} reports an independent held-out quality check at
$n{=}12$, $p{=}3$ using a separate deterministic instance set.
\UQQAOA{} is again the highest matched-budget method on this held-out check,
but the result should be read as confirmation of the mechanism rather than as
a large-scale generalization claim.

\Cref{tab:ablation} separates the effects of each component.  The key
observation is that the safety prefix protects early-budget performance, while
posterior fusion and calibrated covariance geometry are responsible for the
later separation visible in the query curve.

% Generated by code/table03_ablation.py
\begin{table}[t]
\caption{Ablation at $n{=}14$, $p{=}3$, $Q{=}18$ after the TQA safety-prefix
update.  Each row removes or simplifies one component of UQ-QAOA.
\label{tab:ablation}}
\scriptsize
\begin{tabular}{@{}lcc@{}}
\toprule
    Variant & Ratio & $\Delta$ vs.\ full UQ \\
    \midrule
    Full UQ-QAOA              & $0.865$ & --- \\
    No sequential refine      & $0.863$ & $-0.002$ \\
    No coordinate probes      & $0.772$ & $-0.094$ \\
    Mean + isotropic $\sigma^2 I$ & $0.750$ & $-0.115$ \\
    No global prior           & $0.711$ & $-0.155$ \\
    GIN mean only             & $0.682$ & $-0.183$ \\
    Mean + fixed box          & $0.657$ & $-0.209$ \\
    No local ($k$-NN) prior   & $0.645$ & $-0.221$ \\
\bottomrule
\end{tabular}
\end{table}

% Generated by code/fig03_heldout_quality.py
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{fig03_heldout_quality.pdf}
    \caption{Held-out quality check at $n{=}12$, $p{=}3$ on a disjoint
    deterministic graph set ($Q{=}18$ matched budget, same four families).
    This serves as an internal consistency test of the learned
    query-allocation mechanism under the same matched-budget protocol.
    \label{fig:ablation}}
\end{figure*}

\subsection{Experiment 4: Trust-region mechanism}
\label{sec:mechanism}

\Cref{fig:landscape_trust} provides a low-dimensional diagnostic on the
$p{=}1$ QAOA landscape for the 4-cycle graph $C_4$.  The plot illustrates the
intended mechanism---constraining probes to a productive region of parameter
space---rather than proving that the same geometry fully explains every
$p{=}3$ instance.

% Generated by code/fig04_trust_region_concept.py
\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{fig04_trust_region_concept.pdf}
    \caption{Trust-region mechanism on the $p{=}1$ landscape for the
    4-cycle graph $C_4$ ($Q{=}6$).
    This figure is schematic: it illustrates how covariance-restricted probing
    can focus evaluations in a productive region, but it is not presented as a
    complete geometric explanation of the higher-dimensional $p{=}3$ results.
    \label{fig:landscape_trust}}
\end{figure}

\subsection{Experiment 5: Higher-depth tests}
\label{sec:higher_depth}

To test whether the query policy remains operational as the parameter
dimension increases, we evaluate depths $p{=}6$ and $p{=}7$ using a
dimension-scaled budget $Q_{\min}(p)=5+4p$ truncated to $Q{=}5$ for the
single-instance smoke configuration.  \Cref{tab:higher_depth} reports the
generated summaries.  These runs confirm that the configurable-depth
implementation operates correctly and that the posterior-fusion and refinement
machinery executes without modification at $2p\in\{12,14\}$.  At this minimal
single-instance budget the methods are tightly clustered and no method
dominates; statistical claims at $p{>}3$ require larger benchmarks and are
explicitly \emph{not} made here.  The value of the experiment is to demonstrate
correct higher-dimensional operation and to expose the budget regime in which
the learned geometry has too few queries to separate from the physics-informed
prefix.

% Generated from code/results/summary_p6.csv and summary_p7.csv
\begin{table}[t]
\caption{Higher-depth single-instance smoke tests at $p{=}6$ and $p{=}7$,
$Q{=}5$ matched queries (one held-out instance per depth).  Confidence
intervals are degenerate at a single instance and are omitted; the table
documents correct configurable-depth execution rather than a statistical
comparison.
\label{tab:higher_depth}}
\centering
\scriptsize
\begin{tabular}{@{}lcc@{}}
\toprule
Method & Ratio ($p{=}6$) & Ratio ($p{=}7$) \\
\midrule
Random search            & $0.6074$ & $0.6346$ \\
TQA                      & $0.6022$ & $0.6117$ \\
TQA + refinement         & $0.6296$ & $0.6238$ \\
Posterior anchors only   & $0.6204$ & $0.6199$ \\
UQ-QAOA (full)           & $0.6204$ & $0.6199$ \\
\;\;-- no covariance     & $0.6204$ & $0.6199$ \\
\;\;-- no refinement     & $0.6204$ & $0.6199$ \\
\;\;-- no TQA prior      & $0.6296$ & $0.6257$ \\
\;\;-- no $k$-NN prior   & $0.6296$ & $0.6257$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Experiment 6: Calibration and finite-shot diagnostics}
\label{sec:calibration_finite}

\paragraph{Trust-region calibration.}
The artifact generates empirical trust-region coverage curves
(\texttt{code/figures/trust\_region\_coverage.pdf}) and an expected
calibration error (ECE) summary.  On the controlled two-instance held-out set
at $p{=}3$, the measured ECE and uncertainty--error correlation are both
$0.000$ at the reported precision, and the empirical coverage at the tested
Mahalanobis radii $\rho\in\{1.5,3,6,12,24\}$ is degenerate because the
reference parameters fall outside the very compact predicted ellipsoids on this
small diagnostic set.  We report these numbers transparently: they indicate
that calibration at the two-instance scale is uninformative and that meaningful
coverage estimation requires the larger validation pools described in
\cref{sec:limitations}.  The diagnostic tests whether the predicted covariance
is operationally meaningful, not whether it achieves perfect Bayesian coverage.

\paragraph{Finite-shot robustness.}
The finite-shot component samples objective estimates from the statevector
probability distribution for a fixed candidate and verifies deterministic
replay under fixed seeds.  \Cref{tab:finite_shots} shows the expected
$1/\sqrt{S}$ concentration of the shot estimator toward the exact objective:
as the shot count increases from $256$ to $8192$, the standard error of the TQA
objective estimate contracts from $0.1108$ to $0.0201$ and the noisy estimate
converges to the exact value $8.2921$.  Because the component does not yet
evaluate full \UQQAOA{} candidate selection under realistic device noise,
end-to-end finite-shot robustness of the selection policy remains future work;
the present result validates the estimator and the deterministic shot-replay
contract.

% Generated from code/tables/table_finite_shots.tex
\begin{table}[t]
\caption{Finite-shot estimator behaviour at $p{=}3$ for the TQA candidate.
The standard error contracts as $1/\sqrt{S}$ and the noisy estimate converges
to the exact objective $8.2921$.  The misranking rate is undefined (NaN) at a
single candidate and is reported for completeness.
\label{tab:finite_shots}}
\centering
\scriptsize
\begin{tabular}{@{}rrrr@{}}
\toprule
Shots $S$ & Noisy objective & Standard error & Exact objective \\
\midrule
$256$  & $8.4570$ & $0.1108$ & $8.2921$ \\
$512$  & $8.3887$ & $0.0769$ & $8.2921$ \\
$1024$ & $8.3330$ & $0.0565$ & $8.2921$ \\
$4096$ & $8.2759$ & $0.0283$ & $8.2921$ \\
$8192$ & $8.2863$ & $0.0201$ & $8.2921$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Experiment 7: Higher-powered benchmark on a $6\times$ larger held-out set}
\label{sec:expanded}

The controlled benchmark of \cref{tab:efficiency} uses eight held-out instances,
which yields a wide paired confidence interval. To test whether the headline
advantage survives at higher statistical power, we replicate the identical protocol
($n{=}14$, $p{=}3$, $Q{=}18$, exact statevector, four families) on a six-times larger
held-out set of $48$ instances (twelve per family). The additional test graphs use the
same deterministic seeding namespace as the original benchmark, which is disjoint from
the training-library namespace, so the larger set adds new held-out graphs without
train/test leakage. The expanded run is generated by \path{table01_expanded.py} and is
fully reproducible.

\Cref{tab:expanded} reports the result. The mean approximation ratios are essentially
unchanged from the eight-instance benchmark---\UQQAOA{} $0.862$ versus TQA $0.850$---and
the paired advantage holds at $+0.012$, but the $95\%$ bootstrap confidence interval
contracts from $[+0.003,+0.024]$ (eight instances) to $[+0.008,+0.016]$ (forty-eight
instances), roughly a threefold reduction in width that places the interval well away
from zero. \UQQAOA{} wins on $39$ of $48$ instances ($81.2\%$); the nine losses are
small and visible in \cref{fig:expanded}(b). All black-box optimizers (DE $0.715$,
BO $0.707$, CMA-ES $0.724$, multi-seed random $0.724$, Nelder--Mead $0.682$) again
remain below every transfer-based method, consistent with \cref{tab:efficiency}. This
higher-powered replication shows that, \emph{under the configuration of
\cref{tab:efficiency}}, the covariance-calibrated posterior policy extracts more value
from a fixed query budget than the TQA baseline as run here. As detailed in
\cref{sec:reassessment}, this gap is sensitive to whether the TQA baseline itself
receives greedy coordinate refinement, and should not be read as a state-of-the-art
result.

% Generated by code/table01_expanded.py
\begin{table}[t]
\caption{Higher-powered replication of the primary benchmark on $48$ held-out
instances (twelve per family) at $n{=}14$, $p{=}3$, $Q{=}18$. Confidence intervals are
$10{,}000$-resample bootstraps over the $48$ instances; $\Delta$ is the paired mean
versus TQA. The advantage of \cref{tab:efficiency} is preserved with a substantially
tighter interval.
\label{tab:expanded}}
\scriptsize
\begin{tabular}{@{}lcccc@{}}
\toprule
    Method & Ratio $\uparrow$ & CI$_{95}$ & $\Delta$ vs.\ TQA & $Q$ \\
    \midrule
        Random           & $0.733$ & $[0.718,\,0.748]$ & $-0.117$ & $18$ \\
        Heuristic        & $0.609$ & $[0.591,\,0.627]$ & $-0.241$ & $18$ \\
        k-NN             & $0.636$ & $[0.621,\,0.651]$ & $-0.215$ & $18$ \\
        TQA              & $0.850$ & $[0.841,\,0.860]$ & $+0.000$ & $18$ \\
        GNN point        & $0.635$ & $[0.617,\,0.654]$ & $-0.215$ & $18$ \\
    \UQQAOA{} (ours)
    & $\mathbf{0.862}$ & $[0.854,\,0.871]$ & $\mathbf{+0.012}$ & $18$ \\
\bottomrule
\end{tabular}
\end{table}

% Generated by code/fig05_expanded_benchmark.py
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{fig05_expanded_benchmark.pdf}
    \caption{Higher-powered benchmark on $48$ held-out instances
    ($n{=}14$, $p{=}3$, $Q{=}18$). \textbf{(a)}~Mean approximation ratio with $95\%$
    bootstrap confidence intervals for all methods, ranked; \UQQAOA{} ($\bigstar$) is
    highest at $0.862$, above the strongest baseline TQA at $0.850$. \textbf{(b)}~The
    per-instance paired advantage $\UQQAOA{}-\mathrm{TQA}$ across all $48$ instances,
    sorted; the dashed line marks the bootstrap mean $+0.0120$ and the shaded band its
    $95\%$ CI $[+0.008,+0.016]$. The advantage is positive on $39$ of $48$ instances.
    \label{fig:expanded}}
\end{figure*}

\subsection{Summary of empirical evidence}

\begin{enumerate}
    \item \textbf{Fixed-budget query allocation:} \UQQAOA{} achieves the
    highest mean best-of-budget ratio in the controlled trace-level comparison
    ($0.865$, CI $[0.839,0.892]$) vs.\ TQA ($0.853$) under matched
    $Q{=}18$ (\cref{tab:efficiency}).

    \item \textbf{Higher-powered replication:} On a $6\times$ larger held-out set
    of $48$ instances, the advantage holds at $+0.012$ with a threefold tighter
    bootstrap CI $[+0.008,+0.016]$ and a $39/48$ win rate
    (\cref{tab:expanded,fig:expanded}).

    \item \textbf{Query curves:} The proposed curve is best-or-tied-best at
    every tested query budget and strictly highest after the learned
    trust-region refinement begins (\cref{fig:query_curve}).

    \item \textbf{Cross-size check:} In the generated cross-size diagnostic,
    \UQQAOA{} is competitive across the tested sizes
    $n\in\{8,10,12,14\}$ (\cref{fig:statevector_grid}).

    \item \textbf{Ablation:} Removing covariance, individual priors, or
    refinement degrades performance; the largest isolated loss comes from
    the $k$-NN prior (\cref{tab:ablation}).

    \item \textbf{Held-out confirmation:} On a separately tagged graph set,
    \UQQAOA{} again achieves the highest mean ratio among the included methods
    (\cref{fig:ablation}).

    \item \textbf{Mechanism:} The trust region concentrates evaluations in
    the productive landscape region (\cref{fig:landscape_trust}).
\end{enumerate}

% ============================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================

\subsection{Reassessment: the advantage is configuration-dependent}
\label{sec:reassessment}

The benchmarks above grant UQ-QAOA a greedy coordinate-refinement phase
(Algorithm~\ref{alg:gctr2}, Phase~2c) that the headline TQA curve does not
receive: the reported TQA baseline evaluates the linear-ramp schedule and its
anchors but is not given an equal coordinate-refinement budget around its
incumbent. This is an unequal-protocol comparison, and the $+0.012$ figure
reported in \cref{tab:efficiency,tab:expanded} should be read accordingly.
When the same coordinate-refinement procedure is applied to the TQA incumbent
under a matched budget, the ordering reverses: in the controlled
single-instance ablation generated by our own pipeline
(\path{table_ablation_p3.csv}, row \texttt{tqa\_refine}), refined TQA reaches a
higher approximation ratio than the full UQ-QAOA policy on the tested instance.
In other words, much of the apparent gain reflects the refinement phase rather
than the learned covariance geometry per se, and a fair comparison that equalizes
refinement does not show a UQ-QAOA win. We therefore do not claim a
state-of-the-art result; UQ-QAOA is presented as a method study of how
graph-conditioned predictive uncertainty can be turned into operational search
geometry, and the negative finding---that an equally refined physics-informed
baseline is competitive or better---is itself an honest, reproducible outcome.
A full equal-refinement sweep across all families and depths is the natural
next experiment and is not claimed here.

\subsection{Origin of evaluation savings}

The ablation table (\cref{tab:ablation}) shows that the largest isolated gain
comes from the local $k$-NN prior (removing it loses $0.221$).  The
remaining gain is distributed across the four-source posterior fusion,
dimension-wise step sizes from the calibrated covariance, and sequential
refinement.  The savings arise from the full distributional mechanism rather
than the center alone.

The ``Without sequential refinement'' variant achieves $0.863$---already
$+0.010$ above TQA ($0.853$)---suggesting that the improvement is not solely a
consequence of the final greedy pass.  The regenerated query curve shows that
the TQA safety prefix accounts for the early-budget tie, while informed
covariance-scaled coordinate selection and multi-source posterior fusion
produce the later-budget separation.
``GNN point'' ($0.643$, \cref{tab:efficiency}) shows that the learned mean
alone is insufficient; the observed gain arises from four-source posterior
fusion with uncertainty-calibrated step sizes.

\subsection{Interpretation as query allocation}

In low-depth QAOA the dominant cost is circuit evaluations.  In the primary
matched-budget comparison, every method receives the same $Q{=}18$ objective
queries, so the demonstrated effect is better allocation of a fixed budget, not
fewer evaluations.  The variance head serves two purposes: it defines the
admissible region and sets the amount of refinement.  Small predicted variance
produces a tight search and short optimization; large predicted variance
produces broader search and a larger adaptive budget when the adaptive-budget
mode is used.  The method is not intended to be the best absolute-quality
optimizer under unrestricted budgets; its contribution is a candidate policy
that extracts more value from a constrained number of QAOA objective queries.

\subsection{Why the backend contract matters}

QAOA parameter search is naturally a batched workload: many independent
graph--angle pairs must be evaluated under a fixed budget.  A
production-quality implementation can exploit CPU threads, vectorization, and
eventually accelerator kernels---but only if the numerical contract is
explicit.  The backend contract (\cref{tab:backend_contract}) fixes candidate
generation order, floating-point merge order, strict-inequality tie-breaking,
and seed replay so that optimizer decisions are not confounded by
implementation-level nondeterminism.

\subsection{Relation to classical trust-region methods}

\UQQAOA{} differs from classical trust-region methods~\cite{conn2000} in
that the region is predicted from the graph before optimization, not updated
from local second-order information during optimization.  Learning supplies a
center, a metric, and a budget prior to refinement---it does not replace it.

\subsection{Scope and limitations}
\label{sec:limitations}

The controlled experiments use exact statevector simulation with $n \le 14$
and depth $p{=}3$.  Higher-depth $p{=}4$--$7$ runs are included as smoke
tests under dimension-scaled query budgets.

\paragraph{Scale.}
The benchmark uses $n{=}14$ ($2p{=}6$ parameters, 8 test instances).
Generalization to $n > 20$, weighted graphs, or non-MaxCut objectives
remains untested.

\paragraph{Finite-shot and hardware noise.}
The finite-shot experiment (\cref{sec:calibration_finite}) uses ideal
statevector-derived shot samples and does not model gate errors, readout
asymmetry, decoherence, or crosstalk.

\paragraph{Learned prior fragility.}
The method depends on the quality of the GIN predictor and the $k$-NN
neighbourhood.  Failure cases include overconfident miscoverage,
distribution shift from structurally novel test graphs, missed cross-layer
correlations at deeper QAOA, and shot-noise masking of small ratio
differences at larger $n$.  Removing the $k$-NN prior drops performance to
$0.645$ (\cref{tab:ablation}), showing reliance on the nonparametric
component.

\paragraph{Diagonal covariance.}
The covariance is diagonal throughout.  Cross-layer QAOA angle
correlations~\cite{zhou2020} exist and a full-covariance extension is
a natural direction for future work.

\paragraph{No hardware validation.}
All evidence is from classical simulation.  The evaluator-contract design
(\cref{tab:backend_contract}) is intended to facilitate hardware integration
but has not been validated on quantum hardware.

\subsection{Significance for machine learning and quantum technology}

Query-efficient QAOA parameter selection is both a machine-learning and a
quantum-software problem:
measurement budgets and classical outer-loop overhead are direct barriers to
deploying variational algorithms on near-term
hardware~\cite{cerezo2021,mcclean2018}.  This work contributes a
mechanism---graph-conditioned uncertainty as search geometry---that turns
learned structure into resource allocation decisions.  The backend-agnostic
design means the same policy applies to statevector simulation, finite-shot
emulation, or quantum hardware.

\paragraph{Future work.}
Quantum hardware execution, larger paired benchmarks, weighted instances,
full-covariance extensions, richer device-noise models, and torus-aware
parameterizations remain open.  Optimal transport
metrics~\cite{villani2009,peyre2019,dowson1982,cuturi2013,frogner2015,arjovsky2017}
offer a principled alternative to the coordinate-wise Gaussian NLL for
comparing predicted and empirical parameter distributions.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

We have presented UQ-QAOA, a framework that converts graph-conditioned
predictive uncertainty into an operational trust-region geometry for
query-efficient QAOA parameter search.  A precision-weighted posterior fuses
four information sources and determines the anisotropic step sizes for
sequential coordinate refinement.

On a controlled exact-statevector MaxCut benchmark ($n{=}14$, $p{=}3$,
$Q{=}18$), UQ-QAOA achieves $0.865$ mean approximation ratio versus $0.853$
for the TQA baseline as configured (paired difference $+0.012$, bootstrap
95\,\% CI $[+0.003, +0.024]$, win rate $7/8$), reproduced on a six-times larger
held-out replication ($48$ instances, $[+0.008, +0.016]$, $39/48$). We
emphasize that this difference is configuration-dependent and not a
state-of-the-art claim: as shown in \cref{sec:reassessment}, granting the TQA
baseline the same greedy coordinate-refinement budget reverses the ordering, so
the contribution is a method study of uncertainty-as-search-geometry together
with the honest negative finding that an equally refined physics-informed
baseline is competitive or better. The ablation characterizes which components
drive the policy's behavior under the configuration tested.

The accompanying code artifact regenerates all reported results
deterministically.  A C++20 reference interface defines the kernel boundaries
and reproducibility contract for future optimized backends.  Higher-depth
$p{=}4$--$7$ smoke tests confirm correct configurable-depth operation;
quantum hardware execution and finite-shot robustness under device noise
remain future work.

\section*{Data availability}

All data supporting this study are generated deterministically by the
accompanying code artifact, publicly available at
\href{https://github.com/thmolena/Hybrid-Quantum-Graph-AI-QAOA-GNN-Biomedical-Optimization/tree/main/submission}{github.com/thmolena/\ldots/submission}.
The repository includes graph seeds, train/test splits, raw query traces,
figure source data, and one-command reproduction via
\texttt{bash code/reproduce.sh smoke}.
A DOI-bearing archival release will be created before final publication.

\section*{Code availability}

The complete source code that generates every figure, table, and number in
this manuscript is openly available in the same repository under
\texttt{submission/code/}.  It comprises a modular Python package
(\texttt{python/uq\_qaoa/}), a C++20 reference statevector backend
(\texttt{cpp/}), per-experiment entry-point scripts, YAML configurations for
depths $p{=}3$--$7$, a unit-test suite (\texttt{tests/}), and the
end-to-end drivers \texttt{generate\_all.py} and \texttt{reproduce.sh}.
The software is released under the licence stated in the repository and is
versioned to the commit used to produce this manuscript.

\section*{Author contributions}

M.H.\ conceived the study, developed the methodology and theory, implemented
the Python and C++ software, designed and executed all experiments, analysed
the results, and wrote the manuscript.

\section*{Competing interests}

The author declares no competing interests.

\section*{References}
\begin{thebibliography}{95}
\providecommand{\natexlab}[1]{#1}
\providecommand{\url}[1]{\texttt{#1}}
\expandafter\ifx\csname urlstyle\endcsname\relax
  \providecommand{\doi}[1]{doi: #1}\else
  \providecommand{\doi}{doi: \begingroup \urlstyle{rm}\Url}\fi

\bibitem[Farhi et~al.(2014)Farhi, Goldstone, and Gutmann]{farhi2014}
Edward Farhi, Jeffrey Goldstone, and Sam Gutmann.
\newblock A quantum approximate optimization algorithm.
\newblock \emph{arXiv preprint arXiv:1411.4028}, 2014.
\newblock \doi{10.48550/arxiv.1411.4028}.

\bibitem[Preskill(2018)]{preskill2018}
John Preskill.
\newblock Quantum computing in the {NISQ} era and beyond.
\newblock \emph{Quantum}, 2:\penalty0 79, 2018.
\newblock \doi{10.22331/q-2018-08-06-79}.

\bibitem[Crooks(2018)]{crooks2018}
Gavin~E. Crooks.
\newblock Performance of the quantum approximate optimization algorithm on the
  maximum cut problem.
\newblock \emph{arXiv preprint arXiv:1811.08419}, 2018.
\newblock \doi{10.48550/arxiv.1811.08419}.

\bibitem[Zhou et~al.(2020)Zhou, Wang, Choi, Pichler, and Lukin]{zhou2020}
Leo Zhou, Sheng-Tao Wang, Soonwon Choi, Hannes Pichler, and Mikhail~D Lukin.
\newblock Quantum approximate optimization algorithm: Performance, mechanism,
  and implementation on near-term devices.
\newblock \emph{Physical Review X}, 10\penalty0 (2):\penalty0 021067, 2020.
\newblock \doi{10.1103/physrevx.10.021067}.

\bibitem[Wurtz and Love(2021)]{wurtz2021}
Jonathan Wurtz and Peter Love.
\newblock Fixed-angle conjectures for the quantum approximate optimization
  algorithm on regular {MaxCut} graphs.
\newblock \emph{Physical Review A}, 104\penalty0 (5):\penalty0 052419, 2021.
\newblock \doi{10.1103/physreva.104.052419}.

\bibitem[Cerezo et~al.(2021)Cerezo, Arrasmith, Babbush, Benjamin, Endo, Fujii,
  McClean, Mitarai, Yuan, Cincio, and Coles]{cerezo2021}
M.~Cerezo, Andrew Arrasmith, Ryan Babbush, Simon~C. Benjamin, Suguru Endo,
  Keisuke Fujii, Jarrod~R. McClean, Kosuke Mitarai, Xiao Yuan, Lukasz Cincio,
  and Patrick~J. Coles.
\newblock Variational quantum algorithms.
\newblock \emph{Nature Reviews Physics}, 3\penalty0 (9):\penalty0 625--644,
  2021.
\newblock \doi{10.1038/s42254-021-00348-9}.

\bibitem[McClean et~al.(2018)McClean, Boixo, Smelyanskiy, Babbush, and
  Neven]{mcclean2018}
Jarrod~R. McClean, Sergio Boixo, Vadim~N. Smelyanskiy, Ryan Babbush, and
  Hartmut Neven.
\newblock Barren plateaus in quantum neural network training landscapes.
\newblock \emph{Nature Communications}, 9:\penalty0 4812, 2018.
\newblock \doi{10.1038/s41467-018-07090-4}.

\bibitem[Arrasmith et~al.(2021)Arrasmith, Cerezo, Czarnik, Cincio, and
  Coles]{arrasmith2021}
Andrew Arrasmith, M.~Cerezo, Piotr Czarnik, Lukasz Cincio, and Patrick~J.
  Coles.
\newblock Effect of barren plateaus on gradient-free optimization.
\newblock \emph{Quantum}, 5:\penalty0 558, 2021.
\newblock \doi{10.22331/q-2021-10-05-558}.

\bibitem[Larkin et~al.(2022)Larkin, Jonsson, Justice, and
  Guerreschi]{larkin2022qst}
Jason Larkin, Mat{\'i}as Jonsson, Daniel Justice, and Gian~Giacomo Guerreschi.
\newblock Evaluation of {QAOA} based on the approximation ratio of individual
  samples.
\newblock \emph{Quantum Science and Technology}, 7\penalty0 (4):\penalty0
  045014, 2022.
\newblock \doi{10.1088/2058-9565/ac6973}.

\bibitem[Streif and Leib(2020)]{streif2020}
Michael Streif and Martin Leib.
\newblock Training the quantum approximate optimization algorithm without
  access to a quantum processing unit.
\newblock \emph{Quantum Science and Technology}, 5\penalty0 (3):\penalty0
  034008, 2020.
\newblock \doi{10.1088/2058-9565/ab8c2b}.

\bibitem[Moussa et~al.(2020)Moussa, Calandra, and Dunjko]{moussa2020qst}
Charles Moussa, Henri Calandra, and Vedran Dunjko.
\newblock To quantum or not to quantum: towards algorithm selection in
  near-term quantum optimization.
\newblock \emph{Quantum Science and Technology}, 5\penalty0 (4):\penalty0
  044009, 2020.
\newblock \doi{10.1088/2058-9565/abb8e5}.

\bibitem[Zhu et~al.(2023)Zhu, Zhang, Sundar, Green, Alderete, Nguyen, Hazzard,
  and Linke]{zhu2023qst}
Yingyue Zhu, Zewen Zhang, Bhuvanesh Sundar, Alaina~M. Green, C.~Huerta
  Alderete, Nhung~H. Nguyen, Kaden R.~A. Hazzard, and Norbert~M. Linke.
\newblock Multi-round {QAOA} and advanced mixers on a trapped-ion quantum
  computer.
\newblock \emph{Quantum Science and Technology}, 8\penalty0 (1):\penalty0
  015007, 2023.
\newblock \doi{10.1088/2058-9565/ac91ef}.

\bibitem[Bechtold et~al.(2023)Bechtold, Barzen, Leymann, Mandl, Obst, Truger,
  and Weder]{bechtold2023qst}
Marvin Bechtold, Johanna Barzen, Frank Leymann, Alexander Mandl, Julian Obst,
  Felix Truger, and Benjamin Weder.
\newblock Investigating the effect of circuit cutting in {QAOA} for the
  {MaxCut} problem on {NISQ} devices.
\newblock \emph{Quantum Science and Technology}, 8\penalty0 (4):\penalty0
  045022, 2023.
\newblock \doi{10.1088/2058-9565/acf59c}.

\bibitem[Vijendran et~al.(2024)Vijendran, Das, Koh, Assad, and
  Lam]{vijendran2024qst}
V.~Vijendran, Aritra Das, Dax~Enshan Koh, Syed~M. Assad, and Ping~Koy Lam.
\newblock An expressive ansatz for low-depth quantum approximate optimisation.
\newblock \emph{Quantum Science and Technology}, 9\penalty0 (2):\penalty0
  025010, 2024.
\newblock \doi{10.1088/2058-9565/ad200a}.

\bibitem[Ozaeta et~al.(2022)Ozaeta, van Dam, and McMahon]{ozaeta2022qst}
Asier Ozaeta, Wim van Dam, and Peter~L. McMahon.
\newblock Expectation values from the single-layer quantum approximate
  optimization algorithm on {Ising} problems.
\newblock \emph{Quantum Science and Technology}, 7\penalty0 (4):\penalty0
  045036, 2022.
\newblock \doi{10.1088/2058-9565/ac9013}.

\bibitem[Zhang et~al.(2025)Zhang, Paredes, Sundar, Quiroga, Kyrillidis,
  Duenas-Osorio, Pagano, and Hazzard]{zhang2025qst}
Zewen Zhang, Roger Paredes, Bhuvanesh Sundar, David Quiroga, Anastasios
  Kyrillidis, Leonardo Duenas-Osorio, Guido Pagano, and Kaden R.~A. Hazzard.
\newblock Grover-{QAOA} for 3-{SAT}: quadratic speedup, fair-sampling, and
  parameter clustering.
\newblock \emph{Quantum Science and Technology}, 10\penalty0 (1):\penalty0
  015022, 2025.
\newblock \doi{10.1088/2058-9565/ad895c}.

\bibitem[Drapeau et~al.(2026)Drapeau, Banerjee, and Kourtis]{drapeau2026qst}
Julien Drapeau, Shreya Banerjee, and Stefanos Kourtis.
\newblock Counting with the quantum alternating operator ansatz.
\newblock \emph{Quantum Science and Technology}, 11\penalty0 (2):\penalty0
  025037, 2026.
\newblock \doi{10.1088/2058-9565/ae57d6}.

\bibitem[Shaydulin et~al.(2024)Shaydulin, Li, Chakrabarti, DeCross, Herman,
  Kumar, Larson, Lykov, Minssen, Sun, et~al.]{shaydulin2024}
Ruslan Shaydulin, Changhao Li, Shouvanik Chakrabarti, Matthew DeCross, Dylan
  Herman, Niraj Kumar, Jeffrey Larson, Danylo Lykov, Pierre Minssen, Yue Sun,
  et~al.
\newblock Evidence of scaling advantage for the quantum approximate
  optimization algorithm on a classically intractable problem.
\newblock \emph{Science Advances}, 10\penalty0 (22):\penalty0 eadm6761, 2024.
\newblock \doi{10.1126/sciadv.adm6761}.

\bibitem[Bonet-Monroig et~al.(2023)Bonet-Monroig, Wang, Vermetten, Senjean,
  Moussa, B{\"a}ck, Dunjko, and O'Brien]{bonet2023}
Xavier Bonet-Monroig, Hao Wang, Diederick Vermetten, Bruno Senjean, Charles
  Moussa, Thomas B{\"a}ck, Vedran Dunjko, and Thomas~E. O'Brien.
\newblock Performance comparison of optimization methods on variational quantum
  algorithms.
\newblock \emph{Physical Review A}, 107\penalty0 (3):\penalty0 032407, 2023.
\newblock \doi{10.1103/physreva.107.032407}.

\bibitem[Wang et~al.(2021)Wang, Fontana, Cerezo, Sharma, Sone, Cincio, and
  Coles]{wang2021noise}
Samson Wang, Enrico Fontana, M.~Cerezo, Kunal Sharma, Akira Sone, Lukasz
  Cincio, and Patrick~J. Coles.
\newblock Noise-induced barren plateaus in variational quantum algorithms.
\newblock \emph{Nature Communications}, 12:\penalty0 6961, 2021.
\newblock \doi{10.1038/s41467-021-27045-6}.

\bibitem[Bravyi et~al.(2020)Bravyi, Kliesch, Koenig, and Tang]{bravyi2020}
Sergey Bravyi, Alexander Kliesch, Robert Koenig, and Eugene Tang.
\newblock Obstacles to variational quantum optimization from symmetry
  protection.
\newblock \emph{Physical Review Letters}, 125\penalty0 (26):\penalty0 260505,
  2020.
\newblock \doi{10.1103/physrevlett.125.260505}.

\bibitem[Jain et~al.(2022)Jain, Coyle, Kashefi, and Kumar]{jain2022}
Nishant Jain, Brian Coyle, Elham Kashefi, and Niraj Kumar.
\newblock Graph neural network initialisation of quantum approximate
  optimisation.
\newblock \emph{Quantum}, 6:\penalty0 861, 2022.
\newblock \doi{10.22331/q-2022-11-17-861}.

\bibitem[Liang et~al.(2024)Liang, Cheng, Yang, Ren, Qin, Fan, and
  Jiang]{liang2024}
Zhiding Liang, Jinglei Cheng, Rui Yang, Hang Ren, Zhixin Qin, Deliang Fan, and
  Weiwen Jiang.
\newblock Graph neural network-based prediction of {QAOA} parameters for
  combinatorial optimization.
\newblock \emph{Quantum Science and Technology}, 9\penalty0 (3):\penalty0
  035031, 2024.
\newblock \doi{10.1088/2058-9565/ad4f4e}.

\bibitem[Shaydulin and Wild(2023)]{shaydulin2023}
Ruslan Shaydulin and Stefan~M. Wild.
\newblock On the importance of classical preprocessing in {QAOA}.
\newblock \emph{arXiv preprint arXiv:2306.16979}, 2023.
\newblock \doi{10.48550/arxiv.2306.16979}.

\bibitem[Lavrijsen et~al.(2020)Lavrijsen, Tudor, M{\"u}ller, Iancu, and
  de~Jong]{lavrijsen2020}
Wim Lavrijsen, Ana Tudor, Juliane M{\"u}ller, Costin Iancu, and Wibe de~Jong.
\newblock Classical optimizers for noisy intermediate-scale quantum devices.
\newblock \emph{arXiv preprint arXiv:2004.10892}, 2020.
\newblock \doi{10.48550/arxiv.2004.10892}.

\bibitem[Conn et~al.(2000)Conn, Gould, and Toint]{conn2000}
Andrew~R. Conn, Nicholas I.~M. Gould, and Philippe~L. Toint.
\newblock \emph{Trust Region Methods}.
\newblock SIAM, 2000.
\newblock \doi{10.1137/1.9780898719857}.

\bibitem[Nocedal and Wright(2006)]{nocedal2006}
Jorge Nocedal and Stephen~J. Wright.
\newblock \emph{Numerical Optimization}.
\newblock Springer, 2nd edition, 2006.
\newblock \doi{10.1007/978-0-387-40065-5}.

\bibitem[Tibaldi et~al.(2023)Tibaldi, Vodola, Tignone, and
  Ercolessi]{tibaldi2023}
Simone Tibaldi, Davide Vodola, Edoardo Tignone, and Elisa Ercolessi.
\newblock Bayesian optimization for {QAOA}.
\newblock \emph{IEEE Transactions on Quantum Engineering}, 4:\penalty0 1--14,
  2023.
\newblock \doi{10.1109/tqe.2023.3325167}.

\bibitem[Cheng et~al.(2024)Cheng, Liang, Ren, Qin, Song, and Jiang]{cheng2024}
Jinglei Cheng, Zhiding Liang, Hang Ren, Zhixin Qin, Huan Song, and Weiwen
  Jiang.
\newblock {DARBO}: {Derivative}-aware {QAOA} with {Bayesian} optimization.
\newblock \emph{arXiv preprint arXiv:2404.09345}, 2024.
\newblock \doi{10.48550/arxiv.2404.09345}.

\bibitem[Brand{\~a}o et~al.(2018)Brand{\~a}o, Broughton, Farhi, Gutmann, and
  Napp]{brandao2018}
Fernando G. S.~L. Brand{\~a}o, Michael Broughton, Edward Farhi, Sam Gutmann,
  and Hartford Napp.
\newblock For fixed control parameters the quantum approximate optimization
  algorithm's objective function value concentrates for typical instances.
\newblock \emph{arXiv preprint arXiv:1812.04170}, 2018.
\newblock \doi{10.48550/arxiv.1812.04170}.

\bibitem[Akshay et~al.(2021)Akshay, Rabinovich, Campos, and
  Biamonte]{akshay2021}
V.~Akshay, D.~Rabinovich, E.~Campos, and J.~Biamonte.
\newblock Parameter concentrations in quantum approximate optimization.
\newblock \emph{Physical Review A}, 104\penalty0 (1):\penalty0 L010401, 2021.
\newblock \doi{10.1103/physreva.104.l010401}.

\bibitem[Galda et~al.(2021)Galda, Liu, Lykov, Alexeev, and Safro]{galda2021}
Alexey Galda, Xiaoyuan Liu, Danylo Lykov, Yuri Alexeev, and Ilya Safro.
\newblock Transferability of optimal {QAOA} parameters between random graphs.
\newblock In \emph{2021 IEEE International Conference on Quantum Computing and
  Engineering (QCE)}, pages 171--180. IEEE, 2021.
\newblock \doi{10.1109/qce52317.2021.00034}.

\bibitem[Sack and Serbyn(2021)]{sack2021}
Stefan~H. Sack and Maksym Serbyn.
\newblock Quantum annealing initialization of the quantum approximate
  optimization algorithm.
\newblock \emph{Quantum}, 5:\penalty0 491, 2021.
\newblock \doi{10.22331/q-2021-07-01-491}.

\bibitem[Lee et~al.(2021)Lee, Lee, Kim, Kim, and Kwon]{lee2021}
Juneseo Lee, Dohun Lee, Jungwoo Kim, Hyukjoon Kim, and Soonchil Kwon.
\newblock Progress toward favorable landscapes in quantum combinatorial
  optimization.
\newblock \emph{Physical Review A}, 104\penalty0 (3):\penalty0 032401, 2021.
\newblock \doi{10.1103/physreva.104.032401}.

\bibitem[Xu et~al.(2019)Xu, Hu, Leskovec, and Jegelka]{xu2019}
Keyulu Xu, Weihua Hu, Jure Leskovec, and Stefanie Jegelka.
\newblock How powerful are graph neural networks?
\newblock In \emph{International Conference on Learning Representations}, 2019.

\bibitem[Weisfeiler and Leman(1968)]{weisfeiler1968}
Boris Weisfeiler and Andrei Leman.
\newblock The reduction of a graph to canonical form and the algebra which
  appears therein.
\newblock \emph{NTI, Series 2}, 9:\penalty0 12--16, 1968.

\bibitem[Dwivedi et~al.(2023)Dwivedi, Joshi, Luo, Laurent, Bengio, and
  Bresson]{dwivedi2023}
Vijay~Prakash Dwivedi, Chaitanya~K. Joshi, Anh~Tuan Luo, Thomas Laurent, Yoshua
  Bengio, and Xavier Bresson.
\newblock Benchmarking graph neural networks.
\newblock \emph{Journal of Machine Learning Research}, 24\penalty0
  (43):\penalty0 1--48, 2023.

\bibitem[Lim et~al.(2023)Lim, Robinson, Zhao, Smidt, Sra, Maron, and
  Jegelka]{lim2023}
Derek Lim, Joshua Robinson, Lingxiao Zhao, Tess Smidt, Suvrit Sra, Haggai
  Maron, and Stefanie Jegelka.
\newblock Sign and basis invariant networks for spectral graph representation
  learning.
\newblock In \emph{International Conference on Learning Representations}, 2023.

\bibitem[Kreuzer et~al.(2021)Kreuzer, Beaini, Hamilton, L{\'e}tourneau, and
  Tossou]{kreuzer2021}
Devin Kreuzer, Dominique Beaini, William~L. Hamilton, Vincent L{\'e}tourneau,
  and Prudencio Tossou.
\newblock Rethinking graph transformers with spectral attention.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~34, pages 21618--21629, 2021.

\bibitem[Lotshaw et~al.(2021)Lotshaw, Humble, Herrman, Ostrowski, and
  Siopsis]{lotshaw2021}
Phillip~C. Lotshaw, Travis~S. Humble, Rebekah Herrman, James Ostrowski, and
  George Siopsis.
\newblock Empirical performance bounds for quantum approximate optimization.
\newblock \emph{Quantum Information Processing}, 20:\penalty0 403, 2021.
\newblock \doi{10.1007/s11128-021-03342-3}.

\bibitem[Moussa et~al.(2022)Moussa, Calandra, and Dunjko]{moussa2022}
Cezar Moussa, Hector Calandra, and Vedran Dunjko.
\newblock Unsupervised strategies for identifying optimal parameters in quantum
  approximate optimization algorithm.
\newblock \emph{EPJ Quantum Technology}, 9:\penalty0 11, 2022.
\newblock \doi{10.1140/epjqt/s40507-022-00131-4}.

\bibitem[Wurtz and Love(2022)]{wurtz2022}
Jonathan Wurtz and Peter~J Love.
\newblock Counterdiabaticity and the quantum approximate optimization
  algorithm.
\newblock \emph{Quantum}, 6:\penalty0 635, 2022.
\newblock \doi{10.22331/q-2022-01-27-635}.

\bibitem[Egger et~al.(2021)Egger, Mare{\v{c}}ek, and Woerner]{egger2021}
Daniel~J. Egger, Jakub Mare{\v{c}}ek, and Stefan Woerner.
\newblock Warm-starting quantum optimization.
\newblock \emph{Quantum}, 5:\penalty0 479, 2021.
\newblock \doi{10.22331/q-2021-06-17-479}.

\bibitem[Sauvage et~al.(2024)Sauvage, Sim, Kunitsa, Simon, Mauri, and
  Perdomo-Ortiz]{sauvage2024}
Fr{\'e}d{\'e}ric Sauvage, Sukin Sim, Alexander~A. Kunitsa, William~A. Simon,
  Marta Mauri, and Alejandro Perdomo-Ortiz.
\newblock Building spatial symmetries into parameterized quantum circuits for
  faster training.
\newblock \emph{Quantum Science and Technology}, 9\penalty0 (1):\penalty0
  015029, 2024.
\newblock \doi{10.1088/2058-9565/ad152e}.

\bibitem[Skolik et~al.(2023)Skolik, Cattelan, Yarkoni, B{\"a}ck, and
  Dunjko]{skolik2023}
Andrea Skolik, Michele Cattelan, Sheir Yarkoni, Thomas B{\"a}ck, and Vedran
  Dunjko.
\newblock Equivariant quantum circuits for learning on weighted graphs.
\newblock \emph{npj Quantum Information}, 9:\penalty0 47, 2023.
\newblock \doi{10.1038/s41534-023-00710-y}.

\bibitem[Amosy et~al.(2022)Amosy, Danzig, Porat, Makmal, and Hadar]{amosy2022}
Ohad Amosy, Tamuz Danzig, Ely Porat, Adi Makmal, and Ofer Hadar.
\newblock Iterative-free quantum approximate optimization algorithm using
  neural networks.
\newblock \emph{arXiv preprint arXiv:2208.09888}, 2022.
\newblock \doi{10.48550/arxiv.2208.09888}.

\bibitem[Khairy et~al.(2020)Khairy, Shaydulin, Cincio, Alexeev, and
  Balaprakash]{khairy2020}
Sami Khairy, Ruslan Shaydulin, Lukasz Cincio, Yuri Alexeev, and Prasanna
  Balaprakash.
\newblock Learning to optimize variational quantum circuits to solve
  combinatorial problems.
\newblock In \emph{Proceedings of the AAAI Conference on Artificial
  Intelligence}, volume~34, pages 2367--2375, 2020.
\newblock \doi{10.1609/aaai.v34i03.5616}.

\bibitem[Khalil et~al.(2017)Khalil, Dai, Zhang, Dilkina, and Song]{khalil2017}
Elias~B. Khalil, Hanjun Dai, Yuyu Zhang, Bistra Dilkina, and Le~Song.
\newblock Learning combinatorial optimization algorithms over graphs.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~30, 2017.

\bibitem[Bengio et~al.(2021)Bengio, Lodi, and Prouvost]{bengio2021}
Yoshua Bengio, Andrea Lodi, and Antoine Prouvost.
\newblock Machine learning for combinatorial optimization: a methodological
  tour d'horizon.
\newblock \emph{European Journal of Operational Research}, 290\penalty0
  (2):\penalty0 405--421, 2021.
\newblock \doi{10.1016/j.ejor.2020.07.063}.

\bibitem[Cappart et~al.(2023)Cappart, Ch{\'e}telat, Khalil, Lodi, Morris, and
  Veli{\v{c}}kovi{\'c}]{cappart2023}
Quentin Cappart, Didier Ch{\'e}telat, Elias~B. Khalil, Andrea Lodi, Christopher
  Morris, and Petar Veli{\v{c}}kovi{\'c}.
\newblock Combinatorial optimization and reasoning with graph neural networks.
\newblock \emph{Journal of Machine Learning Research}, 24\penalty0
  (130):\penalty0 1--61, 2023.

\bibitem[Schuetz et~al.(2022)Schuetz, Brubaker, and Katzgraber]{schuetz2022}
Martin J.~A. Schuetz, J.~Kyle Brubaker, and Helmut~G. Katzgraber.
\newblock Combinatorial optimization with physics-inspired graph neural
  networks.
\newblock \emph{Nature Machine Intelligence}, 4:\penalty0 367--377, 2022.
\newblock \doi{10.1038/s42256-022-00468-6}.

\bibitem[Kool et~al.(2019)Kool, van Hoof, and Welling]{kool2019}
Wouter Kool, Herke van Hoof, and Max Welling.
\newblock Attention, learn to solve routing problems!
\newblock In \emph{International Conference on Learning Representations}, 2019.

\bibitem[Amos(2023)]{amos2023}
Brandon Amos.
\newblock Tutorial on amortized optimization.
\newblock \emph{Foundations and Trends in Machine Learning}, 16\penalty0
  (5):\penalty0 592--732, 2023.
\newblock \doi{10.1561/2200000102}.

\bibitem[Shu et~al.(2022)Shu, Xu, Oh, and Swersky]{shu2022}
Rui Shu, Hao~H. Xu, Tae-Hyun Oh, and Kevin Swersky.
\newblock Amortized implicit differentiation for stochastic bilevel
  optimization.
\newblock In \emph{International Conference on Learning Representations}, 2022.

\bibitem[Chen et~al.(2022)Chen, Chen, Chen, Heaton, Liu, Wang, and
  Yin]{chen2022}
Tianlong Chen, Xiaohan Chen, Wuyang Chen, Howard Heaton, Jialin Liu, Zhangyang
  Wang, and Wotao Yin.
\newblock Learning to optimize: A primer and a benchmark.
\newblock \emph{Journal of Machine Learning Research}, 23\penalty0
  (189):\penalty0 1--59, 2022.

\bibitem[Finn et~al.(2017)Finn, Abbeel, and Levine]{finn2017}
Chelsea Finn, Pieter Abbeel, and Sergey Levine.
\newblock Model-agnostic meta-learning for fast adaptation of deep networks.
\newblock In \emph{International Conference on Machine Learning}, pages
  1126--1135. PMLR, 2017.

\bibitem[Nelder and Mead(1965)]{nelder1965}
J.~A. Nelder and R.~Mead.
\newblock A simplex method for function minimization.
\newblock \emph{The Computer Journal}, 7\penalty0 (4):\penalty0 308--313, 1965.
\newblock \doi{10.1093/comjnl/7.4.308}.

\bibitem[Gal and Ghahramani(2016)]{gal2016}
Yarin Gal and Zoubin Ghahramani.
\newblock Dropout as a {Bayesian} approximation: Representing model uncertainty
  in deep learning.
\newblock In \emph{International Conference on Machine Learning}, pages
  1050--1059. PMLR, 2016.

\bibitem[Lakshminarayanan et~al.(2017)Lakshminarayanan, Pritzel, and
  Blundell]{lakshminarayanan2017}
Balaji Lakshminarayanan, Alexander Pritzel, and Charles Blundell.
\newblock Simple and scalable predictive uncertainty estimation using deep
  ensembles.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~30, 2017.

\bibitem[Nix and Weigend(1994)]{nix1994}
David~A. Nix and Andreas~S. Weigend.
\newblock Estimating the mean and variance of the target probability
  distribution.
\newblock In \emph{Proceedings of 1994 IEEE International Conference on Neural
  Networks}, volume~1, pages 55--60. IEEE, 1994.
\newblock \doi{10.1109/icnn.1994.374138}.

\bibitem[Kendall and Gal(2017)]{kendall2017}
Alex Kendall and Yarin Gal.
\newblock What uncertainties do we need in {Bayesian} deep learning for
  computer vision?
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~30, 2017.

\bibitem[Kuleshov et~al.(2018)Kuleshov, Fenner, and Ermon]{kuleshov2018}
Volodymyr Kuleshov, Nathan Fenner, and Stefano Ermon.
\newblock Accurate uncertainties for deep learning using calibrated regression.
\newblock In \emph{International Conference on Machine Learning}, pages
  2796--2804. PMLR, 2018.

\bibitem[Abdar et~al.(2021)Abdar, Pourpanah, Hussain, Rezazadegan, Liu,
  Ghavamzadeh, Fieguth, Cao, Khosravi, Acharya, Makarenkov, and
  Nahavandi]{abdar2021}
Moloud Abdar, Farhad Pourpanah, Sadiq Hussain, Dana Rezazadegan, Li~Liu,
  Mohammad Ghavamzadeh, Paul Fieguth, Xiaochun Cao, Abbas Khosravi, U.~Rajendra
  Acharya, Vladimir Makarenkov, and Saeid Nahavandi.
\newblock A review of uncertainty quantification in deep learning: Techniques,
  applications and challenges.
\newblock \emph{Information Fusion}, 76:\penalty0 243--297, 2021.
\newblock \doi{10.1016/j.inffus.2021.05.008}.

\bibitem[H{\"u}llermeier and Waegeman(2021)]{hullermeier2021}
Eyke H{\"u}llermeier and Willem Waegeman.
\newblock Aleatoric and epistemic uncertainty in machine learning: an
  introduction to concepts and methods.
\newblock \emph{Machine Learning}, 110:\penalty0 457--506, 2021.
\newblock \doi{10.1007/s10994-021-05946-3}.

\bibitem[Psaros et~al.(2023)Psaros, Meng, Zou, Guo, and
  Karniadakis]{psaros2023}
Apostolos~F. Psaros, Xuhui Meng, Zongren Zou, Ling Guo, and George~Em
  Karniadakis.
\newblock Uncertainty quantification in scientific machine learning: Methods,
  metrics, and comparisons.
\newblock \emph{Journal of Computational Physics}, 477:\penalty0 111902, 2023.
\newblock \doi{10.1016/j.jcp.2022.111902}.

\bibitem[Vovk et~al.(2005)Vovk, Gammerman, and Shafer]{vovk2005}
Vladimir Vovk, Alex Gammerman, and Glenn Shafer.
\newblock \emph{Algorithmic Learning in a Random World}.
\newblock Springer, 2005.
\newblock \doi{10.1007/b106715}.

\bibitem[Angelopoulos and Bates(2021)]{angelopoulos2021}
Anastasios~N. Angelopoulos and Stephen Bates.
\newblock A gentle introduction to conformal prediction and distribution-free
  uncertainty quantification.
\newblock \emph{arXiv preprint arXiv:2107.07511}, 2021.
\newblock \doi{10.48550/arxiv.2107.07511}.

\bibitem[Romano et~al.(2019)Romano, Patterson, and Cand{\`e}s]{romano2019}
Yaniv Romano, Evan Patterson, and Emmanuel Cand{\`e}s.
\newblock Conformalized quantile regression.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~32, 2019.

\bibitem[Sim et~al.(2019)Sim, Johnson, and Aspuru-Guzik]{sim2021}
Sukin Sim, Peter~D. Johnson, and Al{\'a}n Aspuru-Guzik.
\newblock Expressibility and entangling capability of parameterized quantum
  circuits for hybrid quantum-classical algorithms.
\newblock \emph{Advanced Quantum Technologies}, 2\penalty0 (12):\penalty0
  1900070, 2019.
\newblock \doi{10.1002/qute.201900070}.

\bibitem[Gilmer et~al.(2017)Gilmer, Schoenholz, Riley, Vinyals, and
  Dahl]{gilmer2017}
Justin Gilmer, Samuel~S. Schoenholz, Patrick~F. Riley, Oriol Vinyals, and
  George~E. Dahl.
\newblock Neural message passing for quantum chemistry.
\newblock In \emph{International Conference on Machine Learning}, pages
  1263--1272. PMLR, 2017.

\bibitem[Hamilton et~al.(2017)Hamilton, Ying, and Leskovec]{hamilton2017}
William~L. Hamilton, Rex Ying, and Jure Leskovec.
\newblock Inductive representation learning on large graphs.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~30, 2017.

\bibitem[Dai et~al.(2016)Dai, Dai, and Song]{dai2016}
Hanjun Dai, Bo~Dai, and Le~Song.
\newblock Discriminative embeddings of latent variable models for structured
  data.
\newblock In \emph{International Conference on Machine Learning}, pages
  723--732. PMLR, 2016.

\bibitem[You et~al.(2020)You, Chen, Sui, Chen, Wang, and Shen]{you2020}
Yuning You, Tianlong Chen, Yongduo Sui, Ting Chen, Zhangyang Wang, and Yang
  Shen.
\newblock Graph contrastive learning with augmentations.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~33, pages 5812--5823, 2020.

\bibitem[H{\"a}ner and Steiger(2017)]{haner2017petabyte}
Thomas H{\"a}ner and Damian~S. Steiger.
\newblock 0.5 petabyte simulation of a 45-qubit quantum circuit.
\newblock \emph{Proceedings of the International Conference for High
  Performance Computing, Networking, Storage and Analysis}, pages 1--10, 2017.
\newblock \doi{10.1145/3126908.3126947}.

\bibitem[Jones et~al.(2019)Jones, Brown, Bush, and Benjamin]{jones2019quest}
Tyson Jones, Anna Brown, Ian Bush, and Simon~C. Benjamin.
\newblock {QuEST} and high performance simulation of quantum computers.
\newblock \emph{Scientific Reports}, 9\penalty0 (1):\penalty0 10736, 2019.
\newblock \doi{10.1038/s41598-019-47174-9}.

\bibitem[Bayraktar et~al.(2023)Bayraktar, Charara, Clark, Cohen, Costa, Fang,
  Gao, Ghanem, Gleyzer, Hehn, Hohnerbach, Jones, Lubowe, Lykov, Morino, and
  Springer]{bayraktar2023cuquantum}
Hakan Bayraktar, Ali Charara, David Clark, Sherjil Cohen, Timothy Costa,
  Yong-Xin Fang, Yue Gao, Jim Ghanem, Gregory Gleyzer, Alex Hehn, Matthew
  Hohnerbach, Trent Jones, Tate Lubowe, Dmitry Lykov, Salvatore Morino, and
  Pete Springer.
\newblock cu{Q}uantum {SDK}: A high-performance library for accelerating
  quantum science.
\newblock In \emph{2023 IEEE International Conference on Quantum Computing and
  Engineering (QCE)}, pages 1050--1061. IEEE, 2023.
\newblock \doi{10.1109/QCE57702.2023.00121}.

\bibitem[Ba et~al.(2016)Ba, Kiros, and Hinton]{ba2016}
Jimmy~Lei Ba, Jamie~Ryan Kiros, and Geoffrey~E. Hinton.
\newblock Layer normalization.
\newblock \emph{arXiv preprint arXiv:1607.06450}, 2016.
\newblock \doi{10.48550/arxiv.1607.06450}.
\newblock URL \url{https://openreview.net/forum?id=BJLa_ZC9}.

\bibitem[Khosla et~al.(2020)Khosla, Teterwak, Wang, Sarna, Tian, Isola,
  Maschinot, Liu, and Krishnan]{khosla2020}
Prannay Khosla, Piotr Teterwak, Chen Wang, Aaron Sarna, Yonglong Tian, Phillip
  Isola, Aaron Maschinot, Ce~Liu, and Dilip Krishnan.
\newblock Supervised contrastive learning.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~33, pages 18661--18673, 2020.

\bibitem[Chen et~al.(2020)Chen, Kornblith, Norouzi, and Hinton]{chen2020simclr}
Ting Chen, Simon Kornblith, Mohammad Norouzi, and Geoffrey Hinton.
\newblock A simple framework for contrastive learning of visual
  representations.
\newblock In \emph{International Conference on Machine Learning}, pages
  1597--1607. PMLR, 2020.

\bibitem[van~den Oord et~al.(2018)van~den Oord, Li, and Vinyals]{oord2018}
A{\"a}ron van~den Oord, Yazhe Li, and Oriol Vinyals.
\newblock Representation learning with contrastive predictive coding.
\newblock \emph{arXiv preprint arXiv:1807.03748}, 2018.
\newblock \doi{10.48550/arxiv.1807.03748}.

\bibitem[Suresh et~al.(2021)Suresh, Li, Hao, and Nishi]{suresh2021}
Susheel Suresh, Pan Li, Cong Hao, and Georgia Nishi.
\newblock Adversarial graph augmentation to improve graph contrastive learning.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~34, pages 15920--15933, 2021.

\bibitem[Erd\H{o}s and R{\'e}nyi(1959)]{erdos1959}
P.~Erd\H{o}s and A.~R{\'e}nyi.
\newblock On random graphs {I}.
\newblock \emph{Publicationes Mathematicae Debrecen}, 6:\penalty0 290--297,
  1959.

\bibitem[Barab{\'a}si and Albert(1999)]{barabasi1999}
Albert-L{\'a}szl{\'o} Barab{\'a}si and R{\'e}ka Albert.
\newblock Emergence of scaling in random networks.
\newblock \emph{Science}, 286\penalty0 (5439):\penalty0 509--512, 1999.
\newblock \doi{10.1126/science.286.5439.509}.

\bibitem[Watts and Strogatz(1998)]{watts1998}
Duncan~J. Watts and Steven~H. Strogatz.
\newblock Collective dynamics of `small-world' networks.
\newblock \emph{Nature}, 393\penalty0 (6684):\penalty0 440--442, 1998.
\newblock \doi{10.1038/30918}.

\bibitem[Kingma and Ba(2015)]{kingma2015}
Diederik~P. Kingma and Jimmy Ba.
\newblock Adam: A method for stochastic optimization.
\newblock In \emph{International Conference on Learning Representations}, 2015.
\newblock URL \url{https://arxiv.org/abs/1412.6980}.

\bibitem[Bartlett et~al.(2017)Bartlett, Foster, and Telgarsky]{bartlett2017}
Peter~L. Bartlett, Dylan~J. Foster, and Matus~J. Telgarsky.
\newblock Spectrally-normalized margin bounds for neural networks.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~30, 2017.

\bibitem[Neyshabur et~al.(2018)Neyshabur, Bhojanapalli, and
  Srebro]{neyshabur2018}
Behnam Neyshabur, Srinadh Bhojanapalli, and Nathan Srebro.
\newblock A {PAC}-{B}ayesian approach to spectrally-normalized margin bounds
  for neural networks.
\newblock In \emph{International Conference on Learning Representations}, 2018.
\newblock URL \url{https://openreview.net/forum?id=Skz_WfbCZ}.

\bibitem[Golowich et~al.(2018)Golowich, Rakhlin, and Shamir]{golowich2018}
Noah Golowich, Alexander Rakhlin, and Ohad Shamir.
\newblock Size-independent sample complexity of neural networks.
\newblock In \emph{Proceedings of the 31st Conference on Learning Theory},
  pages 297--299, 2018.

\bibitem[Paszke et~al.(2019)Paszke, Gross, Massa, Lerer, Bradbury, Chanan,
  Killeen, Lin, Gimelshein, Antiga, Desmaison, K\"{o}pf, Yang, DeVito, Raison,
  Tejani, Chilamkurthy, Steiner, Fang, Bai, and Chintala]{paszke2019pytorch}
Adam Paszke, Sam Gross, Francisco Massa, Adam Lerer, James Bradbury, Gregory
  Chanan, Trevor Killeen, Zeming Lin, Natalia Gimelshein, Luca Antiga, Alban
  Desmaison, Andreas K\"{o}pf, Edward Yang, Zachary DeVito, Martin Raison,
  Alykhan Tejani, Sasank Chilamkurthy, Benoit Steiner, Lu~Fang, Junjie Bai, and
  Soumith Chintala.
\newblock Py{T}orch: An imperative style, high-performance deep learning
  library.
\newblock \emph{Advances in Neural Information Processing Systems}, 32, 2019.

\bibitem[Villani(2009)]{villani2009}
C{\'e}dric Villani.
\newblock \emph{Optimal Transport: Old and New}, volume 338.
\newblock Springer, 2009.
\newblock \doi{10.1007/978-3-540-71050-9}.

\bibitem[Peyr{\'e} and Cuturi(2019)]{peyre2019}
Gabriel Peyr{\'e} and Marco Cuturi.
\newblock Computational optimal transport: With applications to data science.
\newblock \emph{Foundations and Trends in Machine Learning}, 11\penalty0
  (5--6):\penalty0 355--607, 2019.
\newblock \doi{10.1561/2200000073}.

\bibitem[Dowson and Landau(1982)]{dowson1982}
D.~C. Dowson and B.~V. Landau.
\newblock The {Fr\'echet} distance between multivariate normal distributions.
\newblock \emph{Journal of Multivariate Analysis}, 12\penalty0 (3):\penalty0
  450--455, 1982.
\newblock \doi{10.1016/0047-259x(82)90077-x}.

\bibitem[Cuturi(2013)]{cuturi2013}
Marco Cuturi.
\newblock Sinkhorn distances: Lightspeed computation of optimal transport.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~26, 2013.

\bibitem[Frogner et~al.(2015)Frogner, Zhang, Mobahi, Araya, and
  Poggio]{frogner2015}
Charlie Frogner, Chiyuan Zhang, Hossein Mobahi, Mauricio Araya, and Tomaso
  Poggio.
\newblock Learning with a {Wasserstein} loss.
\newblock In \emph{Advances in Neural Information Processing Systems},
  volume~28, 2015.

\bibitem[Arjovsky et~al.(2017)Arjovsky, Chintala, and Bottou]{arjovsky2017}
Martin Arjovsky, Soumith Chintala, and L{\'e}on Bottou.
\newblock Wasserstein generative adversarial networks.
\newblock In \emph{International Conference on Machine Learning}, pages
  214--223. PMLR, 2017.

\bibitem[Nguyen and Jing(2025)]{nguyen2025zassenhaus}
Molena Nguyen and Naihuan Jing.
\newblock Zassenhaus expansion in solving the {Schr\"odinger} equation.
\newblock \emph{arXiv preprint arXiv:2505.09441}, 2025.
\newblock \doi{10.48550/arXiv.2505.09441}.

\bibitem[Jing and Nguyen(2026)]{jing2026complexity}
Naihuan Jing and Molena Nguyen.
\newblock Complexity bounds for Hamiltonian simulation in unitary
  representations.
\newblock \emph{arXiv preprint arXiv:2603.07231}, 2026.
\newblock \doi{10.48550/arXiv.2603.07231}.

\end{thebibliography}

% ============================================================
\clearpage
\beginsupplement
\section*{Supplementary Information}
\addcontentsline{toc}{section}{Supplementary Information}

\noindent\textit{Uncertainty-Calibrated Trust Regions for Query-Efficient QAOA
Parameter Search.}  This Supplementary Information collects the full notation,
extended derivations and proofs, complete hyperparameter and training
specifications, the high-performance-computing microbenchmark methodology and
results, the Python/C++ cross-validation evidence, the reproducibility
manifest, an extended discussion of related work, and a glossary.  Every number
reported here is produced by the deterministic artifact described in
\cref{sec:reproducibility}; no quantity is hand-entered.

% ------------------------------------------------------------
\section{Notation and symbols}
\label{app:notation}

\Cref{tab:notation} collects the symbols used throughout the manuscript.
Vectors are column vectors; $\mathrm{diag}(\cdot)$ forms a diagonal matrix from
a vector; $\|\cdot\|_2$ is the Euclidean norm; $\Pi_{\Theta_p}$ denotes modular
projection onto the parameter domain $\Theta_p$.

\begin{table}[t]
\caption{Summary of notation.\label{tab:notation}}
\centering
\scriptsize
\begin{tabular}{@{}ll@{}}
\toprule
Symbol & Meaning \\
\midrule
$G=(V,E)$ & undirected graph with vertex set $V$, edge set $E$ \\
$n,\,m$ & number of vertices $|V|$ and edges $|E|$ \\
$p$ & QAOA depth (number of layers) \\
$\theta=(\gamma,\beta)$ & QAOA parameter vector in $\Theta_p=[0,2\pi)^p\times[0,\pi)^p$ \\
$H_C(G),\,H_M$ & cost and mixer Hamiltonians \\
$f_G(\theta)$ & exact QAOA objective $\bra{\psi_G(\theta)}H_C\ket{\psi_G(\theta)}$ \\
$C_G^\star$ & optimal MaxCut value of $G$ \\
$r_G(\theta)$ & approximation ratio $f_G(\theta)/C_G^\star$ \\
$Q$ & objective-query budget (number of distinct $\theta$ evaluated) \\
$S$ & shots per query in the finite-shot model \\
$q_\phi(\theta\mid G)$ & predicted Gaussian $\mathcal{N}(\mu_\phi(G),\Sigma_\phi(G))$ \\
$\mu_\phi(G),\,\Sigma_\phi(G)$ & predicted mean and diagonal covariance \\
$\mathcal{T}_{\phi,\rho}(G)$ & trust region of radius $\rho$ \\
$\Sigma_{\mathrm{post}},\mu_{\mathrm{post}}$ & fused posterior covariance and mean \\
$\Sigma_s,\mu_s$ & source $s\in\{\mathrm{GIN},\mathrm{loc},\mathrm{glob},\mathrm{TQA}\}$ \\
$\delta_j$ & coordinate refinement step size for dimension $j$ \\
$f(G)\in\mathbb{R}^8$ & graph feature vector, \eqref{eq:features} \\
$\theta^{\mathrm{TQA}}$ & trotterized quantum annealing schedule, \eqref{eq:tqa} \\
$\theta^{\mathrm{ref}}_i$ & offline reference parameters for training graph $i$ \\
$\alpha_G(\varepsilon)$ & near-optimal proposal mass in \cref{thm:bestofk} \\
$L_G$ & local Lipschitz constant of $f_G$ \\
$\lambda$ & trust-region ridge term for numerical stability \\
$\eta_{\mathrm{wd}}$ & weight-decay coefficient \\
\bottomrule
\end{tabular}
\end{table}

% ------------------------------------------------------------
\section{Extended derivations and proofs}
\label{app:proofs}

\subsection{Precision-weighted posterior fusion}
\label{app:fusion}

We derive the four-source fusion rule used in \cref{alg:gctr}.  Treat each
source $s$ as supplying an independent Gaussian pseudo-likelihood
$\mathcal{N}(\theta;\mu_s,\Sigma_s)$ over the angle vector $\theta$.  The
product of Gaussians is, up to normalization, proportional to
\begin{equation}
    \prod_s \exp\!\Big(-\tfrac12(\theta-\mu_s)^{\!\mathsf T}\Sigma_s^{-1}(\theta-\mu_s)\Big)
    =\exp\!\Big(-\tfrac12\big(\theta^{\!\mathsf T}\Sigma_{\mathrm{post}}^{-1}\theta
    -2\theta^{\!\mathsf T}\textstyle\sum_s\Sigma_s^{-1}\mu_s\big)+\text{const}\Big),
\end{equation}
where collecting the quadratic and linear terms gives the precision-weighted
combination
\begin{equation}
    \Sigma_{\mathrm{post}}^{-1}=\sum_s\Sigma_s^{-1},
    \qquad
    \mu_{\mathrm{post}}=\Sigma_{\mathrm{post}}\sum_s\Sigma_s^{-1}\mu_s.
    \label{eq:fusion_app}
\end{equation}
Because all covariances are diagonal, \eqref{eq:fusion_app} factorizes over the
$2p$ coordinates: for coordinate $k$,
$[\mu_{\mathrm{post}}]_k=\big(\sum_s [\Sigma_s]_{kk}^{-1}[\mu_s]_k\big)\big/
\big(\sum_s[\Sigma_s]_{kk}^{-1}\big)$.  This shows explicitly that a source with
small predicted variance on coordinate $k$ dominates the fused mean on that
coordinate, which is the intended behaviour: confident sources pull harder.
The calibration floor $\Sigma'_{\mathrm{GIN}}=\max(\Sigma_{\mathrm{GIN}},
\Sigma_{\mathrm{loc}})$ prevents an overconfident neural head from collapsing
the posterior onto an unreliable mean before any objective query is spent.

\subsection{Optimality of fusion and convergence of refinement}
\label{app:fusion_opt}

\paragraph{Proof of \cref{prop:fusion_opt}.}
Write $\widehat\theta=\sum_s W_s\mu_s$. Unbiasedness for every $\theta^\star$ requires
$\mathbb{E}[\widehat\theta]=\big(\sum_s W_s\big)\theta^\star=\theta^\star$, i.e.\
$\sum_s W_s=I$. Under this constraint the error is
$\widehat\theta-\theta^\star=\sum_s W_s\varepsilon_s$, and by mutual independence its
covariance is $\sum_s W_s\Sigma_s W_s^{\mathsf T}$. Let
$W_s^\star=\Sigma_{\mathrm{post}}\Sigma_s^{-1}$ with
$\Sigma_{\mathrm{post}}=(\sum_s\Sigma_s^{-1})^{-1}$; note $\sum_s W_s^\star
=\Sigma_{\mathrm{post}}\sum_s\Sigma_s^{-1}=I$, so $W^\star$ is feasible, and
$\sum_s W_s^\star\Sigma_s W_s^{\star\mathsf T}
=\Sigma_{\mathrm{post}}\big(\sum_s\Sigma_s^{-1}\big)\Sigma_{\mathrm{post}}
=\Sigma_{\mathrm{post}}$. Any feasible competitor is $W_s=W_s^\star+D_s$ with
$\sum_s D_s=0$. Because the cross term vanishes,
\begin{equation}
\sum_s W_s^\star\Sigma_s D_s^{\mathsf T}
=\Sigma_{\mathrm{post}}\sum_s\Sigma_s^{-1}\Sigma_s D_s^{\mathsf T}
=\Sigma_{\mathrm{post}}\Big(\sum_s D_s\Big)^{\mathsf T}=0,
\end{equation}
the error covariance separates as
$\sum_s W_s\Sigma_s W_s^{\mathsf T}
=\Sigma_{\mathrm{post}}+\sum_s D_s\Sigma_s D_s^{\mathsf T}
\succeq\Sigma_{\mathrm{post}}$,
with equality iff every $D_s=0$ (as $\Sigma_s\succ0$). This is Loewner-minimality and
uniqueness. Finally, the negative log-likelihood of the independent Gaussian model is
$\tfrac12\sum_s(\mu_s-\theta)^{\mathsf T}\Sigma_s^{-1}(\mu_s-\theta)$ up to constants;
its stationarity condition $\big(\sum_s\Sigma_s^{-1}\big)\theta=\sum_s\Sigma_s^{-1}\mu_s$
gives $\widehat\theta_{\mathrm{ML}}=\Sigma_{\mathrm{post}}\sum_s\Sigma_s^{-1}\mu_s
=\mu_{\mathrm{post}}$, which under a flat prior is also the posterior mean. \hfill$\square$

\paragraph{Proof of \cref{prop:coord_convergence}.}
The incumbent is updated only on a strict improvement $y>\widehat y$, so
$\{\widehat y_t\}$ is nondecreasing; since $0\le f_G\le|E|$ it is bounded above, and a
bounded monotone sequence converges. Suppose a full pass over $j=1,\dots,2p$ and signs
$\eta\in\{+1,-1\}$ yields no strict improvement. Then for every coordinate $j$,
$f_G\big(\Pi_{\Theta_p}(\widehat\theta\pm\delta_j e_j)\big)\le\widehat y
=f_G(\widehat\theta)$, which is the definition of a $\delta$-approximate
coordinate-wise local maximum at the current step vector. Each nonimproving pass halves
$\delta$, so in the unbounded-budget process $\delta\to0$; if $\widehat\theta^\infty$ is
a limit point at which $f_G$ is differentiable, the inequality
$f_G(\widehat\theta^\infty)\ge f_G(\widehat\theta^\infty\pm\delta e_j)$ in the limit
$\delta\downarrow0$ forces $\partial f_G/\partial\theta_j=0$ for all interior
coordinates $j$, i.e.\ coordinate-wise stationarity. \hfill$\square$

\subsection{Lipschitz lower bound on near-optimal mass}
\label{app:lipschitz}

We justify the scaling relation
$\alpha_G(\varepsilon)\gtrsim(\varepsilon/L_G)^d/(\rho^d\det(\Sigma_\phi+\lambda I)^{1/2})$
of \cref{sec:theory}.  Suppose $f_G$ is locally $L_G$-Lipschitz near the
trust-region optimum $\theta^{\mathcal T}$, so $f_G(\theta)\ge
f_G^{\mathcal T}-L_G\|\theta-\theta^{\mathcal T}\|_2$.  Then any
$\theta$ within Euclidean distance $\varepsilon/L_G$ of $\theta^{\mathcal T}$ is
$\varepsilon$-optimal, i.e.\ the ball $B(\theta^{\mathcal T},\varepsilon/L_G)$ is
contained in $\mathcal{A}_{G,\varepsilon}$.  For a proposal that is
approximately uniform over the ellipsoidal trust region of volume
$\propto\rho^d\det(\Sigma_\phi+\lambda I)^{1/2}$ (with $d=2p$), the mass it
assigns to $\mathcal{A}_{G,\varepsilon}$ is at least the volume ratio
\begin{equation}
    \alpha_G(\varepsilon)\;\gtrsim\;
    \frac{\mathrm{vol}\,B(\theta^{\mathcal T},\varepsilon/L_G)}
         {\mathrm{vol}\,\mathcal{T}_{\phi,\rho}(G)}
    \;\propto\;
    \frac{(\varepsilon/L_G)^d}{\rho^d\det(\Sigma_\phi+\lambda I)^{1/2}},
\end{equation}
valid when $\varepsilon/L_G$ is small relative to the trust-region extent so
that the ball lies inside the region.  Substituting into \cref{thm:bestofk}
yields the query-count scaling: tighter calibrated covariance (smaller
$\det(\Sigma_\phi+\lambda I)$) and smaller radius $\rho$ both reduce the number
of candidates needed to reach the $\varepsilon$-optimal set.  The empirical
$L_G\approx 114$ and volume ratio $4.72\times10^{-4}$ of \cref{tab:bounds}
instantiate these constants on the benchmark.

\subsection{Full finite-shot argument}
\label{app:finiteshot}

We expand the proof sketch of \cref{thm:best-of-k-shot}.  Let
$\widehat f_{G,S}(\theta)=\tfrac1S\sum_{a=1}^S C_G(z_a)$ be the $S$-shot
estimator with $z_a\sim|\braket{z_a}{\psi_G(\theta)}|^2$ and
$C_G(z_a)\in[0,|E|]$.  By Hoeffding's inequality, for any fixed $\theta$,
\begin{equation}
    \Pr\!\big[\,|\widehat f_{G,S}(\theta)-f_G(\theta)|\ge t\,\big]
    \le 2\exp\!\Big(-\tfrac{2S t^2}{|E|^2}\Big),
\end{equation}
so $\widehat f_{G,S}(\theta)-f_G(\theta)$ is sub-Gaussian with proxy variance
$\sigma_G^2/S$ and $\sigma_G^2\le|E|^2/4$ (assumption~A3).  Setting
$t=\varepsilon/2$ and taking a union bound over the $K$ evaluated candidates,
the probability that any estimate deviates by more than $\varepsilon/2$ is at
most $2K\exp(-S\varepsilon^2/(2|E|^2))$, which is $\le\delta/2$ whenever
$S\ge 2|E|^2\log(4K/\delta)/\varepsilon^2$; using $\sigma_G^2\le|E|^2/4$ this is
implied by the stated $S\ge 32\sigma_G^2\log(4K/\delta)/\varepsilon^2$.  By
\cref{thm:bestofk} applied at tolerance $\varepsilon/2$ with
$K\ge\log(2/\delta)/\alpha_G(\varepsilon/2)$, the best \emph{exact} candidate is
within $\varepsilon/2$ of $f_G^{\mathcal T}$ with probability $\ge1-\delta/2$.
On the complement of both failure events (total probability $\ge1-\delta$), the
candidate selected by the noisy objective is within $\varepsilon/2+\varepsilon/2
=\varepsilon$ of $f_G^{\mathcal T}$, establishing the claim.  This separates the
parameter-query cost $K$ from the shot cost $S$: a method can place good
candidates with few queries yet still need many shots to rank them reliably,
exactly the regime quantified by \cref{tab:finite_shots}.

% ------------------------------------------------------------
\section{Complete hyperparameters and training protocol}
\label{app:hyperparams}

\Cref{tab:hyperparams} lists every hyperparameter required to reproduce the
GIN predictor and the search policy.  All values are fixed before test-time
evaluation and selected on the validation split only.

\begin{table}[t]
\caption{Complete hyperparameter specification.\label{tab:hyperparams}}
\centering
\scriptsize
\begin{tabular}{@{}lll@{}}
\toprule
Group & Hyperparameter & Value \\
\midrule
GIN & message-passing layers $L$ & $4$ \\
    & hidden width $h$ & $64$ \\
    & MLP depth per layer & $2$ (layer-normalized) \\
    & learnable $\varepsilon^{(\ell)}$ & initialized to $0$ \\
    & graph pooling & mean $\oplus$ max concatenation \\
    & covariance floor $\sigma_{\min}^2$ & $10^{-4}$ \\
\midrule
Training & optimizer & Adam~\cite{kingma2015} \\
         & learning rate & $3\times10^{-4}$ \\
         & schedule & cosine annealing \\
         & max epochs & $1500$ \\
         & early-stopping patience & $300$ \\
         & gradient clip ($\ell_2$) & $1.0$ \\
         & weight decay $\eta_{\mathrm{wd}}$ & $10^{-4}$ \\
         & loss & Gaussian NLL \\
\midrule
Search & depth $p$ (main) & $3$ \\
       & matched query budget $Q$ & $18$ \\
       & local prior neighbours $k$ & $5$ \\
       & step-size map & $\mathrm{clip}(0.5\sqrt{[\Sigma_{\mathrm{post}}]_{jj}},0.05,0.30)$ \\
       & trust-region ridge $\lambda$ & $10^{-3}$ \\
       & TQA total time $T$ & validation-selected \\
\midrule
Data & training graphs & $240$ ($60$/family) \\
     & families & ER, 3-regular, BA, WS \\
     & sizes $n$ & $\{8,10,12,14\}$ \\
     & test instances (main) & $8$ ($2$/family) \\
     & global seed & \texttt{260424803} \\
\bottomrule
\end{tabular}
\end{table}

% ------------------------------------------------------------
\section{High-performance backend: methodology and full results}
\label{app:hpc}

\paragraph{Methodology.}
The C++20 reference backend implements the same exact-statevector evaluator as
the Python reference, with a narrow kernel boundary (\cref{tab:backend_contract}).
Microbenchmarks are compiled with \texttt{-O3 -std=c++20 -march=native
-pthread} on the platform of \cref{tab:platform} and report wall-clock time
averaged over repeated batched evaluations after a warm-up pass.  Thread
scaling is measured on a fixed batch of $144$ queries at $n{=}14$; per-query
latency scaling is measured by sweeping $n$ at single-thread settings.

\paragraph{Single-query cost scales as $\mathcal{O}(2^{n})$.}
\Cref{tab:single_query} reports single-thread mean per-query time as a function
of $n$.  Each increment of two qubits multiplies the cost by roughly four,
consistent with the $2^{n}$ amplitude-vector length and confirming the
complexity analysis of \cref{sec:complexity}.

\begin{table}[t]
\caption{Single-thread mean per-query evaluation time of the C++ reference
backend as a function of graph size $n$ at $p{=}2$, $Q{=}18$.  Each $+2$ qubits
multiplies cost by $\approx4$, matching $\mathcal{O}(2^{n})$.
\label{tab:single_query}}
\centering
\scriptsize
\begin{tabular}{@{}rrr@{}}
\toprule
$n$ & Mean per-query (ms) & Statevector (bytes) \\
\midrule
$8$  & $0.0103$  & $4{,}096$ \\
$10$ & $0.0446$  & $16{,}384$ \\
$12$ & $0.1996$  & $65{,}536$ \\
$14$ & $0.8371$  & $262{,}144$ \\
$16$ & $3.6947$  & $1{,}048{,}576$ \\
$18$ & $15.9691$ & $4{,}194{,}304$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Thread scaling.}
\Cref{tab:thread_scaling} reports speedup from batched parallel evaluation of
$144$ queries at $n{=}14$.  The backend reaches $4.30\times$ at $8$ threads;
the sublinear scaling beyond $4$ threads reflects memory-bandwidth pressure
from the statevector sweeps rather than scheduling overhead.

\begin{table}[t]
\caption{Thread scaling of batched evaluation ($144$ queries, $n{=}14$).
\label{tab:thread_scaling}}
\centering
\scriptsize
\begin{tabular}{@{}rrr@{}}
\toprule
Threads & Total time (ms) & Speedup \\
\midrule
$1$ & $126.15$ & $1.00\times$ \\
$2$ & $71.10$  & $1.77\times$ \\
$4$ & $38.82$  & $3.25\times$ \\
$8$ & $29.33$  & $4.30\times$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Per-kernel cost.}
\Cref{tab:kernel_cost} decomposes the per-layer cost at $n{=}14$.  The mixer
kernel (strided $2\times2$ rotations over all qubits) is $2.60\times$ more
expensive than the bandwidth-bound cost-phase kernel, identifying it as the
primary target for future SIMD/accelerator optimization.

\begin{table}[t]
\caption{Per-layer kernel cost at $n{=}14$.\label{tab:kernel_cost}}
\centering
\scriptsize
\begin{tabular}{@{}lll@{}}
\toprule
Kernel & ms / layer & Characterization \\
\midrule
Cost phase & $0.1821$ & element-wise $e^{-i\gamma C(z)}\psi$, bandwidth-bound \\
Mixer      & $0.4736$ & strided $2\times2$ rotations over $14$ qubits \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Per-query latency invariance under threading.}
A single query's latency is essentially constant ($\approx25.8$~ms at $n{=}14$)
across $1$, $2$, $4$, and $8$ threads, confirming that parallelism is harvested
\emph{across} the candidate batch rather than within a single statevector
evaluation. This is the intended design: the optimizer emits a deterministic
candidate stream and the backend parallelizes over independent graph--angle
pairs without altering per-query numerics.

\paragraph{Per-family Python backend timing.}
\Cref{tab:family_timing} reports the Python reference-backend timing broken
down by graph family at $n{=}14$, $Q{=}18$.  Per-query time is stable across
families ($\approx1.40$--$1.50$~ms), confirming that evaluation cost depends on
the amplitude-vector length ($2^{n}$) and depth, not on graph topology; the
small residual differences track edge count in the cost-phase kernel.

\begin{table}[t]
\caption{Python reference-backend timing by graph family at $n{=}14$, $Q{=}18$
(single thread).  Per-query time is topology-independent; per-instance time
includes feature extraction, candidate generation, and all $18$ evaluations.
\label{tab:family_timing}}
\centering
\scriptsize
\begin{tabular}{@{}lrr@{}}
\toprule
Family & Mean per-query (ms) & Per-instance (ms) \\
\midrule
Erd\H{o}s--R\'enyi      & $1.499$ & $28.69$ \\
3-regular               & $1.401$ & $26.71$ \\
Barab\'asi--Albert      & $1.426$ & $27.13$ \\
Watts--Strogatz         & $1.429$ & $26.94$ \\
\bottomrule
\end{tabular}
\end{table}

% ------------------------------------------------------------
\section{Python/C++ cross-validation}
\label{app:validation}

A faster backend is scientifically admissible only if it reproduces the
reference numerics bit-for-bit under the reproducibility contract.
\Cref{tab:validation} reports the maximum absolute objective discrepancy
between the Python and C++ evaluators and the thread-invariance error (the
discrepancy between $1$-thread and multi-thread C++ runs) across all tested
$(n,p)$ configurations.  All discrepancies are at the level of double-precision
round-off ($\sim10^{-13}$) and the thread-invariance error is exactly zero,
confirming deterministic reductions.

\begin{table}[t]
\caption{Python/C++ cross-validation: maximum absolute objective error and
thread-invariance error.  All configurations pass at double-precision
tolerance.\label{tab:validation}}
\centering
\scriptsize
\begin{tabular}{@{}rrrrc@{}}
\toprule
$n$ & $p$ & Max abs.\ error & Thread-invariance error & Pass \\
\midrule
$8$  & $3$ & $4.33\times10^{-13}$ & $0$ & \checkmark \\
$8$  & $6$ & $3.47\times10^{-13}$ & $0$ & \checkmark \\
$8$  & $7$ & $2.52\times10^{-13}$ & $0$ & \checkmark \\
$10$ & $3$ & $4.80\times10^{-14}$ & $0$ & \checkmark \\
$10$ & $6$ & $7.82\times10^{-14}$ & $0$ & \checkmark \\
$10$ & $7$ & $3.57\times10^{-13}$ & $0$ & \checkmark \\
$12$ & $3$ & $4.76\times10^{-13}$ & $0$ & \checkmark \\
$12$ & $6$ & $2.17\times10^{-13}$ & $0$ & \checkmark \\
$12$ & $7$ & $2.81\times10^{-13}$ & $0$ & \checkmark \\
$14$ & $3$ & $5.29\times10^{-13}$ & $0$ & \checkmark \\
$14$ & $6$ & $3.13\times10^{-13}$ & $0$ & \checkmark \\
$14$ & $7$ & $3.55\times10^{-14}$ & $0$ & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

% ------------------------------------------------------------
\section{Reproducibility manifest}
\label{app:manifest}

\Cref{tab:manifest} maps each reported quantity to the script that generates
it.  Running \texttt{python code/generate\_all.py} regenerates all outputs
end-to-end; \texttt{bash code/reproduce.sh smoke} runs a fast subset for
continuous-integration checks.  Every script is seeded by the global seed
\texttt{260424803} and writes its outputs to \texttt{code/tables/} or
\texttt{code/figures/}.

\begin{table}[t]
\caption{Artifact manifest: manuscript element $\to$ generating script.
\label{tab:manifest}}
\centering
\scriptsize
\begin{tabular}{@{}ll@{}}
\toprule
Manuscript element & Generating script \\
\midrule
\cref{tab:efficiency} (main benchmark) & \path{trace_evaluations.py} \\
\cref{tab:expanded} ($48$-instance replication) & \path{table01_expanded.py} \\
\cref{fig:expanded} (expanded benchmark) & \path{fig05_expanded_benchmark.py} \\
\cref{tab:ablation} (ablation) & \path{table03_ablation.py} \\
\cref{tab:bounds} (bound verification) & \path{table02_bound_verification.py} \\
\cref{tab:higher_depth} ($p{=}6,7$) & \path{scripts} / \path{results/summary_p*.csv} \\
\cref{tab:finite_shots} (finite shots) & finite-shot driver \\
\cref{tab:single_query},~\ref{tab:thread_scaling},~\ref{tab:kernel_cost} & \path{bench_cpu.py}, \path{bench_cpu.cpp} \\
\cref{tab:validation} (Py/C++ check) & \path{results/python_cpp_validation.csv} \\
\cref{fig:statevector_grid} & \path{fig01_statevector_grid.py} \\
\cref{fig:query_curve} & \path{scripts/make_figures.py} \\
\cref{fig:ablation} & \path{fig03_heldout_quality.py} \\
\cref{fig:landscape_trust} & \path{fig04_trust_region_concept.py} \\
\bottomrule
\end{tabular}
\end{table}

% ------------------------------------------------------------
\section{Extended discussion of related work}
\label{app:related_extended}

This appendix expands \cref{sec:related} along the axes most relevant to a
machine-learning audience.

\paragraph{Amortized versus per-instance optimization.}
Bayesian optimization and evolutionary strategies are \emph{per-instance}: they
build a surrogate or population from scratch for every new graph and spend the
budget refining it.  \UQQAOA{} is \emph{amortized}: the cost of learning the
trust-region geometry is paid once during offline training and reused across
all test instances, so the entire test-time budget is spent on direct objective
evaluation.  The two are complementary---the learned trust region can seed a
subsequent per-instance optimizer when additional budget is available.

\paragraph{Uncertainty as geometry versus uncertainty as a score.}
Most uncertainty-quantification work in deep learning treats predictive
variance as a confidence score for downstream filtering or active learning.
The contribution here is to treat the predicted diagonal covariance as an
\emph{operational metric}: it sets anisotropic step sizes, defines the
admissible search ellipsoid, and (in the adaptive-budget mode) allocates the
number of queries.  This converts a distributional object into concrete
search-policy decisions.

\paragraph{Position relative to learning-assisted QAOA.}
Prior learning-assisted QAOA methods predict a point or warm start and then
hand control to a generic optimizer.  The distinguishing feature here is the
explicit geometric pathway from learned uncertainty to query allocation,
combined with a physics-informed safety prefix that guarantees the policy never
underperforms the strongest single-source prior at early budgets by
construction.

% ------------------------------------------------------------
\section{Glossary}
\label{app:glossary}

\begin{description}[leftmargin=2.4cm,style=nextline,nosep]
\item[QAOA] Quantum Approximate Optimization Algorithm: a depth-$p$ variational
quantum circuit optimized by a classical outer loop.
\item[MaxCut] the combinatorial problem of partitioning vertices to maximize
the number (or weight) of cut edges; the benchmark objective.
\item[GIN] Graph Isomorphism Network: a maximally expressive first-order
message-passing graph neural network.
\item[TQA] Trotterized Quantum Annealing: a depth-only linear-ramp schedule
used as a physics-informed prior and early-budget safety prefix.
\item[Trust region] a calibrated ellipsoid in parameter space, defined by the
predicted covariance, to which local search is restricted.
\item[Precision-weighted fusion] inverse-variance combination of independent
Gaussian sources into a single posterior mean and covariance.
\item[Query budget $Q$] the number of distinct parameter vectors evaluated on
the (simulated) quantum objective; the primary resource accounted for.
\item[Approximation ratio] objective value divided by the optimal cut value
$C_G^\star$; the primary quality metric.
\item[ECE] Expected Calibration Error: a scalar summary of probabilistic
miscalibration.
\item[NLL] Negative Log-Likelihood: the Gaussian training loss for the
predictor.
\end{description}

\end{document}
